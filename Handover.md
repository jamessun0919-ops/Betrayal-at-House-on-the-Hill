# 交接文檔 Handover

最後更新：2026-08-25（第 2 次工作階段：**遊戲訊息彈窗流程與機制——7 個任務全數實作完成、整分支審查通過、已合併進 `main` 並推送 origin**。確認執行方式 Subagent-Driven，開獨立 worktree，依序完成：Task 1（`movePlayerTo` 回傳是否首次進入）、Task 2（串 4 條路徑——移動/開門/傳送/`move_to_room` 效果——到 `game:roomEntered` 的新欄位 `enteredNewRoom`，implementer 自行抓到並修正 2 個計畫未列出、被新欄位打壞的既有測試）、Task 3（`game:itemUseResolved` 廣播）、Task 4（`CheckModal.jsx` 換成播放 `ROLLDICE.mp4`＋考驗結果改用卡片 `feedbacktextDice`）、Task 5（`DebugGameScreen.jsx` 佇列邏輯擴充）、Task 6（抽出 `SimplePopup.jsx` 共用元件）、Task 7（`CharacterPanel.jsx` 道具選單補上 description）。7 個任務各自審查一次通過。**整分支最終審查（Opus 模型）抓到 3 個 Important＋2 個 Minor**：①`roomIntro` 彈窗對 5 間起始房間（二樓平台/地下平台/大廳C）會跳出空白內文——設計文件當初查證「52/52 房間都有 description」只查了 `rooms.json`，沒查到 `starting-rooms.json` 這份獨立檔案，這 5 間完全沒有 description/text，而三個 `move_to_room` 樓梯目的地剛好全部是起始房，代表幾乎每個玩家第一次走樓梯都會中獎；②`game:itemUseResolved` 原本只廣播給效果承受者，點擊使用的人（尤其對他人使用道具時）完全收不到任何回饋；③擲骰動畫影片沒加 `muted`/`playsInline`，手機瀏覽器自動播放可能被擋住卡在第一幀。兩個 Important 需要開發者決定方向，已用 `AskUserQuestion` 確認：**itemUseResolved 改成使用者與承受者兩邊都會收到彈窗**；**擲骰動畫影片加 `muted`（拿掉音效）＋`playsInline`＋`onError` 防呆，換取手機自動播放穩定**。連同 2 個 Minor（`activatedOnUse` 的預兆卡在抽到當下就會被判定成「已無考驗」跳出過早的結果彈窗，改成排除這類卡片；`feedbackText` 空字串會誤退回通用句子，改用 `!= null` 判斷）一次派遣修正波次，複審全數 ADDRESSED、無新增破壞，560/560 測試全綠。合併回本地 `main`（fast-forward），推送 origin。**手動瀏覽器驗證遇到一個環境限制**：`preview_start` 工具啟動伺服器時 `cwd` 固定綁在主要 repo 根目錄，不會跟著 worktree 走——在 worktree 內測試時實際點到的是合併前的舊程式碼，一度誤判 Task 7 的 description 沒生效。跟開發者確認後改成「先合併回 main、再在主目錄測試」，合併後用 JS 直接操作 DOM（畫面本身因環境問題點擊/截圖逾時失效，跟 2026-08-17 記錄的已知限制一致）走完角色選擇→進遊戲→驗證 `roomIntro`（真實描述文字）／`itemDrawNoCheck`（既有行為不變）／`itemUseResolved`（`待補充` fallback）三種彈窗實際渲染正確，console/伺服器皆無錯誤。`eventIntro`/`eventNoCheck` 與 `CheckModal` 影片/`feedbacktextDice` 路徑因 RNG 導向沒能手動點到，依賴自動化測試＋2 輪程式碼審查佐證，已誠實告知開發者這個限制。收工前清理了本次啟動的 preview 伺服器與一個 Task 3 派工時遺留的殘留 jest 行程，確認無殘留 node 行程。

最後更新：2026-08-25（第 1 次工作階段：**遊戲訊息彈窗流程與機制——設計文件與實作計畫已完成並 commit，尚未進入實作**。開發者提供極詳細的規格（房間/事件彈窗、道具使用彈窗、襲擊/反擊僅記錄流程留給 M3），走 `brainstorming` 流程分 4 段確認設計（整體架構／房間/事件彈窗／道具使用彈窗／範圍排除），5 輪 `AskUserQuestion` 確認邊界（佇列共用一條／房間彈窗只給實際移動的玩家／無考驗事件直接接 feedbacktextOccur／有骰子分支的道具走既有考驗佇列／omen 之後會補跟 event 一樣的 `feedbacktextDice`/`feedbacktextOccur` 欄位，目前沒有就顯示「待補充」）。設計文件 [2026-08-25-game-message-popup-design.md](docs/superpowers/specs/2026-08-25-game-message-popup-design.md)、實作計畫 [2026-08-25-game-message-popup.md](docs/superpowers/plans/2026-08-25-game-message-popup.md)（7 任務）。**寫計畫過程中查證程式碼發現一個關鍵簡化**：開場靜態資料 `cardContent`（`game:started` 一次性廣播）本來就完整包含 `feedbacktextDice`/`feedbacktextOccur`（未過濾），所以文字比對邏輯全部可以放在前端，伺服器只需要送最小的 id 訊號（`game:roomEntered` 新增 `enteredNewRoom` 布林欄位、新增 `game:itemUseResolved` 廣播），不需要伺服器重複做一次文字查找。**機制摘要**：擴充既有 `pendingCheckQueue`/`CheckModal` 機制成 6 種佇列項目（`roomIntro`/`eventIntro`/`eventNoCheck`/`leaveCheck`/`collapseCheck`/`cardCheck`/`itemUseResolved`，共用同一條先進先出佇列）；`CheckModal` 的擲骰動畫從純文字佔位改成播放 `ROLLDICE.mp4`，考驗結果改用卡片自己的 `feedbacktextDice`（房間類考驗維持原本通用文字不變）；新增共用元件 `SimplePopup.jsx`。**範圍排除**：襲擊/反擊（開發者原始規格 3/4 節）只在設計文件附錄原樣記錄，不設計機制細節、不寫程式碼，留給 M3。**下次開工從執行方式已確認為 Subagent-Driven，直接開始派工 Task 1**。

最後更新：2026-08-23（第 2 次工作階段：**道具手持/配戴機制——已完成並合併進 `main`**。確認執行方式為 Subagent-Driven，開獨立 worktree，依序完成 4 個任務：Task 1（`wieldedWeaponId`/`wornGearIds` 欄位＋`clearEquipStateIfNeeded`＋給予/遺留/物品攜帶上限強制遺留三處整合）、Task 2（`wield`/`unwield`/`wear`/`unwear` 四種道具動作＋`socketHandlers.js` 解析 `itemCategory`）、Task 3（`item_010` 油燈改成配戴中才觸發擲骰介入＋開場自動裝備，過程中 implementer 發現一個真實的計畫缺口並詢問——`gameManager.test.js` 的 `baseStartArgs()` 預設沒有 `cards` 欄位，計畫的程式碼片段沒防呆，已確認並補上 `cards &&` 保護後繼續）、Task 4（前端 `CharacterPanel.jsx` 道具選單依 category 動態決定選項）。4 個任務各自審核一次通過。**整分支最終審查（Opus 模型）抓到 1 個 Critical＋1 個 Important，根因相同**：計畫誤判「預兆卡沒有 category 欄位，五分類邏輯不會影響到它」，但實際查證 `data/cards/omen-cards.json` 發現全部 13 張預兆卡都有 `category`（10 張 `general`、2 張 `weapon`、1 張 `gear`），導致 10 張預兆卡的「使用」按鈕消失（違反設計文件「預兆卡選單完全不動」的明確要求）、3 張預兆卡多出必定失敗的「手持/配戴」按鈕（伺服器只從 `content.cards.items` 查 category，查不到預兆卡）。已用 `isOmenCard` 判斷優先於 category 邏輯修復，兩個問題一次解決。另修 3 個 Minor（`handleLoseItem`/`consumesItem`/`consumeItemIfApplied` 這三條道具移除路徑沒清除裝備狀態，改成把 `clearEquipStateIfNeeded` 移進 `removeItem()` 本體讓所有現在/未來的呼叫點都自動涵蓋；`gameManager.js` 的 `cards.items` 容錯跟同函式裡的 `createGameState` 呼叫不一致，補齊；前端傳了一個伺服器根本不採信的 `itemCategory` 欄位，移除避免誤導）。複審通過，554/554 測試全綠。**手動瀏覽器實測**（潔西卡・摩根角色，開場帶 gear 類「安全帽」）：確認開場自動配戴生效（選單直接顯示「取下」而非「配戴」）、配戴/取下切換正確、console 無錯誤；因隨機考驗/RNG 導向明確武器與預兆卡的路徑較耗時，未逐一實測武器手持與預兆卡選單，改依賴自動化測試（554/554，含新增的 omen 相關測試）＋2 輪程式碼審查（task 級＋整分支＋複審）佐證，已告知開發者這個限制。合併回 `main` 並推送 origin。

最後更新：2026-08-23（第 1 次工作階段：**道具手持/配戴機制——設計文件與實作計畫已完成並 commit，尚未進入實作**。走 `brainstorming` 流程確認範圍：查證現有程式碼後發現「手持」/「配戴」不是現有「使用」按鈕的改名，而是全新的跨回合裝備狀態（開發者確認：手持跟配戴不衝突，武器最多手持一件，裝備可同時配戴多件、無上限）。設計文件 [2026-08-23-item-equip-mechanism-design.md](docs/superpowers/specs/2026-08-23-item-equip-mechanism-design.md)、實作計畫 [2026-08-23-item-equip-mechanism.md](docs/superpowers/plans/2026-08-23-item-equip-mechanism.md)。**機制摘要**：`player` 新增 `wieldedWeaponId`（單一武器 id 或 `null`）／`wornGearIds`（裝備 id 陣列，無上限）；`selectAction` 新增 `wield`/`unwield`/`wear`/`unwear` 四種道具動作，各扣 1 行動力，換持自動頂替不需先手動取下；道具離開背包（給予／自願遺留／物品攜帶上限強制遺留）時同步清除裝備狀態；`item_010`（油燈）既有的擲骰介入效果從「持有即可用」改成「配戴中才觸發」（刻意的行為改變，已確認）；角色初始道具若是 weapon/gear，開場自動裝備；前端 `CharacterPanel.jsx` 的道具選單改成依 `category` 動態決定選項組合（`weapon`→手持/取下、`gear`→配戴/取下、`consumable`/`reusable`→使用、`decoration`→無使用類選項，皆保留給予/遺留）。**範圍排除**：攻擊機制本身（`actionType:'attack'` 實際邏輯、傷害計算）與個別 gear 卡片的被動效果（`item_008`/`item_024`/`item_031`/`item_037`）不在這次範圍內，留給未來（多半綁在 M3）。4 個任務的計畫已寫完，**下次開工從執行方式選擇（Subagent-Driven / Inline）開始**。

最後更新：2026-08-22（第 1 次工作階段：**道具/事件卡內容整理（category 重新分類、weapon 攻擊欄位骨架、回饋文字抽取）——資料層已完成並 commit，機制層（動態選單/配戴機制/attackStat 實際數值）列為待辦**。開場稽核 `item`/`event`/`omen` 三份卡片檔案的完整度，開發者確認兩張 `decoration` 類道具（`item_014`/`item_015`）沒有 `text` 是設計本身如此，不是缺漏。

**⚠️ 本階段發生一次嚴重事故，已建立防範規則，務必先讀**：為了把 `category` 值 `Reusable` 改成小寫，agent 用「讀取整個 JSON → 修改物件 → `JSON.stringify` 整檔寫回」的方式改 `item-cards.json`，導致整檔排版被打亂；agent 想用 `git checkout -- <file>` 復原，但這份檔案本來就是開發者還沒 commit 的進行中編輯，`checkout` 只會退回「最後一次 commit」，不是「agent 改壞之前」——結果把開發者尚未提交的道具卡編輯（新的五分類系統，48 張卡）整個清空，git 完全沒有留下可復原的版本。**開發者自行手動復原了內容**。已存進 agent 記憶（`feedback_json_data_file_edits.md`）的防範規則：①改動任何 JSON 資料檔案一律用精準文字取代（Edit 工具的 exact old_string/new_string），禁止「解析成物件→`JSON.stringify`整檔寫回」；②絕不對有未提交變更的檔案使用 `git checkout --`／`git reset --hard`／`git clean` 這類指令來「復原自己的錯誤」；③批次腳本編輯前一律先備份到 scratchpad。**下一步行動第一項會再提一次補強措施**（scratchpad 額度中斷風險）。

**道具卡 `category` 重新分類定案（開發者提供，已記錄）**：五分類，各自對應不同的道具選單選項——`weapon` 武器（手持／給予／遺留，需標明攻擊用哪項能力/是否影響擲骰）、`gear` 裝備（配戴／給予／遺留，配戴中選項切換為取下——**這個機制目前完全沒實作**，`turnFlow.js` 只有 use/give/leave/pickup 四種道具動作，沒有配戴/取下的概念）、`consumable` 消耗品（使用／給予／遺留，用一次消失除非 `text` 說明返回牌堆）、`reusable` 重複使用（使用／給予／遺留，可重複使用）、`decoration` 裝飾品（給予／遺留，無效果純平衡/彩蛋）。`item-cards.json` 全數 48 張已依此分類（`Reusable`→`reusable` 統一小寫）。**道具選單要改成依 category 動態決定顯示選項，取代現在寫死的邏輯——這是程式碼待辦，這次只處理資料**。

**weapon 攻擊欄位骨架已建立（9 張武器卡，值尚未填）**：新增 `attackStat`/`attackDice` 兩個欄位（皆為 `null` 占位），M3 傷害/襲擊機制會讀取。開發者說明：無效果型武器（`item_041` 華麗匕首，文字寫「裝備後無任何效果」）會自己填入「基礎模式力量考驗＋無額外骰子」；有額外機制的武器（`item_001`／`item_012`／`item_020`／`item_035`，各自有消耗其他道具/多重考驗/偷竊等複雜機制）之後會跟 agent 討論怎麼把 `text` 的機制轉寫成 `effects`——**這兩項都還沒做，列入下一步行動**。

**「」回饋文字抽取已完成（item 15 張、event 27 張），只做 item／event，omen 開發者說還要再調整、這次不動**：`text` 欄位裡用「」框住、原本是彈窗要顯示給玩家的回饋訊息，已抽取到新欄位：單一結果（無骰數分支）用 `feedbacktextOccur`（純字串，命名從 `feedbacktextUse` 改成 `feedbacktextOccur`——開發者指出這個名字對道具跟事件都適用，不只限定「使用」動作）；有骰數分支的用 `feedbacktextDice`（dict，key 是骰數區間字串，例如 `"5+"`／`"0-4"`／`"0"`，格式跟現有 `effects.tiers` 的 `min`/`max` 精神一致但不要求知道確切上限）。過程中額外發現並修正 5 處半形引號 `｢｣` 打字錯誤（`item_040`／`event_009`／`event_033` 一致使用半形、`event_001`／`event_010` 混用半形開全形閉），已統一成全形 `「」`。兩個特例（開發者確認過）：`item_044` 有限手套是「電腦隨機選 1~7 觸發對應效果」不是骰數分支，key 改用 `"1"`~`"7"`；`event_029` 濃煙密布的第一段「濃煙」是標記名稱引用不是玩家回饋，只抽第二段。**抽取規則**：只移除 `「...」` span 本身，不做任何額外標點清理（骰數條件文字即使後面沒有機制文字也要完整保留，例如 `item_009` 的「骰數０至４：」即使沒有其他機制敘述也不能被清掉——這是開發者明確指示的規則，之後如果要再抽別的欄位要延用同一條規則）。**執行方式**：先做完整乾跑（dry-run，把結果存成純文字檔給開發者看過確認）才實際寫入，寫入用逐行文字比對取代整檔重寫（跟本節開頭事故的教訓一致），寫入後跟備份逐行 diff 確認只有目標行被改動。524/524 測試全綠。

**新的檔案協作慣例定案（取代原本 `img/` 慣例）**：開發者已把 `img/` 資料夾改名為 `developersketch/`，作為統一的個人暫存資料夾（圖片/影片/prompt 筆記，**不 commit**，已加進 `.gitignore`）。原本開發者提議「你手寫草稿放 developersketch，agent 讀了再同步進 data」的流程，討論後發現有瑕疵：兩邊分開會各自往前跑、失去共同基準，agent 修好的東西開發者不知道，可能被覆蓋或重複修改。**改成**：`data/cards/*.json`／`data/characters/characters.json`／`data/rooms/rooms.json` 這幾份雙方都會直接編輯的內容檔案，**開發者跟 agent 直接共用同一份 `data/` 檔案作業，不再分開兩份**；developersketch 底下原本重複的這幾份 JSON 副本已刪除。**commit 節奏改為小量多次**：agent 改完一批、測試確認沒問題後就整理成一次 commit，**但 commit 前一定要提醒開發者確認，開發者說要自己看過才 commit，不是 agent 自主決定 commit 時機**。這樣任何一次意外的影響範圍只會是「最近這一小批還沒 commit 的改動」，比之前「整個工作階段結束才一次 commit」的風險小很多。

最後更新：2026-08-21（第 1 次工作階段：**物品攜帶數量上限機制——已完成並合併進 `main`**。走完整 `brainstorming`→`writing-plans`→`subagent-driven-development` 流程（4 任務＋整分支審查，開獨立 worktree）。設計文件 [2026-08-21-item-carry-limit-design.md](docs/superpowers/specs/2026-08-21-item-carry-limit-design.md)、實作計畫 [2026-08-21-item-carry-limit.md](docs/superpowers/plans/2026-08-21-item-carry-limit.md)。**機制**：玩家能攜帶的道具卡數量上限＝目前力量值（不含預兆卡），取得道具（不論搜索/撿取/給予/卡片效果，皆已統一經由 `addItem()` 進背包）後若超過上限，開啟 `resolverEntry.pendingInventoryChoice` 暫停狀態（沿用既有 `promptState.js` 通用機制），廣播 `game:inventoryChoicePending` 讓玩家選一件遺留（不扣行動力），逾時 20 秒預設自動遺留剛取得那批道具中最後一件仍持有的；一次取得多件超過上限會連續開多輪，直到回到上限內；`game:move`/`game:selectAction`/`game:useStairs`/`game:endTurn` 四個既有守衛點都會擋下。4 個任務全數一次審查通過，**整分支最終審查（Opus 模型）抓到 2 個 Important**：①彈窗原本廣播給全房間玩家，但「給予」道具時只有接收方能操作，其他人點了必定失敗，已改成只有目標玩家才顯示；②`resolveCardDraw` 有一條直接進背包的路徑完全沒經過上限檢查（目前遊戲資料不會走到這條路徑，但補上避免未來房間設定改用 `drawType:"item"` 時留地雷）。另修 2 個 Minor（give/pickup 分支重複的 `game:stateUpdate` 廣播、Task 1 產出但無人呼叫的死碼 `countHeldItems`，皆已刪除）＋補 1 個測試（4 個守衛點都要擋下），複審通過。**開發者確認一個用字問題**：新功能文字原本統一寫成「遷留」（非標準中文詞，`AI` 生成文字疑似顯示/判讀誤差），實測發現本專案既有道具選單（使用/遺留/取消）早就用「遺留」，已跨設計文件/實作計畫/伺服器/前端 4 個檔案統一改正。524/524 測試全綠，已合併進 `main` 並推送 origin。過程中一個工具使用教訓（重複發生）：`Agent` 工具的 `isolation:"worktree"` 參數即使已明確在 prompt 裡指定正確工作目錄，仍會建立另一個獨立 worktree——已用 `cherry-pick` 修正回正確分支；之後的任務派工全部改成不傳這個參數。手動瀏覽器實測物品上限彈窗因房間離開檢定（RNG）反覆失敗，沒能在真實遊玩流程觸發到滿版溢位彈窗（僅確認頁面/角色選擇/移動/考驗/道具選單皆正常運作、無 console 錯誤），改以完整的 socket.io 端對端自動化測試（非 mock）＋2 輪程式碼審查（task 級 + 整分支）確認彈窗邏輯正確性。收工前已確認清空本次啟動的 `node.exe` 行程。

**下次開工前務必先讀「下一步行動」的新 10 項待辦清單**（2026-08-21 重新整理，取代舊清單；已完成的「物品攜帶數量上限」已移出清單）。

最後更新：2026-08-20（第 4 次工作階段：「先判斷合理房型再抽房間卡」——走完整 `brainstorming`→`writing-plans`→`subagent-driven-development` 流程並合併進 `main`；開門時優先抽一張門型不需要退化就能放置的房間卡（重用 `computeDoorLayout` 當可行性檢查器），大幅減少（但無法完全消除）房間退化成單門房的情況。全分支審查抓到 1 個 Critical：新機制打破了舞廳/包廂配對重抽迴圈原本隱含的「重抽一定會前進」保證，特定盤面下會讓伺服器永久卡死——審查實際寫了 repro 腳本驗證確實會發生，已修復（排除已拒絕過的房間 id）並補上終止性論證＋帶 timeout 保護的迴圈測試，複審通過，516/516 測試全綠。第 3 次工作階段：「房間圖片旋轉機制」——`canonicalDoors` 資料填完（含修正 `room_kitchen`/`room_gallery` 2 項缺口）後走完整 `brainstorming`→`writing-plans`→`subagent-driven-development` 流程並合併進 `main`；伺服器端第一次放置房間時算好旋轉角度存進房間實例，前端中心房間格套用；鄰房預覽帶明確排除不套用。全分支審查抓到 1 個 Critical（新旋轉機制跟既有的「房間退化成單門」fallback 規則衝突，模擬 500 局遊戲全部會中途拋錯），已修復並複審通過；瀏覽器實測（transform matrix 精確比對）確認旋轉與縮放疊加正確。新增待辦「先判斷合理房型再抽房間卡」。過程中兩次遇到 `TaskStop` 回報成功但底層 node 行程沒真的被殺掉，皆已用 PowerShell 排查清除。第 2 次工作階段：「開門/進新房間行動力扣除規則調整」走完整 `brainstorming`→`writing-plans`→`subagent-driven-development` 流程並合併進 `main`——歸零改成固定扣 2 點、行動力 ≥2 才給開門選項、開門後不再強制結束回合；全分支審查零 Critical/Important 程式碼問題，496/496 測試全綠，並實機雙人遊戲驗證通過；已修正 Handover 內 3 處描述舊規則的文字。第 1 次工作階段：房間圖片旋轉機制 `brainstorming` 進行中，暫停等待開發者填寫資料——`rooms.json` 52 間房間已加上 `canonicalDoors` 佔位欄位；查核房間美術圖：`rooms.json` 52 間全數有效，`starting-rooms.json` 的地下平台仍缺圖；新增 3 項待辦（遊戲訊息彈窗流程、事件卡/道具卡品項補充、描述補充）。2026-08-19 第 3 次工作階段：手機版面測試階段，CharacterPanel 狀態面板大改版〔角色形象框、行動力 8 格顯示、道具/預兆 3×4 格網、px 改百分比 RWD〕；視野區域新增房間格局部縮放功能（全域鎖死原生縮放、5 段固定倍率＋拖動查看四角）；移除除錯框線；記錄擲骰機制異常待查。第 2 次工作階段：修復開發者編輯後的 `rooms.json` 語法錯誤與內容缺口〔美術圖 filename／`item` 欄位／描述欄位／崩塌房間 id 復原〕，同步 38 張房間美術圖進 `client/public/images/rooms/`；記錄 2 項待 brainstorm 的新機制待辦（開門行動力改扣 2、物品攜帶上限＝力量值）；排查並修復手機透過家用路由器連線失敗的根因——`client/.env.development` 殘留檔案寫死 `VITE_SERVER_URL` 蓋掉動態網址邏輯，已刪除；`vite.config.js` 改用明確的 `0.0.0.0` 綁定。第 1 次工作階段：手機測試連線修正＋icon 調整；搜索機制 7 項 Minor 建議處理完畢〔4 修復/1 列待辦/2 純提醒〕；道具「給予」選項完成；房間多重行動機制〔`actions` 結構化清單、LobbyC 下樓、包廂房/崩塌房間跳下統一消耗行動力〕走完整 `brainstorming`→`writing-plans`→`subagent-driven-development` 全流程完成並合併）

**`summon-control-and-item-drop`、房間獨立小任務、`dice-interjection`（Part A + Part B）、M2D1（大廳流程）、M2D2（角色選擇正式畫面）、M2D3 後端資料串接計畫、大門廳整合＋遊戲開始畫面、M2D3 一般房間地圖骨架、`lobby:players` 競態條件修復、房間／角色資料稽核與內容補完、手機直式版面改版、三樓層＋座標式房間連接機制、leaveCheck 失敗扣屬性引擎缺口、角色圖示串接與版面調整（背景圖／立繪／ICON）、統一考驗彈窗機制＋訊息欄可讀化、統一考驗彈窗機制 6 項 Minor 修正、道具合成機制（廚房烹飪配方）、搜索機制（取代 item 類型房間自動抽卡）＋7 項 Minor、道具「給予」選項、房間多重行動機制（`actions` 結構化清單）、開門/進新房間行動力扣除規則調整（固定扣 2 點）、房間圖片旋轉機制（中心房間格）、先判斷合理房型再抽房間卡皆已完成並合併進 `main`**：所有 worktree 與分支（本機與遠端）都已依標準流程清理刪除。目前工作目錄就是 `main`，無待接續的 worktree。

## 專案目標 (Project Goal)
將實體桌遊「山中小屋」(Betrayal at House on the Hill) 移植為可供多位使用者同時連線遊玩的網頁遊戲，兼具技術學習與朋友圈實際遊玩用途，並保留未來擴充原創劇本與 AI 玩家的彈性。

## 已完成進度 (Completed)
- 設計文件、MVP 里程碑拆分（M1-M4）、選定的兩個原版劇本，皆已確認（[spec 文件](docs/superpowers/specs/2026-07-31-web-multiplayer-design.md)）
- **M1（伺服器與大廳骨架）已全部完成並合併進 `main`**：PR [#1](https://github.com/jamessun0919-ops/Betrayal-at-House-on-the-Hill/pull/1)，已 merge
- **M2（探索引擎）內容範圍已確認**：M2/M3 邊界＝M2 房間拼圖/移動/屬性/卡片抽取/預兆計數，M3 鬼屋降臨＋劇本外掛＋劇本1/10；內容資料已全部由開發者填完（[data/rooms/rooms.json](data/rooms/rooms.json) 31筆、[data/cards/](data/cards/) 三類卡片）；兩份核心設計參考文件（[card-mechanics-reference.md](docs/superpowers/specs/2026-08-01-card-mechanics-reference.md)、[turn-flow-and-action-points.md](docs/superpowers/specs/2026-08-01-turn-flow-and-action-points.md)）
- **M2 拆成子計畫依序執行**（M2a/M2b/M2c，是 M2 內部分階段；M2c 又再細分 M2c-1~M2c-5，見下方「下一步行動」）：
  - **M2a：遊戲核心狀態＋房間版圖系統 —— 已完成並合併進 `main`**：`contentLoader.js`、`doorLayout.js`、`boardGenerator.js`、`playerEntity.js`（刻度制屬性）、`gameState.js`
  - **M2b：提問協定＋回合流程 —— 已全部完成並合併進 `main`**（M2b-1 核心邏輯、M2b-2 Socket 整合＋除錯頁面）：`roomDeck.js`、`promptState.js`、`characterSelection.js`/`characterSelectionManager.js`、`gameManager.js`、`turnFlow.js`（樓梯免費、`advanceTurn` 自動重設行動力）、`lobbyManager.js`（host 追蹤）、`socketHandlers.js` 選角色與回合事件、`DebugGameScreen.jsx`
  - **M2c：卡牌牌庫＋效果解析器**：
    - 設計文件：[docs/superpowers/specs/2026-08-05-m2c-card-effect-resolver-design.md](docs/superpowers/specs/2026-08-05-m2c-card-effect-resolver-design.md)
    - **M2c-1（純邏輯模組）—— 已完成並合併進 `main`**：[計畫](docs/superpowers/plans/2026-08-05-m2c1-card-effect-core-logic.md)，7 任務全數完成（inline execution）——`cardDeck.js`、`effectPipeline.js`（自訂骰面 **0/0/1/1/2/2**）、`modifiers.js`、`effectResolver.js`（`stat_change`/`grant_item`/`lose_item`/`persistent_modifier`/`dice_check`/`choice`；`peek_and_reorder` 故意留 `UNSUPPORTED_EFFECT_TYPE`，等有實際卡片需求再補）、`playerEntity.js` 新增 `addItem`/`removeItem`
    - **M2c-2（Socket 整合）—— 已完成並合併進 `main`**：[計畫](docs/superpowers/plans/2026-08-05-m2c2-socket-integration.md)，7 任務全數完成（inline execution）＋獨立整分支審查（見下方「除錯注意事項」）——`effectResolverManager.js`（`{promptState, pendingChoice}`，生命週期跟 `gameState` 一致）、`gameState.js`/`gameManager.js` 擴充事件/道具/預兆牌庫、`socketHandlers.js` 的 `game:move` 自動抽卡解析、`game:effectPromptRespond`＋真實逾時計時器、除錯頁面顯示。**移除**了 M2b-2 的 `game:pendingCardDraw` 廣播，改用 `game:cardDrawn`/`game:effectResolved`/`game:effectPendingChoice`
    - **M2c-4/M2c-5（道具/操作動作接線＋邪祟考驗機制）—— 已完成、已通過獨立審查（含 1 輪修正）、已合併進 `main`**：[spec](docs/superpowers/specs/2026-08-05-m2c4-m2c5-action-and-haunt-design.md)、[計畫](docs/superpowers/plans/2026-08-05-m2c4-m2c5-action-and-haunt.md)——`item-cards.json`/`omen-cards.json` 補 `category`（武器/消耗品/一般）/`canTargetOthers` 欄位（開發者已對照實體卡片全數確認正確，無需修改）、`effectResolver.js` 新增 `appliedCount` 回傳值、`turnFlow.js` 的 `selectAction` 接上道具/操作真實邏輯、`socketHandlers.js` 的 `cardId`→`sourceId` 改名（`game:cardDrawn` 例外保留 `cardId`）＋`consumeItemIfApplied` 參數、`game:selectAction` 接上真實效果解析、`resolveCardDraw` 加入邪祟考驗。測試全綠（290/290）
    - **M2c-3（36 張卡片＋房間操作類 effects 內容）—— 進行中，22 張已草擬並驗證，等開發者審核；2 張（犬靈＋道具給予/遺留機制）的設計文件已寫完等開發者過目**：agent 依 `card-mechanics-reference.md` 逐卡分類，跟開發者確認多個機制缺口的處理方向後，草擬了 22 張卡片/房間的 `effects` 內容——`item_003`（治療藥膏）/`item_004`（嗅鹽）/`item_009`（魔術方塊）、`omen_002`（書）/`omen_003`（水晶球）/`omen_005`（女孩）/`omen_006`（聖符）/`omen_007`（瘋漢）/`omen_008`（面具）/`omen_009`（徽章，維持無效果）、`event_001`（腐敗惡臭）/`event_004`（電池耗盡）/`event_005`（不起眼的櫃子）/`event_006`（滴答聲）/`event_007`（祈禱聲）/`event_009`（挑釁的幻覺）/`event_010`（電話鈴聲）、`room_vault`（保險庫）。已用一次性腳本載入真實內容、多組骰值/選項分支實際跑過 `resolveEffects` 驗證無誤才提交。`omen-cards.json` 全部 13 張已補上 `needsCustomLogic` 欄位（原本完全沒有這個欄位）。
      過程中新增的機制（詳見下方除錯注意事項）：`draw_card`（隨機抽 N 張卡）、`preview_and_choose`/`take_previewed_card`（水晶球：展示牌庫前 3 張供選一張，牌庫順序不調整）、`toggle_active`（面具：可泛用的「生效狀態」切換）、**預兆牌現在會跟道具一樣被加進玩家背包**（水晶球/面具這類需要「持有後主動使用」的預兆牌因此可以透過 `game:selectAction actionType:'item'` 使用）、`removeWhen` 可以填陣列（OR 邏輯）、`persistent_modifier` 新增 `blocksOpenDoor` 效果（電池耗盡）、`checkRemoveConditions` 終於接上真實呼叫點（移動後檢查同房間、任何效果解析後檢查持有道具）。也修好 3 個既有缺口：`restoreToBase` 會誤降已達基本值以上的數值、`dice_check` 的骰數調整沒有 `[1,8]` 上下限、`persistent_modifier` 的 `removeWhen` 原本被誤設為強制欄位。
      其餘卡片維持 `needsCustomLogic:true`（M3 傷害/攻擊系統、房間結束回合/離開考驗觸發點等機制缺口，詳見下方除錯注意事項）
    - **M2c-3 附屬功能：召喚物操控切換＋道具給予/遺留/撿取（`summon-control-and-item-drop`）—— 全部完成，已合併進 `main`**：設計文件 [docs/superpowers/specs/2026-08-09-summon-control-and-item-drop-design.md](docs/superpowers/specs/2026-08-09-summon-control-and-item-drop-design.md)、實作計畫 [docs/superpowers/plans/2026-08-10-summon-control-and-item-drop.md](docs/superpowers/plans/2026-08-10-summon-control-and-item-drop.md)（7 任務，用 `subagent-driven-development` 在獨立 worktree 執行，每任務都經過獨立審查）：
      - Task 1（`droppedItems` 房間欄位）、Task 2（`switch_control` 效果類型）、Task 3（道具 give/leave/pickup，一般玩家）、Task 4（`moveSummon`/`selectSummonAction`，含一輪修正：`advanceTurn` 安全網原本會弄丟召喚物身上攜帶的道具）、Task 6（`socketHandlers.js` 接上 `player.summons` 分流＋`mode` 透傳）、Task 7（`omen_004` 犬靈卡片內容）——皆完成並通過審查
      - **Task 5（回合手動結束機制，全體玩家）—— 執行期間新增的任務，不在原計畫裡**：Task 4 審查時發現，犬靈道具（`switch_control`）本身要花 1 點行動力，若玩家使用當下剩 1 點，行動力歸零會自動觸發回合結束，剛建立的召喚物瞬間被摧毀。跟開發者確認後決定**全面改為手動結束回合**（不限召喚物情境）：新增 `endTurn`/`game:endTurn`，移除 `advanceTurnIfOver` 及其 4 個呼叫點，`isTurnOver` 保留但不再被自動呼叫。**前端「結束回合」按鈕本來規劃留到 M2d，但全分支審查發現除錯頁面完全無法結束回合會讓多回合遊玩完全跑不動，已補了一個最小按鈕到 `DebugGameScreen.jsx`（見下方全分支審查段落），完整的行動選單 UI 仍留給 M2d**
      - **全分支最終審查（合併前，見下方除錯注意事項的新段落）發現 1 個 Critical＋4 個 Important，已全數修復並重新審查通過，349/349→358/358**
    - **房間獨立小任務（結束回合被動加成＋離開房間前考驗）—— 已完成並合併進 `main`**：直接在 `main` 上用 TDD 逐項實作（範圍小，未開 worktree）。禮拜堂/圖書室/食品儲藏室/健身房的「結束回合獲得加成」（`onceOnlyPerPlayer`）終於接上 `game:endTurn` 觸發點；塔橋/雜亂的房間/藤蔓糾纏的溫室的「離開房間前考驗」用新的 `leaveCheck` 房間欄位＋`moveToRoom` 新參數實作，兩種離開方式（移動到已知房間／開新門）都會被擋，失敗只扣 1 點行動力（跟正常移動花費一樣，不會多扣），可以當回合重試。3 個 commit，364/364 測試全綠。
    - **`dice-interjection`（可被道具介入的擲骰）Part A + Part B —— 全部完成並合併進 `main`，功能至此完整**：
      - **Part A（`dice_check` 路徑，卡片/房間操作觸發）**：設計文件 [docs/superpowers/specs/2026-08-10-dice-interjection-design.md](docs/superpowers/specs/2026-08-10-dice-interjection-design.md)、實作計畫 [docs/superpowers/plans/2026-08-10-dice-interjection-part-a.md](docs/superpowers/plans/2026-08-10-dice-interjection-part-a.md)（7 任務，`subagent-driven-development` 在獨立 worktree 執行）。天使羽毛（`item_005`，擲骰前自選 0~8 覆蓋結果）、詭異人偶（`item_006`，擲骰前多骰兩顆，代價意志-1，每回合限一次）、蠟燭（`item_010`，僅事件卡觸發的考驗可用，多骰一顆）三張卡現在能在擲骰前跳出詢問視窗讓玩家決定要不要介入。幸運兔腳（`item_007`，擲骰後重骰）已依開發者指示直接從遊戲移除，不做。全分支最終審查發現 3 個 Important（`overrideValue` 未驗證就先消耗道具、`pendingRollChoice`／`pendingChoice` 共存導致永久卡死房間、除錯頁面完全沒有新事件監聽）＋1 個 Minor（`sourceDeckType` 沒有在恢復流程中保留），全數修復並重新審查通過，391/391 測試全綠。
      - **Part B（`leaveCheck` 路徑，離開房間前考驗）—— 已完成並合併進 `main`**：設計文件 [docs/superpowers/specs/2026-08-11-dice-interjection-part-b-design.md](docs/superpowers/specs/2026-08-11-dice-interjection-part-b-design.md)、實作計畫 [docs/superpowers/plans/2026-08-11-dice-interjection-part-b.md](docs/superpowers/plans/2026-08-11-dice-interjection-part-b.md)（4 任務，`subagent-driven-development` 在獨立 worktree 執行）。天使羽毛/詭異人偶現在也能用在塔橋/雜亂的房間/藤蔓糾纏的溫室的離開房間前考驗上，完全重用 Part A 寫好的 `computeInterjectedRoll`（`effectResolver.js` export 出來）＋既有 `pendingRollChoice` 機制（新增 `resumeKind:'leaveCheck'` 分支），沒有另建平行機制。全分支最終審查發現 1 個 Important（`moveToRoom` 直接擲骰路徑〔沒有道具介入時〕沒有套用房間/玩家的持續性修正 modifier，但新的介入/逾時路徑會套用，兩條路徑不一致——現行出貨內容還踩不到，屬潛伏性缺陷）＋1 個 Minor（測試註解算術錯誤），跟開發者確認方向後修復並通過 scoped re-review，399/399 測試全綠。`item_010` 蠟燭因為 `scope:"eventTriggered"` 天生不適用於 `leaveCheck`（非事件卡觸發），無需額外排除邏輯，`item-cards.json` 完全沒改。
  - **M2D（簡易使用者介面，新里程碑）—— 開發者已確認討論方式：一次把整個 M2D 討論完，執行階段拆成多份計畫依序做**。範圍已細分：M2D1（大廳流程，本次完成）、M2D2（角色選擇正式畫面，等開發者補角色圖片後另外提出規劃）、M2D3 之後（遊戲進行中畫面，即原本 Handover 列的地圖/屬性/道具/行動選單/擲骰介入等項目）
    - **M2D1（大廳流程：開頭頁面／建立/進入大廳／等候室）—— 已完成並合併進 `main`**：設計文件 [docs/superpowers/specs/2026-08-11-m2d1-lobby-flow-design.md](docs/superpowers/specs/2026-08-11-m2d1-lobby-flow-design.md)、實作計畫 [docs/superpowers/plans/2026-08-11-m2d1-lobby-flow.md](docs/superpowers/plans/2026-08-11-m2d1-lobby-flow.md)（6 任務，`subagent-driven-development` 在獨立 worktree 執行）。取代原本陽春的 `LobbyScreen.jsx`：開頭頁面（`Gate.png` 全螢幕背景＋建立/進入大廳按鈕）、暱稱輸入視窗、大廳列表（依房主暱稱瀏覽可加入的房間，進入時拉一次＋手動刷新，不即時更新）、等候室（房主/一般玩家看到不同按鈕）、房主按「準備完成」後的角色選擇開發中佔位畫面（M2D2 正式畫面待補）。**房主權限轉移已依開發者指示簡化為不做**：房主主動退出或斷線，整個大廳直接解散，所有人被踢回開頭頁面。目標裝置為手機橫向螢幕，響應式設計。後端新增 `lobby:list`（可加入房間查詢）、`lobby:leave`（主動離開，區分房主/一般玩家）、`lobby:closed` 廣播、`LobbyManager` 的 `isHost`/`getHostName`/`closeRoom`/`listJoinableRooms`。全分支最終審查發現 2 個 Important（大廳列表/等候室有 4 處直接把伺服器原始錯誤代碼顯示給玩家，沒走既有翻譯對照表；背景圖片 8.1MB 過大，合併後會永久留在 git 歷史）＋1 處設計文件過時敘述，全數修復並通過 scoped re-review，412/412 測試全綠。
    - **M2D2（角色選擇正式畫面）—— 已完成並合併進 `main`**：設計文件 [docs/superpowers/specs/2026-08-12-m2d2-character-select-design.md](docs/superpowers/specs/2026-08-12-m2d2-character-select-design.md)、實作計畫 [docs/superpowers/plans/2026-08-12-m2d2-character-select.md](docs/superpowers/plans/2026-08-12-m2d2-character-select.md)（3 任務，`subagent-driven-development` 在獨立 worktree 執行）。取代 M2D1 的 `CharacterSelectPlaceholder.jsx`：6 個角色橫向排開的可捲動畫廊（`client/src/lobby/CharacterSelectScreen.jsx`），圖片高度依角色 `tall` 屬性依比例縮放（最高者基準畫面高度 2/3）；點擊角色跳出屬性資料卡 modal（姓名/職業/4 項能力/初始道具，左翻/右翻/退出/確定選擇四顆按鈕）；維持輪流制但允許自由瀏覽，只有「確定選擇」被輪到誰才會亮起；確定後鎖定並廣播，未輪到者的圖片疊加「已被選擇」徽章。**`LobbyScreen.jsx` 重構為單一協調層**：`game:characterSelectUpdate`/`game:prompt`/`game:promptResolved`/`game:started` 四個監聽都收斂到協調層（原本分散在 `WaitingRoomScreen`/`DebugGameScreen`，導致一次性事件在畫面切換過程中可能被漏接），`DebugGameScreen` 新增 `initialGameState` prop 由協調層餵入 `game:started` payload。全分支最終審查發現 2 個 Important（3 種狀態訊息只做了 2 分支，玩家看自己已確認的角色會誤顯示「其他玩家選擇中」；6 張角色圖合計約 37MB 即將永久進入 git 歷史）皆已修復（新增最高優先的 `isMine` 分支＋非錯誤樣式；PowerShell `System.Drawing` 縮圖到約 600px 寬、合計約 4.5MB），412/412 測試全綠。**CSS 除錯教訓（詳見下方除錯注意事項）**：「已被選擇」徽章寬度異常是選擇器特異性衝突，不是容器寬度不明確的問題，最終靠獨立靜態 HTML/CSS repro（脫離遊戲流程／不受 30 秒逾時干擾）配合 `getComputedStyle` 實測才正確定位。
    - **M2D3 一般房間地圖骨架（大門廳以外的其餘房間）—— 已完成並合併進 `main`**：設計文件 [docs/superpowers/specs/2026-08-14-m2d3-general-room-skeleton-design.md](docs/superpowers/specs/2026-08-14-m2d3-general-room-skeleton-design.md)、實作計畫 [docs/superpowers/plans/2026-08-14-m2d3-general-room-skeleton.md](docs/superpowers/plans/2026-08-14-m2d3-general-room-skeleton.md)（6 任務，`subagent-driven-development` 在獨立 worktree 執行）。新增 `client/src/gameplay/mapUtils.js`（前端重算門狀態，鏡射 `turnFlow.js` 的 `getAvailableDirections`，含 `blocksOpenDoor` 修正效果檢查）、`RoomTile`/`PlayerBadge`（共用基礎元件，無美術圖房間顯示色塊＋名稱）、`FocusedRoomView`（目前房間＋鄰居淡化預覽＋門狀態按鈕）、`OverviewMap`（個人探索紀錄格狀地圖，樓層切換）、`CharacterPanel`（屬性刻度／道具清單／訊息記錄／行動按鈕），整合進 `DebugGameScreen.jsx` 取代原本純文字的通用按鈕（大門廳三格繼續走 `EntranceHallView`）。**Task 6 審查抓到 1 個 Critical，成因是計畫文件本身自相矛盾**（見下方除錯注意事項），修復期間 implementer session 因額度限制中斷，controller 確認檔案改動已落地後自行完成收尾。427/427 測試通過。
    - **M2D3 手動測試後的 5 項修正＋3 輪版面迭代（已完成並合併進 `main`）**：開發者手動測試後陸續回報並確認的功能/版面問題，詳見下方「關鍵設定」的完整清單。重點：大廳改為單人可進角色選擇（移除前後端＋前端按鈕共 3 處各自獨立的「至少 2 人」限制）；玩家圓形圖標新增 `player.enteredFromSide` 欄位依進入方向定位（支援同房間所有玩家，非僅自己）；道具清單改用新增的 `cardContent` 廣播（比照 `roomContent` 模式，含 items/events/omens 三個牌庫）顯示真實名稱；人物面板文字對比度根因是外層 `.lobby-viewport` 的近白色文字設定沒被除錯頁面覆蓋；版面歷經 3 輪迭代（左右固定寬中間吃剩下→中間固定寬左右平分剩下→標題移入左欄＋70%/15%/15% 精確比例），最終版是純 CSS 響應式（`--total-square`/`--tile-size`/`--peek-size` 三個 CSS 自訂屬性，隨視窗高度自動縮放，不需要 JS resize listener）。**目前地圖區／人物面板的暫時除錯框線（紅／綠／藍虛線）尚未移除，等開發者確認版面無誤後要拿掉**。427/427 測試通過。
- **角色資料**：[data/characters/characters.json](data/characters/characters.json)，開發者已填完全部 6 個角色的真實內容（`codename`/`filename`/`gender`/`age`/`tall`/`occupation`/`itemID`/`itemname`/4 項能力刻度），M2D2 合併後 `MISSING_STAT_DEFINITION` 缺口已不存在。**`tall` 屬性**（M2D2 角色選擇圖片依身高比例縮放用）本次階段開發者微調 3 筆：警察 172→170、高中生 175→170、小女孩 151→150
- **角色起始道具機制**（本次階段新增，已合併）：`gameManager.js` 的 `startGame` 在 `addPlayer` 之後，若角色 `itemID` 非空，呼叫 `addItem` 自動把該角色宣告的起始道具放入背包（`itemID` 為 `null` 則不給）
- **已評估過、不採用的外部資源**：`Claude-Code-Game-Studios`——技術棧/規模都跟本專案不符
- **房間／角色資料稽核與內容補完（2026-08-16，已完成，`data/rooms/rooms.json`/`data/rooms/starting-rooms.json`/`data/characters/characters.json` 仍是開發者自己持有並填寫的內容資料，agent 依開發者指示直接編輯）**：稽核報告 [docs/2026-08-16-room-data-audit.md](docs/2026-08-16-room-data-audit.md)（JSON 格式檢查、邪祟降臨房間比對、美術圖缺漏清單）。`characters.json` 修正 `flieicon`→`fileicon` 手誤（6 筆，尚未被任何前端程式碼讀取，開發者說明用途是遊戲地圖上顯示的 icon 圖樣，M2D 之後的待辦）。`rooms.json` 從 32 筆擴充到 49 筆：11 筆先前佔位房間（餐廳／手術室／天花閣樓／五芒星室／墓園／地下墓穴／地底深淵／鍋爐室／地窖／酒窖／倉庫）已依 `_rule.pdf` 房間附錄官方原文補上具體機制（天花閣樓/五芒星室/墓園用既有 `leaveCheck`；地下墓穴/地底深淵「房間分兩側，通過需檢定」/鍋爐室/地窖「停留結束回合受傷」機制超出現有 schema，標記 `needsCustomLogic:true`＋官方原文，程式邏輯未寫；餐廳/手術室/酒窖/倉庫查證後確認官方就是純板塊無特殊機制）；新增髒亂的房間（`room_junk_room`，官方力量檢定3+機制，獨立於既有 `room_messy_room`）／風琴室／地下湖／包廂房／臥房／崩塌的房間共 6 筆；`room_ballroom` 樓層從 `"any"` 改為 `"ground"`（配合包廂房綁定機制需要明確樓層對應）。`starting-rooms.json` 新增地下平台（`room_basement_landing`，`floor:"basement"`，`filename:null` 待開發者補圖）。跨全部 50 個劇本（不限本專案 2 個選定劇本）查核 10 間房間的使用情形：老朽迴廊／塵封迴廊／客房 3 間確認全劇本 0 筆命中；其餘 7 間（地下湖/風琴室/崩塌的房間/包廂房/神秘電梯/煤導槽/臥房）都查到具體劇本用法，詳見下方「目前的瓶頸或停頓點」的地下室新機制段落。
- **手機直式版面改版（2026-08-17，已完成並合併進 `main`）**：見下方「關鍵設定」的完整技術記錄，這裡只列開發者可見的成果——橫式／直式版面並存（自動依螢幕方向切換，不需要手動選擇）；遊戲進行畫面精簡成「視野畫面」／「角色資訊＋行動區域」兩個區域，兩者都完全鎖定、玩家不能上下左右滑動；移動/開門方向按鈕浮在視野正方形四邊的鄰房預覽帶上；行動／總覽地圖／襲擊／回合結束 4 顆按鈕浮在視野正方形四個角落；原本獨立的樓梯按鈕拿掉，改成走到 LobbyC 或二樓平台時按「行動」就會上下樓（維持免費，不扣行動力）；狀態欄最終版面是上（姓名/行動力＋力量/速度/意志/知識由上到下各一排，字體與刻度格已放大）下（訊息欄，跟整個面板同寬）兩段，道具清單移到狀態欄右側 20% 寬的獨立區域，點道具名稱會跳出「使用／遺留／取消」選項（**「給予」尚未實作**，見下方「目前的瓶頸或停頓點」）。
- **三樓層架構＋座標式房間連接機制＋leaveCheck 失敗扣屬性引擎缺口（2026-08-17，已完成並合併進 `main`，開發者已確認）**：`boardGenerator.js` 的 `FLOORS` 新增 `'basement'`（現為 `['ground','upper','basement']`），`createBoard` 新增 `basement` Map 並放置 `room_basement_landing`（北/東/西三門，跟二樓平台同形狀），但**目前沒有走樓梯可以到地下室**——地下室平台本身只是佔位，實際能抵達地下室的唯一途徑是崩塌的房間掉落（見下）。`turnFlow.js` 的 `moveToRoom` 新增崩塌房間（`room_collapsed_room`）速度檢定 5+（`COLLAPSE_CHECK_STAT`/`COLLAPSE_CHECK_MIN`，沿用既有道具介入擲骰模式），失敗會在正下方**同座標**生成一張新地下室房間卡並把玩家移過去（`applyCollapseCheck`，掉落連結記在 `placedRoom.collapseLink`）；新增舞廳／包廂房雙向綁定（`placeBallroomGalleryPair`：生成其中一間時同步在對應樓層同座標生成另一間，位置衝突則重抽，牌堆僅存最後一張時改隨機位置不強制同座標）。`leaveCheck` 的 `failPenalty`（塔橋/雜亂的房間/藤蔓糾纏的溫室/天花閣樓/五芒星室/墓園/髒亂的房間共 7 間房，此前只會擋住移動不會真的扣屬性）現在會呼叫 `changeStat` 真的執行扣屬性。**因為本次對話中途發生過一次視窗壓縮，這部分工作的原始逐字對話紀錄沒有完整保留**，chatlog 只能記錄重點摘要，如需更多實作細節請直接讀程式碼（`turnFlow.js`/`boardGenerator.js`/`roomDeck.js`）或問開發者。
- **角色圖示串接與版面調整：大廳背景圖／選角立繪／遊戲內角色 ICON（2026-08-17，已完成並合併進 `main`，雙分頁模擬雙人流程實測通過）**：大廳初始頁面背景改為 `house_cloudy_day.webp`；選角頁面／遊戲內玩家圓形徽章的圖片來源整批換成開發者提供的新角色圖（`male_priest`/`female_Nurse`/`male_worker`/`female_police`/`male_athlrte`/`female_painter`，含對應 `_icon` 版本），從 `img/`（開發者個人暫存資料夾，不進版控）複製進 `client/public/images/`。**過程中發現並修補一個先前沒人注意到的伺服器缺口**：`gameManager.js` 的 `startGame` 雖然早就會依 `player.characterId` 查角色 `stats`/`itemID`，但這個 `characterId` 從未被寫回遊戲內玩家物件本身（`playerEntity.js` 的 `createPlayer` 原本沒有這個欄位），代表前端原理上不可能知道某個在場玩家對應哪個角色——已補上 `characterId` 從 `playerEntity`→`gameState`→`gameManager`→`socketHandlers` 的完整串接，`game:started` 新增 `characterContent` 廣播（比照既有 `roomContent`/`cardContent` 的一次性靜態資料模式）。`FocusedRoomView.jsx` 新增 `findCharacterIcon` 查表函式，`PlayerBadge.jsx` 有 `iconSrc` 時顯示角色圖示、否則維持原本顏色圓圈+字母的 fallback。
- **統一考驗彈窗機制＋訊息欄可讀化（2026-08-17，已完成並合併進 `main`）**：`leaveCheck`／崩塌房間速度考驗／卡片內建 `dice_check` 三個原本各自獨立的考驗機制，全部統一改發新的 broadcast 事件 `game:checkResolved`（另新增 `game:roomEntered` 給一般進房訊息），前端建立考驗佇列，依序播放「考驗前彈窗（描述＋擲骰按鈕）→ 動畫佔位 → 結果彈窗」，一次只處理一個考驗。訊息欄從 `JSON.stringify` 傾印改成人類可讀句子（含玩家署名），字體放大到 24px。技術細節見下方「關鍵設定」。**開發流程**：`writing-plans` 拆成 7 個任務的完整計畫（[docs/superpowers/plans/2026-08-17-check-modal-and-readable-messages.md](docs/superpowers/plans/2026-08-17-check-modal-and-readable-messages.md)）→ `subagent-driven-development` 在獨立 worktree 逐任務執行＋審查 → 整分支最終審查（Opus 模型，特別要求聚焦「沒有實機驗證過」的路徑）抓到 1 個 Critical＋4 個 Important（見下方除錯注意事項），修復並經 2 輪複審確認乾淨後合併。後端測試 466/466、client build 全數通過。6 項 Minor 建議已於 2026-08-18 第 1 次工作階段全數處理完畢。
- **道具合成機制（廚房烹飪配方，2026-08-18 第 2 次工作階段，已完成並合併進 `main`）**：走完整 `brainstorming` 流程確認範圍（武器/消耗品分類三個問題〔複合分類、限定使用次數、屬性門檻〕已確認全部併入 M3 戰鬥系統設計時處理，這次只做道具合成）。設計文件 [2026-08-18-item-combine-mechanic-design.md](docs/superpowers/specs/2026-08-18-item-combine-mechanic-design.md)：`item_016`（礦泉水）／`item_017`（泡麵）新增 `isMaterial:true`（前端道具選單不顯示「使用」）；`room_kitchen` 新增 `"actions":["烹飪"]`＋`craftRecipes`（列表型態，材料＋成果道具）；`socketHandlers.js` 的 `room_action` 新增判斷：材料不足直接回 `MISSING_CRAFT_MATERIALS`（不扣行動力）、材料足夠則動態組出 `choice` 效果（「要不要進行烹飪？」是/否），**完全重用既有的 `choice`／`grant_item`／`lose_item` 效果類型，沒有新增任何效果解析邏輯**。直接在 `main` 上 TDD 實作（範圍小，未開 worktree），4 個新測試，測試 473/473 通過，瀏覽器實測角色選擇/一般道具流程無回歸（材料道具/廚房實際觸發因隨機抽卡限制未能實機驗證，同一類已知限制見下方除錯注意事項）。
- **搜索機制（取代 item 類型房間自動抽卡，2026-08-18 第 2 次工作階段，已完成並合併進 `main`）**：走完整 `brainstorming`→`writing-plans`→`subagent-driven-development` 流程（5 任務＋整分支審查，直接在 `main` 上執行，開發者選擇不開 worktree）。設計文件 [2026-08-18-search-mechanic-design.md](docs/superpowers/specs/2026-08-18-search-mechanic-design.md)、實作計畫 [2026-08-18-search-mechanic.md](docs/superpowers/plans/2026-08-18-search-mechanic.md)。**過程中發現一個既有缺口**：`resolveCardDraw` 對 item/event 牌堆抽到的卡只會立即解析 `effects` 就結束，卡片本身從未真正進背包（只有 omen 牌堆才有 `addItem`）——這次順便修正，找到的道具現在會正確進背包。核心機制：10 間原本 `drawType:"item"` 的房間裡，9 間改為 `"actions":["搜索"]`＋`"item":"random_one"`（`room_vault` 因為已有非空 `effects` 開保險箱機制，只改 `drawType`，原本先决定不加新欄位，見下方 Minor 修正）；房間地圖實體新增 `item` 欄位（複製自靜態房間定義，避免污染共用資料，跨場遊戲互相干擾）；`player.searchedThisTurn` 每回合限一次；`game:selectAction room_action` 新增第三分支：沒有 `craftRecipes`、也沒有非空 `effects` 時預設走搜索，找到道具重用既有 `game:cardDrawn` 訊息樣板，沒找到廣播新事件 `game:searchEmpty`。5 個任務全數一次審查通過，後端測試 487/487。**整分支最終審查（Opus 模型）發現 1 個 Important（需開發者決定）＋1 個關聯 Important（既有 bug）**：食品儲藏室／健身房雖然依開發者指示也加了搜索欄位，但因為這兩間房已有非空 `effects`（結束回合被動加成），照分支優先序永遠讀不到，形同死資料；同時也暴露一個既有 bug——`room_action` 完全沒檢查 `onceOnlyPerPlayer`，理論上可以透過反覆按「行動」無限疊加結束回合加成。跟開發者確認後選擇「真的讓它們可以搜索」，修改分支邏輯（`effects` 全部是 `onceOnlyPerPlayer` 時視為沒有 effects 型行動、改走搜索），一次修復兩個問題，複審通過，486→487 測試。**7 項 Minor 建議中 4 項已於 2026-08-19 修復（詳見下方「除錯注意事項」），1 項列入待辦，2 項純提醒**。
- **道具「給予」選項（2026-08-19，已完成並合併進 `main`）**：`DebugGameScreen.jsx` 新增 `roommates`（同房間排除自己的玩家清單）傳給 `CharacterPanel.jsx`，道具選單新增「給予」按鈕（僅同房有其他玩家時顯示），點下去列出可選對象，呼叫既有的 `giveItemAction`（`mode:'give'`+`targetPlayerId`，伺服器端邏輯本來就有，這次補的是前端 UI）。雙人瀏覽器實測（Bob→Alice 治療藥膏）驗證通過。
- **房間多重行動機制（2026-08-19，已完成並合併進 `main`）**：走完整 `brainstorming`→`writing-plans`→`subagent-driven-development` 流程（4 任務＋整分支審查，這次選擇開獨立 worktree）。設計文件 [2026-08-19-multi-room-action-design.md](docs/superpowers/specs/2026-08-19-multi-room-action-design.md)、實作計畫 [2026-08-19-multi-room-action.md](docs/superpowers/plans/2026-08-19-multi-room-action.md)。核心變更：`rooms.json`/`starting-rooms.json` 的 `actions` 欄位從單純字串陣列改成結構化清單 `[{label, kind, ...}]`（`kind` 分 `search`/`craft`/`effects`/`teleport` 四種）；房間頂層 `effects` 語意收斂為**只給結束回合被動加成用**（`applyRoomEndTurnBonus` 讀取），room_action 觸發的效果全部改放進對應 `actions[].effects`；新增 `getRoomActions(roomDefinition, placedRoom)`（server 端 `socketHandlers.js`、client 端 `mapUtils.js` 各一份邏輯相同的版本），沒有 `actions` 欄位的房間預設 `[{label:'搜索',kind:'search'}]`；`game:selectAction actionType:'room_action'` 新增 `actionIndex` 選擇機制（payload 明確帶 index 時一律嚴格驗證，不再只看清單長度）；新增 `kind:"teleport"`，由 `turnFlow.js` 的 `performTeleport`/`resolveTeleportDestination` 實作（取代舊的 `jumpIntoCollapsedRoom`），涵蓋兩種情境：崩塌房間的「跳下」（現在消耗 1 行動力，不再免費——只有第一次不可抗拒的摔落維持不扣）、包廂房→舞廳的新「跳下」（消耗 1 行動力，不需考驗，掉落傷害留給 M3）。前端 `DebugGameScreen.jsx` 的「行動」按鈕，房間有 2 種以上目前有效行動時改開選單。13 間房間 `rooms.json` 資料遷移＋LobbyC/二樓平台 `starting-rooms.json` 遷移。4 個任務全數一次審查通過，整分支最終審查（Opus 模型）發現 2 個 Important（包廂房/舞廳跳躍的目的地座標完全沒驗證是否真的有對應房間；`ack` 在跳躍真正發生前就送出，失敗時玩家端會收到假成功但行動力已扣）皆已修復（跳躍前先驗證目的地存在，驗證通過才扣行動力，成功後才回應），複審通過，493/493 測試全綠。
- **開門/進新房間的行動力扣除規則調整（2026-08-20 第 2 次工作階段，已完成並合併進 `main`）**：走完整 `brainstorming`→`writing-plans`→`subagent-driven-development` 流程（3 任務＋整分支審查，開獨立 worktree）。設計文件 [2026-08-20-open-door-ap-cost-design.md](docs/superpowers/specs/2026-08-20-open-door-ap-cost-design.md)、實作計畫 [2026-08-20-open-door-ap-cost.md](docs/superpowers/plans/2026-08-20-open-door-ap-cost.md)。**規則變更**：開新房間的門原本會讓行動力直接歸零、回合立刻結束；現在改成固定扣 **2 點**（`OPEN_DOOR_AP_COST`，`turnFlow.js` 與其前端鏡像 `mapUtils.js` 各一份同值常數），且只有行動力 ≥2 時「開門」才會被列為可用選項（不夠 2 點時該方向視覺上跟沒門的牆一樣，不會出現在可互動清單裡）——這個資格檢查同時保證扣完之後行動力必定 ≥0，不需要額外的負數保護。開門後如有剩餘行動力，**不再強制結束回合**，可以直接繼續行動（例如同房間立刻搜索），跟其他行動一致。已確認並保持不變：移動到已放置鄰房仍是 1 點；樓梯等 `freeRoomAction`、`leaveCheck`（塔橋等離開檢定）、崩塌房間檢定、舞廳/包廂配對，都是完全獨立的程式路徑，不受影響。3 個任務全數一次審查通過，整分支最終審查（Opus 模型）確認零 Critical/Important 程式碼缺陷、`OPEN_DOOR_AP_COST` 前後端零漂移、「不需負數保護」的前提逐條路徑驗證成立（含骰子插入續行路徑），496/496 測試全綠。收工前另外實際玩了一局兩人遊戲手動驗證：開門後行動力 4→2（不是歸零），行動力剩 2 點時「解鎖」按鈕仍正常顯示，扣到 0 點後所有「解鎖」選項正確消失，過程無錯誤無當機。**過程中一個工具使用教訓**：`Agent` 工具的 `isolation:"worktree"` 參數會建立一個全新、獨立於 controller 自己 worktree 的分支，如果在 subagent-driven-development 流程裡對同一個任務鏈使用，implementer 的 commit 會落在錯誤的分支上——後續任務改成不傳這個參數，讓 implementer 直接在 controller 的 worktree 裡工作，並把已經產生的 commit `cherry-pick` 回正確分支修正。另一個教訓：`TaskStop` 回報「成功停止」的背景 `npm start`/`npm run dev`/`npx jest` 行程，在 Windows 上並沒有真的把底層 `node.exe` 子行程一併殺掉，導致 worktree 目錄一度因為檔案佔用刪不掉——之後改用 PowerShell 的 `Get-CimInstance Win32_Process` 依命令列比對出確切殘留 PID 再 `Stop-Process -Force` 才清乾淨，收工前務必實際確認 `node.exe` 行程清單而不是只信任 `TaskStop` 的回報。
- **房間圖片旋轉機制（2026-08-20 第 3 次工作階段，已完成並合併進 `main`）**：走完整 `brainstorming`→`writing-plans`→`subagent-driven-development` 流程（3 任務＋整分支審查，開獨立 worktree）。設計文件 [2026-08-20-room-image-rotation-design.md](docs/superpowers/specs/2026-08-20-room-image-rotation-design.md)、實作計畫 [2026-08-20-room-image-rotation.md](docs/superpowers/plans/2026-08-20-room-image-rotation.md)。**機制**：房間圖片是設計成「單張圖旋轉對應任意真實門位置」的俯視圖，但旋轉渲染邏輯從未實作。`doorLayout.js` 新增 `computeRotation(canonicalDoors, doorSides)`，比對房間圖片畫死的門位置（`rooms.json` 的 `canonicalDoors` 欄位，開發者逐間房間手動核對填寫）跟這局遊戲真實生成的門位置，算出需要旋轉幾度（0/90/180/270）；`boardGenerator.js` 的 `placeNewRoom`/`placeRoomAt` 在房間**第一次被放置到地圖上時**算好存進房間實例（`rotation` 欄位跟 `doorSides` 並列，自動隨既有 `game:stateUpdate` 廣播傳到前端，沒改 `gameState.js`/`socketHandlers.js`），之後這個房間顯示的角度就固定下來。前端 `RoomTile.jsx` 套用 CSS `transform:rotate()`，跟既有的房間格局部縮放/拖動功能疊加時旋轉放最內層（`translate(pan) scale(zoom) rotate(rotation)`），確保拖動手感永遠是螢幕座標方向不受房間角度影響。**範圍排除**：鄰房預覽帶（`NeighborPeek`）明確不套用旋轉——開發者實測後認為移動/開門按鈕本來就蓋住門框位置，角度不對不會造成明顯視覺問題，維持現有 `background-image`+`backgroundPosition` 實作。**固定房間**（大門廳三格／二樓平台／地下平台）不套用這套機制。3 個任務全數一次審查通過，**整分支最終審查（Opus 模型）抓到 1 個 Critical**：新的 `computeRotation` 遇到 `canonicalDoors` 跟真實 `doorSides` 門數對不上就拋 `ROTATION_NOT_FOUND`，但這撞上 `doorLayout.js` 一個**這次改動之前就存在**的既有規則——`computeDoorLayout` 在所有候選門位置都跟已放置鄰房衝突時，會把房間強制退化成只剩入口一扇門，不管宣告幾扇門。審查用真實 `rooms.json` 模擬 500 局遊戲，**500 局全部會踩到**（中位數第 8 間房就會拋錯），而且拋錯時機點在舞廳/包廂配對、崩塌房間掉落這兩條路徑上，會發生在卡片已抽走、行動力已扣除之後，導致伺服器狀態跟前端斷開同步。**修復方向（已跟開發者確認）**：`computeRotation` 遇到門數對不上時，視為 `computeDoorLayout` 既有 fallback 造成的合法結果，回退角度 0（不拋錯）；只有門數相同但形狀對不上（真正的 `canonicalDoors` 資料填錯）才維持拋錯。已知限制（開發者已接受）：房間退化成單門時，美術圖仍會畫出原本門數，但只有入口方向按鈕可互動，玩家主要靠方向按鈕辨認可走方向，不特別處理。修復過程中順帶發現並修正了 2 個既有測試（Task 1/2 各自寫的 `ROTATION_NOT_FOUND` 範例剛好都是門數不合的案例，改用門數相同但形狀不合的範例），並新增一個用真實 `rooms.json` 全 52 間房×4 個入口方向跑過 `computeRotation` 的迴歸測試鎖定這個修復。複審通過，509/509 測試全綠。收工前實機瀏覽器驗證：開門進入新房間後，用 JS 讀取圖片的 CSS 計算後 transform，精確比對數學矩陣（例如 `matrix(0,1,-1,0,0,0)` 精確等於 `rotate(90deg)`），確認縮放（`matrix(0,1.5,-1.5,0,0,0)` 精確等於「先轉 90° 再放大 1.5 倍」）也正確疊加，無 console 錯誤。**討論過但決定不做的方案**：開發者提出「先判斷合理房型再抽卡」（讓抽卡結果從一開始就符合鄰房限制，減少甚至消除退化情況），確認過就算做了這個機制，它自己保留的最後一道「牌庫抽不出合適房型、降級卡片門數」的 fallback，仍然會重現一模一樣的門數對不上問題——所以這次的小修正（`computeRotation` 回退 0）是任何情況下都必要的安全網，跟這個大改動與否無關，因此列成獨立待辦，見下方「下一步行動」。
- **先判斷合理房型再抽房間卡（2026-08-20 第 4 次工作階段，已完成並合併進 `main`）**：走完整 `brainstorming`→`writing-plans`→`subagent-driven-development` 流程（3 任務＋整分支審查，開獨立 worktree）。設計文件 [2026-08-20-feasible-room-draw-design.md](docs/superpowers/specs/2026-08-20-feasible-room-draw-design.md)、實作計畫 [2026-08-20-feasible-room-draw.md](docs/superpowers/plans/2026-08-20-feasible-room-draw.md)。**機制**：開門抽房間卡時，原本是先隨機抽卡、再嘗試湊出跟已放置鄰房不衝突的門位置，湊不出來就退化成單門房（`computeDoorLayout` 既有規則，是上一個「房間圖片旋轉機制」全分支審查抓到的 Critical 問題根源）。現在改成：`boardGenerator.js` 新增 `isDoorLayoutFeasible`，**直接重用 `computeDoorLayout` 本身當可行性檢查器**（不另外手刻一套判斷邏輯，永遠跟實際放置結果一致）；`roomDeck.js` 新增 `drawFeasibleRoom`，比照既有 `drawRoom` 的「依片庫順序、對不到就搬到牌堆尾端」寫法，多加一個可行性判斷條件，整副牌都找不到就退回原本的 `drawRoom` 行為；`turnFlow.js` 的 `moveToRoom` 把兩處既有抽卡呼叫都換成新函式。**範圍**：只改主要開門路徑，崩塌房間掉落地下室的生成維持原樣不動（開發者確認：地下室通常是未探索新地區，鄰房衝突機率本來就低）。3 個任務全數一次審查通過，**整分支最終審查（Opus 模型）抓到 1 個 Critical**：舞廳/包廂配對的既有重抽迴圈，原本靠 `drawRoom`「一定會抽到別張牌」的隱含保證維持終止性；換成 `drawFeasibleRoom` 後，如果目標位置周圍只有舞廳/包廂本身可行、牌堆裡其他同樓層牌全部不可行，會不斷重抽到同一張被拒絕的牌，**造成無限迴圈、同步卡死整個 Node event loop，等於伺服器掛死**。審查實際寫了 repro 腳本驗證（watchdog 計數器跑滿 5000 次沒有終止），並確認用真實 `room_ballroom`（doors:4）資料跟中後期棋盤填滿的盤面是可以觸發的，不是理論邊角案例。**修復方向**：在重抽迴圈外宣告一個 `rejectedIds` 集合，每次拒絕重抽時記錄被拒絕的房間 id，重抽時額外排除已拒絕過的 id——恢復迴圈原本「重抽一定會前進」的保證（終止性論證：排除掉所有已拒絕房間後，`drawFeasibleRoom` 會退回 `drawRoom`，而失敗的可行性搜尋一輪保證牌堆順序完整還原，剛被拒絕、推到牌堆尾端的那張牌仍在尾端，`hasRoomForFloor` 已保證還有別的同樓層牌排在它前面，`drawRoom` 必定抽到別張）。修復同時補上一個帶 5 秒 timeout 保護的迴圈整合測試（重現原本會掛死的盤面，斷言 `moveToRoom` 真的會結束）、以及一個既有測試檔案缺少 `afterEach(jest.restoreAllMocks())` 的 Minor 修正。複審通過，516/516 測試全綠。**已知限制**：這個機制無法完全消除退化成單門房的情況（牌庫剩下的卡全部門型都跟目前位置衝突時，仍會退回原本行為），已由上一個房間旋轉功能的 `computeRotation` 安全網處理過，兩者不互斥。
- **物品攜帶數量上限機制（2026-08-21 第 1 次工作階段，已完成並合併進 `main`）**：詳細內容見上方最新更新摘要。核心檔案：`server/src/game/playerEntity.js`（`addItem` 成為所有道具進背包的唯一路徑）、`server/src/game/effectResolverManager.js`（新增 `pendingInventoryChoice`/`inventoryChoiceTimeoutHandle` 欄位）、`server/src/socketHandlers.js`（`openInventoryChoiceIfNeeded`/`applyInventoryLeave`/`handleInventoryChoiceTimeout`/`pickInventoryChoiceDefault`，新 socket 事件 `game:inventoryChoicePending`/`game:inventoryChoiceRespond`）、`client/src/DebugGameScreen.jsx`（新彈窗，`onInventoryChoicePending` 已過濾只有目標玩家會顯示）、`client/src/gameplay/mapUtils.js`（新增 `findCardName`，過程中發現設計文件誤植它已存在於 `DebugGameScreen.jsx`，實際只存在於 `CharacterPanel.jsx` 私有版本，兩邊邏輯重複但明確判定不在本次範圍內整併）。524/524 測試全綠。
- **道具手持/配戴機制（2026-08-23 第 2 次工作階段，已完成並合併進 `main`）**：詳細內容見上方最新更新摘要。核心檔案：`server/src/game/playerEntity.js`（新增 `wieldedWeaponId`/`wornGearIds` 欄位；`clearEquipStateIfNeeded` 現在被移進 `removeItem()` 本體，讓所有經過 `removeItem` 的道具移除路徑自動清除裝備狀態，`turnFlow.js` 的 `giveItemAction`/`leaveItemAction` 因為用 `splice` 不經過 `removeItem`，仍保留各自明確呼叫）、`server/src/game/turnFlow.js`（`wieldItemAction`/`unwieldItemAction`/`wearItemAction`/`unwearItemAction`，皆不匯出、只透過 `selectAction` 的 `mode` 分派）、`server/src/socketHandlers.js`（`mode==='wield'`/`'wear'` 時從 `content.cards.items` 解析 `itemCategory`，前端傳來的同名欄位完全不採信）、`server/src/game/diceInterjection.js`（`category:"gear"` 的道具現在要 `wornGearIds` 包含才會被 `findInterjectionOptions` 列入）、`server/src/game/gameManager.js`（`startGame` 依角色初始道具的 category 自動裝備）、`client/src/gameplay/CharacterPanel.jsx`（道具選單依 `selectedItem.isOmen`／`selectedItem.category` 動態決定按鈕，**預兆卡永遠優先走原本的「使用」邏輯，完全不受五分類影響**——這是全分支審查抓到的 Critical 修復後的最終行為，務必維持這個判斷順序，之後如果再改這個選單，先確認 omen 分支還在最前面）。554/554 測試全綠。
- **遊戲訊息彈窗流程與機制（2026-08-25 第 2 次工作階段，已完成並合併進 `main`）**：詳細內容見上方最新更新摘要。核心檔案：`server/src/game/playerEntity.js`（`movePlayerTo` 回傳是否首次進入）、`server/src/game/turnFlow.js`／`server/src/game/effectResolver.js`（串 4 條移動路徑到 `enteredNewRoom`）、`server/src/socketHandlers.js`（`game:roomEntered` 新欄位、新增 `game:itemUseResolved` 廣播給使用者與承受者雙方）、`client/src/gameplay/CheckModal.jsx`（播放 `ROLLDICE.mp4`，`muted`/`playsInline`/`onError`；考驗結果依 `check.sourceKind` 分流，卡片來源顯示 `feedbacktextDice` 比對文字，房間來源維持原本通用文字）、`client/src/gameplay/SimplePopup.jsx`（新檔案，共用彈窗元件）、`client/src/DebugGameScreen.jsx`（`pendingCheckQueue` 擴充 `roomIntro`/`eventIntro`/`eventNoCheck`/`itemDrawNoCheck`/`itemUseResolved` 五種 `kind`，`onCardDrawn` 排除 `activatedOnUse` 卡片跳過早的 `eventNoCheck`）、`client/src/gameplay/CharacterPanel.jsx`（道具選單補上 description 顯示）。**架構重點**：伺服器只送最小 id/布林訊號，所有顯示文字（含 `feedbacktextDice` 的骰數區間比對）都在前端用已有的 `cardContent`/`roomContent` 解析，避免同一套比對邏輯在前後端各寫一份。560/560 測試全綠，手動瀏覽器驗證 3 種新彈窗（`roomIntro`/`itemDrawNoCheck`/`itemUseResolved`）實際渲染正確；`eventIntro`/`eventNoCheck` 與影片/`feedbacktextDice` 路徑因 RNG 未手動點到，依賴自動化測試佐證。

## 除錯注意事項 (Debug Notes — 審查發現的問題與後續慣例)

**這一節記錄審查發現過的架構性錯誤，以及由此確立的通用慣例。任何開工前都應該先讀這一節，避免同類錯誤重複發生。**

**通用介面約定（往後所有里程碑都要遵守，不是 M2c-2 專屬）**：
任何會讓某個動作的效果解析「暫停等待玩家選擇」的流程（卡片 `choice` 效果、預兆/事件卡效果、道具/房間操作效果、未來 M3 戰鬥的傷害分配選擇等），在該選擇被實際解決（玩家回應，或逾時採用預設值）之前，**不可以呼叫任何會推進遊戲狀態的動作**（例如換下一位玩家的回合）。已確立的實作模式（見 `server/src/socketHandlers.js` 的 `hasPendingEffectChoice`/`EFFECT_CHOICE_IN_PROGRESS`）：
1. 任何會推進狀態的新動作，執行前先檢查該房間是否有未解決的選擇，有的話直接拒絕（回傳 `XXX_IN_PROGRESS` 類錯誤），不要讓新動作跟未解決的選擇擦身而過
2. 觸發選擇的原始動作本身**先不要**呼叫推進狀態的函式；改成把「推進狀態」延後到選擇真正解決（玩家回應、或逾時採用預設值）的那個 callback 裡才呼叫
3. 解析效果的呼叫要包一層 try/catch，避免任何一次效果解析拋錯就連帶讓「推進狀態」跟「廣播最新狀態」這兩件事被跳過

**M2c-2 最終獨立審查發現的實際案例（供未來同類流程參考）**：
- **Critical（已修復，`986fb64`）**：`game:move` 抽卡後，效果卡在 `choice` 提問未解決時，仍然無條件呼叫 `advanceTurnIfOver` 把回合交給下一位玩家。下一位玩家若立刻又抽到一張需要選擇的卡，會撞上 `promptState` 的「同時只能有一個待處理提問」限制而拋錯，例外進而讓收尾動作（推進回合、廣播狀態）整個被跳過——房間永久卡死，無法自我恢復。已用上述通用模式修復，並補了 item／event／omen 三種牌庫各自的實測回歸測試（`8d91e40`），不是只驗證 item 牌庫就假設其他兩種也對。
- **Important（已修復）**：未知的 `drawType`（房間資料打字錯誤等）原本會讓效果解析拋出未分類的原始 `TypeError`，而非專案慣例的 `UPPER_SNAKE_CASE Error`，且同樣會觸發上面的死鎖路徑——現在拋 `UNKNOWN_DECK_TYPE`，並且被 try/catch 保護，不會讓房間卡死。
- **M2c-4 已依此模式實作**：「道具」「操作」這兩個 `selectAction` 動作接上真實邏輯，需要玩家選擇的效果一律沿用 `pendingChoice`/`EFFECT_CHOICE_IN_PROGRESS` 機制，沒有另外設計一套
- **確認的規則（M2c-4 Task 5 執行期間跟開發者確認）**：進入新房間（開門）會讓行動力直接歸零，回合立刻結束——同一回合內不可能再有行動力觸發「操作」（`room_action`），必須等到該玩家下一次輪到行動才能在該房間操作／考驗。但**進房間自動觸發的卡片效果（抽卡）不受此限**，仍然必須解析完（含玩家選擇）才能結束回合，這是既有慣例，兩者是不同機制，不要混淆。**⚠️ 2026-08-20 第 2 次工作階段更新：「開門讓行動力直接歸零」這條規則已改掉**，改成固定扣 2 點（不歸零，不強制結束回合），詳見下方「已完成進度」的開門行動力扣除規則調整條目；本條僅保留作為歷史記錄，不再是目前的真實規則
- **M3 戰鬥階段的傷害分配**（依 [card-mechanics-reference.md](docs/superpowers/specs/2026-08-01-card-mechanics-reference.md)：肉體傷害在 might/speed 間自由分配、精神傷害在 knowledge/sanity 間自由分配，逾時預設平均分配）是同一種「需要玩家做選擇才能繼續」的模式，M3 設計戰鬥系統時要直接沿用這套機制，不要重新發明
- **Important（記錄為 M2c-3 才會浮現的缺口，不是實作偏差）**：`modifiers.js` 的 `checkRemoveConditions`（buff/debuff 移除判斷）目前在 `src/` 裡完全沒有呼叫點，只有測試用到。等 M2c-3 真的填入 `persistent_modifier` 卡片內容，任何持續性標記都會變成永久 buff，除非在那之前先接上呼叫點

**M2c-4/M2c-5 獨立審查發現的實際案例（已修復，`7eca839`，供未來同類流程參考）**：
- **Important**：`handleEffectResolveResult` 裡 `consumeItemIfApplied` 觸發的 `removeItem` 呼叫原本沒有包 try/catch。如果某個消耗品道具的 `effects` 本身也包含一個指向自己的 `lose_item`（例如魔術方塊卡面文字「魔術方塊消失」，若照字面直接加一個 `lose_item` 效果），該道具會被移除兩次，第二次 `removeItem` 拋 `ITEM_NOT_FOUND`。這個拋錯在 `game:selectAction` 的同步路徑剛好有外層 try/catch擋住，但在 `game:effectPromptRespond`／逾時這兩條非同步路徑沒有，會導致「推進回合」跟「廣播 `game:stateUpdate`」被跳過——**跟 M2c-2 的 C1 是同一類問題，透過一個新增的呼叫點（`removeItem`）重新出現**。已修復：`removeItem` 包一層 try/catch，「已經不存在」視為良性 no-op。**寫 M2c-3 內容時要注意**：`category:"consumable"` 的道具，`effects` 不應該再額外寫一個指向自己的 `lose_item`（移除交給 `consumeItemIfApplied` 自動處理，不用、也不該在 effects 裡重複寫）

**`summon-control-and-item-drop` 全分支最終審查發現的實際案例（已修復，`9f79511`/`e00660b`/`36e5aea`/`cb0b953`，供未來同類流程參考）——這批問題是 7 個任務個別審查時各自看不到的跨任務組合問題，只有把整個分支放在一起看才浮現**：
- **Critical（架構性問題，已修復）：卡片「抽到即觸發」跟「持有後主動使用才觸發」共用同一組 `effects` 欄位，沒有區分**。`resolveCardDraw`（`socketHandlers.js`）不管卡片類型，抽到就無條件解析 `card.effects`；但 `game:selectAction` 的 `item` 使用路徑（`content.cards.omens.find(...).effects`）解析的是**同一組**欄位。水晶球（`preview_and_choose`）／面具（`toggle_active`）卡面文字明明是「當玩家使用...時」，卻在抽到當下就先跑過一次——原本影響較小（多跳一次選擇視窗、面具被動預先啟動），但犬靈的 `switch_control` 一旦在抽卡瞬間自動觸發，會立刻凍結玩家操作，且**手動結束回合機制上線後（Task 5）＋除錯頁面原本沒有消散/結束回合按鈕，會讓整場遊戲永久卡死無法復原**。**修復方向（已跟開發者確認）**：卡片內容新增 `activatedOnUse: true` 旗標（`omen_003`/`omen_004`/`omen_008` 三張都有標），`resolveCardDraw` 看到這個旗標就跳過 `resolveEffects`（卡片仍會加入背包、仍計入邪祟考驗，只是不解析效果），效果只在玩家之後主動「使用道具」時才解析。**這是一個通用機制，不是犬靈專屬——之後任何「持有後才主動使用」的卡片都要記得加這個旗標**，否則會重現同一類問題（只是嚴重程度視效果內容而定）
- **Important（已修復）**：操控召喚物期間 `game:useStairs` 沒有跟 `game:move`/`game:selectAction` 一樣被 `player.summons` 分流擋下，玩家可以用樓梯免費傳送本體，違反「本體完全凍結」的設計。已在 `useStairs` 加上 `SUMMON_ACTIVE` 拒絕
- **Important（已修復）**：設計文件明確要求「一回合只能切換一次來回」，但 `switch_control` 完全沒有限制，玩家可以無限次消散再召喚換取免費移動。已新增 `player.summonUsedThisTurn` 旗標（`switch_control` 檢查並設定，`advanceTurn` 於離開玩家身上重置），`handleSwitchControl` 同時補上輸入驗證（`INVALID_SWITCH_CONTROL_EFFECT`）與重複啟用防呆（`SUMMON_ALREADY_ACTIVE`）
- **Important（已修復，含審查後追加修正）**：`leaveItemAction` 遺留道具到房間時，原本會重建一個只有 `{id}` 的新物件，丟失道具本身的狀態（例如已啟動的面具的 `active` 標記）。第一輪修復只補了「遺留」這半邊，重新審查時發現「撿取」（`pickupItemAction`）同樣有這個問題，兩邊都補齊物件狀態保留（比照 `giveItemAction` 既有的正確作法），並補了一個遺留＋撿取往返不遺失狀態的測試
- **Important（已修復）**：`DebugGameScreen.jsx` 完全沒有「結束回合」按鈕——Task 5 把自動結束回合機制拿掉後，除錯頁面連過第二回合都做不到。補了一個最小的 `結束回合` 按鈕（跟既有 `handleUseStairs` 同樣寫法），完整的行動選單／召喚物操控 UI 仍然是 M2d 的範圍
- **Minor（已記錄，暫不處理）**：`SUMMON_ALREADY_USED_THIS_TURN` 這類新拋錯目前會被 `game:selectAction` 既有的 catch-and-log 模式吞掉（玩家端看不到錯誤訊息，但行動力已經被扣），這是既有的吞錯模式，不是這次新增的缺陷，但這次新的拋錯讓它第一次在正常遊玩中可能被踩到，前端做出對應 UI 提示前先记录

**`dice-interjection-part-a` 全分支最終審查發現的實際案例（已修復，供未來同類流程參考）**：
- **Important（已修復）：consume-before-validate 模式——道具先被消耗，驗證失敗才拋錯，導致玩家永久失去道具且擲骰結果憑空消失**。天使羽毛的 `overrideValue`（玩家自選 0~8 覆蓋擲骰結果）原本只在 `computeInterjectedRoll` 深處驗證，但**驗證之前**就已經呼叫 `respondToPrompt`（消耗提示）跟移除道具——玩家送出缺漏或超出範圍的 `overrideValue`，道具就永久消失，擲骰也不會有結果。修復：在 `game:diceChoiceRespond` 的**最前面**（`respondToPrompt` 之前，提示狀態都還完整保留時）先驗證 `overrideValue`，驗證失敗直接回錯誤讓玩家可以重新送出正確的值。**通用教訓：任何「先扣資源、後驗證輸入」的順序都要反過來——驗證永遠要在消耗/移除任何東西之前**
- **Important（已修復）：新的暫停狀態（`pendingRollChoice`）沒有清掉舊的暫停狀態（`pendingChoice`），兩者同時存在會讓房間永久卡死**。`handleRollChoicePending` 建立 `pendingRollChoice` 時沒有把既有的 `pendingChoice` 清空——如果一張卡片的 `choice` 效果選項本身又包含一個會跳出道具介入的 `dice_check`（目前沒有卡片這樣寫，但架構上可行），兩個暫停狀態會同時存在，導致回應其中一個會讓另一個指向一個已經不存在的提示，永久卡死房間（`game:move`/`game:selectAction`/`game:endTurn`/`game:useStairs` 全部被 `ROLL_CHOICE_IN_PROGRESS` 擋死，逾時計時器的防呆分支也沒有清掉卡死的狀態）。修復：建立新暫停狀態時明確清空舊的，逾時的提早return分支也要防呆補清。**通用教訓：任何新增的「暫停等待玩家回應」狀態，都要檢查會不會跟既有的暫停狀態共存衝突，不能只顧自己這一個狀態**
- **確立的規則**：這次是本專案第二次新增獨立的「暫停等待玩家回應」狀態（第一次是 M2c-2 的 `pendingChoice`），兩次都不是靠「重用既有欄位」而是「開一個新的、平行的欄位＋各自獨立的逾時計時器 map＋各自獨立的 socket 回應事件」——這是刻意的架構選擇（兩種暫停的「恢復邏輯」本質不同），但也代表每次新增暫停狀態都要重新檢查「這個新狀態會不會跟其他既有的暫停狀態衝突」，不會自動免疫

**`dice-interjection-part-b` 全分支最終審查發現的實際案例（已修復，`67573ef`，供未來同類流程參考）**：
- **Important（已修復，需開發者確認遊戲規則方向）：新路徑重用既有邏輯時，沒人回頭檢查舊路徑要不要跟著補齊，形成兩條路徑的新不一致**。`leaveCheck` 的介入/逾時擲骰路徑重用了 `computeInterjectedRoll`（含 `onBeforeRoll`/`onAfterRoll` modifier 套用），但 `moveToRoom` 原本的「直接擲骰」路徑（沒有道具可介入時）完全沒有套用 modifier——結果變成「光是身上帶著一張可介入的道具（即使選擇不用）就會改變這次考驗吃不吃 modifier」。現行出貨內容還踩不到（leaveCheck 的三間房都是抽預兆牌，不是抽事件牌，摸不到唯一的房間 modifier 卡），屬潛伏性缺陷，不是立即可見的 bug，但方向已經是設計文件本身寫好的期望（「持續性修正全部一致生效」）。跟開發者確認後修復：讓直接路徑也套用同一組 modifier（5 行，`turnFlow.js`）。**通用教訓：任何「新路徑重用既有邏輯的一部分」的改動，都要主動比對『這段邏輯在其他既有路徑上是否也該套用』，而不是只確保新路徑本身正確——這類不一致只有在全分支審查把兩條路徑放在一起看時才會浮現，個別任務審查看不到**
- 過程中也發現這個機制帶有一個已知、未修的極低可達性缺口（Part A 就有，本分支只是多了第二條可達路徑）：`computeInterjectedRoll` 套用道具代價時，完全沒檢查 `resolveEffects` 的回傳值——如果某張卡的 `diceInterjection.cost` 未來被設計成本身也需要玩家選擇（巢狀 `choice`/`dice_check`），那個選擇會被靜默丟掉。目前所有卡片的代價都只是單純 `stat_change`，不會踩到，記錄供未來設計新卡片代價時留意

**M2D1 全分支最終審查發現的實際案例（已修復，`7048b6f`，供未來同類流程參考）**：
- **Socket.IO 房間廣播的時機陷阱（Task 3 執行期間發現並修復，`b64eab2`；已用獨立除錯腳本定位、審查時獨立查證 `socket.io-adapter` 原始碼確認）**：`io.to(roomCode).emit(...)` 的目標對象是**廣播當下**該房間的成員集合，不是呼叫端記住的快照。如果程式碼先讓所有 socket 依序 `.leave(roomCode)`，再呼叫 `io.to(roomCode).emit(...)`，這時候房間已經空了，訊息會安靜地送不到任何人（不拋錯、不警告）。**通用教訓：任何「先讓大家離開某個房間、同時要通知大家」的邏輯，廣播一定要放在讓任何人離開房間之前**，用 `fetchSockets()` 先拿到快照沒問題（那只是讀取，不影響房間成員），但實際的 `.leave()` 呼叫要排在 `emit` 之後
- **新暫停狀態要考慮「同一個事件會不會被多個地方收到」（Task 3 審查時發現，Task 5 開始前就修正計畫，未進入程式碼）**：房主自己主動呼叫 `lobby:leave` 時，因為上一點的廣播時機，房主自己的 socket 廣播當下還在房間裡，所以房主自己的客戶端**同時**會收到「自己呼叫的 `lobby:leave` 的 ack」跟「`lobby:closed` 廣播」——如果兩者都各自驅動一次畫面轉場，會互相搶跑。修法：讓其中一個訊號（這裡選 `lobby:closed` 廣播）當作**唯一**驅動轉場的來源，另一個（`lobby:leave` 的 ack）只負責回報成功/失敗，不做畫面轉場。**通用教訓：新增一個會廣播的動作時，要主動想清楚「呼叫者自己會不會也收到這個廣播」，不能預設呼叫者不會收到自己觸發的事件**
- **worktree 資料夾清不掉的真正原因（解開了上一階段 dice-interjection Part B 一直沒查出根因的同類問題）**：Task 6 端對端驗證時啟動的 Vite dev server 進程沒有完全關乾淨，佔用了 worktree 資料夾內檔案的控制代碼，導致 `git worktree remove`/`rm -rf`/PowerShell `Remove-Item` 都回報「被佔用」但查不到「明顯」的佔用程序。**通用教訓：下次再遇到 worktree 資料夾刪不掉，先用 `Get-CimInstance Win32_Process -Filter "name='node.exe'"` 找出 `CommandLine` 裡包含該 worktree 路徑的進程（不只是找一般的 node 進程，要比對路徑），關閉後通常就能刪除**

**M2D2 全分支最終審查發現的實際案例（已修復，供未來同類流程參考）**：
- **Important（已修復）：3 種狀態的訊息只做了 2 分支判斷**。角色選擇畫面的「確定選擇」按鈕禁用文字，原本只分「不是你的回合」跟「角色已被選走」兩種，但玩家點開**自己已經確認過的角色**時，`isLocked=true, isMyTurn=false`（跟被別人選走時一模一樣），會誤顯示「其他玩家選擇中，請稍後」，違反設計文件明確要求的「已選擇這個角色，等待其他玩家選擇中...」。修復：新增最高優先的 `isMine` 分支，用非錯誤樣式（`.cs-status-banner`）呈現，跟另外兩種錯誤訊息（`.lobby-error`）視覺區分。**通用教訓：「禁用原因」类的分支判斷，要窮舉所有實際可能組合，不能只看最先想到的兩種**
- **Important（已修復）：6 張角色圖合計約 37MB 即將永久進入 git 歷史**。跟 M2D1 的 `Gate.png` 8.1MB 是同一類問題（見上方 M2D1 段落），這次是 6 張疊加。修復：PowerShell `System.Drawing`（不需要新依賴）縮圖到約 600px 寬，合計降到約 4.5MB。**通用教訓：任何新增的圖片資源，提交前都要主動檢查檔案大小，這是第二次在全分支審查才抓到，之後應該在圖片資源一準備好的當下就先檢查，不要等到最終審查**
- **CSS 選擇器特異性陷阱（Task 2 執行期間發現並修復，兩輪除錯才定位到真正根因）**：「已被選擇」徽章圖片顯示異常放大（313px 而非預期的 117px）。第一輪假設是容器寬度不明確導致 `width:100%` 算不出正確值，改成 `width: fit-content` 後用獨立靜態 HTML/CSS repro（脫離遊戲流程，不受角色選擇 30 秒自動逾時干擾）配合 `getComputedStyle` 實測，**證明容器寬度確實已經解析成功（117.2px），但徽章依然是 313px**——第一輪假設被證偽。第二輪重新分析才找到真正原因：`.cs-portrait-button img` 這個描述符選擇器（specificity 0,1,1）同時命中了角色圖跟徽章圖兩個 `<img>` 元素，它的 `width:auto` 蓋過了徽章專屬規則 `.cs-locked-badge` 的 `width:100%`（specificity 0,1,0）——跟容器寬度是否明確完全無關，純粹是選擇器特異性衝突。修復：把角色圖的樣式規則改成專屬、不重疊的 `.cs-portrait-img` class，不再用描述符選擇器。全分支審查另外用圖片實際長寬比（1264:848 比例 × 209px 高度 = 311px ≈ 回報的 313px）獨立驗證了這個根因判斷正確。**通用教訓：CSS 版面異常時，「容器寬度不明確」跟「選擇器特異性衝突」是兩種完全不同的根因，症狀可能相似（子元素尺寸跑掉），但修法完全不同——遇到第一個假設修了沒效果時，要回頭檢查有沒有其他選擇器同時命中同一個元素，不要卡在同一個假設上反覆調整參數**
- **後續交接發現（Task 3 沿用，同一輪）**：確認選擇的禁用邏輯需要拆成 `isMyTurn`/`isLocked` 兩個獨立 prop 才能組出正確的 3 種訊息分支，原本合併成單一布林值會丟失資訊，這個拆分本身也是本次審查抓到的必要修正

**M2D3 後端資料串接 SDD 執行期間的操作失誤（已修復，供未來同類流程參考）**：
- **Agent 工具的 `isolation:"worktree"` 參數會另外建立一個全新的孤立 worktree，不是「在指定路徑裡跑」的意思**：Task 1 派工時，controller 除了在 prompt 裡指定 implementer 該在哪個既有 worktree（`EnterWorktree` 已建立好的 SDD 工作區）工作，還額外傳了 `isolation:"worktree"` 參數——結果 Agent 工具依這個參數另外建立了一個全新的、獨立的 worktree（`.claude/worktrees/agent-<id>`），implementer 的 prompt 文字指示被覆蓋掉，實際編輯落在這個孤兒 worktree 裡，且該 isolation 模式下無法執行 bash/git 指令（無法自行跑測試、無法 commit）。**通用教訓：`subagent-driven-development` 這類「controller 已經準備好一個共用 worktree，逐一派工進去做」的流程，dispatch implementer 時絕對不要加 `isolation:"worktree"`（也不要加任何其他 isolation 參數）——那個參數是給「需要獨立隔離副本」的任務用的，跟「在已存在的共用工作區裡做事」是相反的兩種需求，兩者不能混用**。修復方式：確認孤兒 worktree 裡的 diff 內容跟任務 brief 完全一致後（`git diff` 逐行比對），用 `git apply` 把 patch 搬到正確的 worktree、重新驗證測試、正常 commit，再刪除孤兒 worktree；後續兩個任務移除該參數後沒有再發生。

**M2D3 一般房間骨架 SDD 執行期間，計畫文件本身自相矛盾導致的 Critical（已修復，供未來同類流程參考）**：Task 6 審查抓到 `DebugGameScreen.jsx` 整合步驟裡，Step 2 的程式碼把 `onPromptResolved` 改成只 push 進新的 `messages` 陣列，但 Step 3 的文字說明卻承諾 `character_select` 階段的「上一個提問結果」顯示（讀取 `lastPromptResolved` state）「不受影響，維持不動」——兩者互斥：因為 `setLastPromptResolved` 已經沒有任何呼叫點，那個顯示永遠會是空的。這不是 implementer 誤判（它確實照著 Step 2 的程式碼字面實作），而是**controller 自己撰寫計畫時的疏漏**：Step 2 修改了共用的事件處理函式，但沒有回頭檢查 Step 3 對同一個函式的另一個承諾是否還成立。修復：讓 `onPromptResolved` 同時做兩件事（push 進 `messages`＋繼續呼叫 `setLastPromptResolved`）。**通用教訓：計畫自我審查階段，如果同一個函式/狀態在多個 Step 分別被提及，要交叉核對每個 Step 的文字承諾在其他 Step 的程式碼改完之後是否依然成立，不能逐步孤立審查——這類矛盾只有在把整份計畫放在一起看時才會浮現，SDD 的個別任務審查看不到（因為 Task 6 的審查者只看得到 Task 6 自己的 diff，看不到「這個計畫的前後文字互相矛盾」這件事本身）**。

**版面反覆迭代期間踩到的 2 個 CSS/flexbox 細節坑（已修復，供未來同類版面調整參考）**：
- **忘記在「按鈕搬出某個欄位」之後，同步調整該欄位原本因為按鈕存在而預留的空間**：地圖區的移動按鈕原本在正方形視野區下方，`--total-square` 因此預留了 200px 給按鈕列；下一輪把按鈕搬到左欄後，這個 200px 的預留高度沒有跟著拿掉，直到再下一輪開發者回報「還是太小」才發現這個殘留數值。**通用教訓：任何「把某段 UI 從 A 搬到 B」的改動，要主動檢查 A 原本因為這段 UI 而存在的版面預留（間距/高度/margin）是否也該一併移除，不能只確認 B 那邊正確**
- **`padding` 直接加在 `flex-basis:0%` 的 flex 項目上，即使設了 `min-width:0`，在部分瀏覽器下仍會頂住縮放下限，導致兩個理論上該平分空間的 `flex:1` 欄位實際寬度不相等**（左欄 362.5px vs 右欄 346.5px，差距剛好等於 padding 值的 2 倍）。這不是 CSS 規格認定的行為（`flex-basis:0%` 理論上應該完全忽略內容尺寸），但實測就是會發生。修法：把 `padding` 搬到欄位內部一層額外的 wrapper `<div>`，欄位本身保持 `padding:0`。**通用教訓：兩個 `flex:1` 欄位分寬度不相等時，先檢查是否直接在 flex 項目本身加了 padding，而不是先懷疑 flex-grow/flex-shrink 設定錯誤**

**`lobby:players` 廣播競態條件（已修復並合併，`d4d059e`，供未來同類流程參考）**：跟子元件自己在 `useEffect` 裡訂閱廣播事件、伺服器又緊接著 ack 之後同步送出該廣播，兩者中間隔著一次 React 掛載週期，廣播可能在子元件真正開始監聽之前就送達並被無聲丟棄。這是本專案第二次踩到同一類問題（第一次是 M2D2 的 `game:started`，修法也一樣：把訂閱搬到協調層 `LobbyScreen.jsx` 常駐的 `useEffect`，從 socket 建立那一刻就開始監聽，再用 props 往下餵，不讓子元件自己掛載後才訂閱）。這次之所以直到現在才被發現，是因為過去所有測試都是雙人流程，第二位玩家加入會觸發補救用的第二次廣播蓋掉被漏接的第一次，直到開發者用上次階段新加的單人建房功能單獨測試才第一次暴露。**通用教訓：任何「伺服器在 ack 後緊接著同步廣播」的模式，前端訂閱該廣播的地方一定要在「觸發這個 ack 的動作發生之前」就已經在監聽，不能依賴子元件掛載後才訂閱這個時序——尤其如果這個廣播沒有「後續會被同類事件覆蓋掉」的保護機制（例如只會廣播一次，或後續事件很久才會發生），競態條件會直接造成永久性的資料遺漏，不會自己恢復**

**手機直式版面改版期間發現的 CSS/流程陷阱（2026-08-17，供未來同類版面工作參考）**：
- **CSS Grid 項目預設 `min-height`/`min-width:auto`，會讓子內容撐開容器、繞過 `overflow:hidden`**：把視野／面板兩區域鎖死不能捲動時，加了 `position:fixed; inset:0; overflow:hidden` 在外層容器，理論上應該完全鎖死，但子元素內容一旦比配置的 grid 軌道高，容器還是會被撐高，`overflow:hidden` 形同虛設，頁面依然可以捲動。根因是 CSS Grid 項目的 `min-height`/`min-width` 預設值是 `auto`（不是 `0`），這個預設值讓內容可以無視軌道尺寸把格子撐大——跟先前 Handover 記錄過的 flexbox `min-width:0` 陷阱是同一類問題，只是換了個排版模型。修法一樣：在會裝真實內容的 grid 項目上明確補 `min-width:0; min-height:0`。**通用教訓：flex 跟 grid 的子項目都有「預設不會縮小到比內容還小」的隱藏規則，任何要鎖死容器尺寸／搭配 `overflow:hidden` 的版面，都要主動幫子項目補 `min-width:0`/`min-height:0`，不能假設設定 `overflow:hidden` 在容器上就一定有效**
- **瀏覽器預設 `body` margin 會在完全鎖定捲動的版面裡留下殘留可捲動空間**：即使遊戲畫面容器本身用 `position:fixed` 精準貼齊視窗，瀏覽器預設的 `body { margin: 8px }` 依然會讓 `document.documentElement.scrollHeight` 比 `clientHeight` 多出 16px（上下各 8px），頁面還是可以微幅捲動。修法：在遊戲畫面的 CSS 檔案裡把 `body` margin 歸零。**通用教訓：任何要求「完全鎖定、不可捲動」的版面，除了容器本身的 `overflow:hidden`，也要記得檢查瀏覽器 UA 預設樣式（`body` margin 是最常見的一個）有沒有貢獻出多餘的可捲動高度**
- **後端伺服器（純 `node src/index.js`，沒有監看檔案的機制）不會因為改了程式碼或資料檔就自動生效，一定要手動重啟**：這次新增 `move_to_room` 效果類型＋修改 `starting-rooms.json` 的房間資料後，直接在瀏覽器裡實測，一直卡在 `NO_ROOM_ACTION_AVAILABLE`（即使確認站對房間），花了一段時間才想到伺服器根本沒重啟過、還在跑改動前的舊程式碼與舊資料。這其實是 CLAUDE.md 就明文規定的既有規則（「Debug階段更新完程式碼後，需要關閉SERVER再重開」），這次是第一次真正踩到沒遵守的後果。**通用教訓：任何會動到 `server/` 底下程式碼或 `data/` 底下內容檔的改動，寫完就要立刻重啟後端伺服器再實測，不要等到卡關才想到——尤其這類「資料看起來對、邏輯看起來對，但伺服器記憶體裡還是舊的」的失敗模式，錯誤訊息本身完全不會提示是重啟問題**
- **把獨立機制合併進共用按鈕時，要主動比對兩邊原本的規則是否一致，不能只看介面層級**：開發者要求把免費的樓梯移動併入固定扣 1 點行動力的「操作」按鈕，如果照字面直接合併，樓梯會從免費變成要扣行動力，悄悄改掉一條文件裡明確記錄的既有遊戲規則。這次是讀程式碼時主動發現「操作固定扣行動力」跟「樓梯明確免費」這兩條規則衝突，攤開來問開發者要不要維持免費，不是開發者自己先注意到的。**通用教訓：使用者要求「把 A 功能併入 B 按鈕」這類介面層級的整併指示時，要主動去查 A 跟 B 各自底層的遊戲規則/行為是否相容，不能只看到「兩個按鈕變一個」這個表面需求就直接動手，規則衝突常常不會在需求描述裡被提到**
- **本階段瀏覽器面板的 `screenshot` 動作全程逾時失效（"the Browser pane is not displayed"）**：無法用視覺截圖驗證任何一輪版面改動，全部改用 `mcp__Claude_Browser__javascript_tool` 直接讀取 `getBoundingClientRect()`/`getComputedStyle()`/DOM 內容/`textContent` 來做結構化驗證（例如比對按鈕座標是否落在預期的鄰房預覽帶中心、面板分割比例是否精確符合 flex 比例換算）。這個方法確實能驗證「數值/結構正確」，但無法確認「視覺上好不好看」，開發者這幾輪都是靠自己手機肉眼檢查才抓到問題（例如視野圖片置中留白那次）。**通用教訓：截圖工具失效時，結構化 DOM 檢查可以確認邏輯/數值正確，但不能取代人眼視覺確認，遇到「畫面看起來怪怪的」這類回報還是需要開發者親自檢查，agent 端的結構化驗證只能排除「數值算錯」這一類問題**

**大門廳整合完成後，開發者回報「看不到圖片，還停留在文字除錯頁面」——正確排查方式（供未來同類「開發者回報異常但原因不明顯」情境參考）**：agent 沒有直接假設是程式碼問題就去改，而是先用自己的瀏覽器分頁，在**同一組**剛啟動的測試伺服器上重新走一次完整流程（建房→雙人選角→進入遊戲），確認圖片跟門按鈕都正常運作。因為能重現「正常」，判斷問題出在開發者那端的瀏覽器狀態，不是程式碼——伺服器重啟會斷開 Vite 熱更新的 websocket 連線，如果開發者的分頁是在伺服器重啟前就開著的，會卡在重啟前的舊版程式碼，需要手動重新整理分頁才會抓到新版本。**通用教訓：開發者回報「功能不正常」時，如果剛好重啟過測試伺服器，第一個要排查的方向是「開發者的瀏覽器分頁是不是重啟前開的」，用自己的分頁重現一次（成功或失敗）可以快速判斷問題是在程式碼還是在對方的瀏覽器狀態，不用先假設是程式碼壞掉就急著改**。

**M2c-3 盤點期間發現的架構缺口（已跟開發者確認方向，記錄供 M3 或未來小任務參考）**：
- **傷害系統完全沒有實作，且不只是 M3 combat 需要，好幾張 M2 階段就會抽到的事件/預兆卡也需要**（駭人尖叫、蜘蛛失敗分支、濕滑的地板、天花板塌陷、噬咬）。**開發者已定案的設計**：多點傷害＝跳出 N 次單點選擇視窗（N＝傷害總點數），每次讓玩家選「力量或速度」（肉體傷害）／「知識或意志」（精神傷害）其中一項扣 1 點，且每次視窗都要顯示兩個候選屬性**當下**的級別與實際數值（角色屬性是刻度制，降級不代表數值一定下降，且邪祟後降到最低級別＝死亡，玩家需要真實數據才能判斷）。**逾時規則**：只要其中一點逾時（不管第幾點），從那一點開始（含當次）剩下的全部點數，改用 8/1 規則書原案「儘量平均分配，無法平分則給數值較高的屬性」批次處理，不再繼續逐點跳窗。需要新增：(1) 新效果類型 `damage`（`damageType`+`amount`，`amount` 可能是固定數字或卡片內部先擲一次獨立骰子決定）；(2) `effectResolverManager` 的 `pendingChoice` 要能記住「連續提示鏈」的進度（第幾點/還剩幾點/傷害類型），不是只記一個待解決選擇；(3) 提示廣播要能即時查詢並附上兩個候選屬性的當下級別+數值，不是卡片作者預先寫死的選項。**確認放在 M3 實作**（駭人尖叫等目前卡在這個缺口的卡片，`needsCustomLogic:true` 空著等 M3）
- **房間「結束回合被動加成」跟「離開房間前考驗」都沒有觸發點**：`rooms.json` 裡禮拜堂/圖書室/食品儲藏室/健身房已經有 `effects`（`onceOnlyPerPlayer:true`），塔橋/雜亂的房間/藤蔓糾纏的溫室的文字是「離開房間前要考驗」——這兩種都不是「進房自動觸發」也不是「玩家主動操作」，目前完全沒有對應的觸發點。**已於 2026-08-16／2026-08-17 分別接上觸發點**（結束回合被動加成走 `applyRoomEndTurnBonus`／`game:endTurn`，離開房間前考驗走 `leaveCheck`），這裡記錄的缺口已解決。**但 `onceOnlyPerPlayer` 欄位在 `room_action`（按「行動」）這條路徑上，直到 2026-08-18 搜索機制整分支審查才第一次被檢查**——在那之前，禮拜堂/圖書室/食品儲藏室/健身房如果被 `room_action` 的 `effects` 分支讀到（例如食品儲藏室/健身房這次因為新增搜索欄位才第一次被人注意到），可以透過反覆按「行動」無限疊加結束回合加成，是一個存在已久但沒被踩到的既有缺口，隨搜索機制的分支邏輯修正一併解決（見下方搜索機制條目）
- **`draw_card` 效果的已知限制**：目前只有透過同步流程（`game:selectAction`/`game:move` 的 ack 回呼，或 `game:effectPromptRespond` 的 ack）才能私下通知玩家抽到什麼卡（`game:cardsDrawn` 私人事件）；如果未來卡片把 `draw_card` 放在**逾時自動觸發**的選擇路徑裡（`handleEffectChoiceTimeout`），目前沒有機制可以私訊——因為那條路徑是伺服器計時器觸發、沒有對應的 socket 可用。目前沒有任何卡片用到這個路徑，遇到了再回頭補
- **保險庫（`room_vault`）的效果本體已經可以用 `dice_check`+`draw_card` 表達，但「開一次後永久變空房間」的一次性標記還沒有追蹤機制**（跟上面「房間結束回合/離開考驗」是同一類缺口）——`needsCustomLogic` 先保持 `true`，效果內容已經寫好可用，等追蹤機制做出來就完整了
- **`peek_and_reorder`（原本 M2c-1 就故意留空的「偷看牌堆+洗牌重排」效果）仍未實作**：水晶球原本需要這個機制，但開發者已經把水晶球簡化成「展示牌庫前 3 張供選 1 張，牌庫順序完全不調整」（見下方 `preview_and_choose`），改用更簡單的新機制解決，不需要真的做 `peek_and_reorder`。唯一還卡在這個缺口的只剩通靈板，已確認整張延後到 M3
- **預兆牌現在會在抽到時加進玩家背包（跟道具共用同一個 `inventory`）**：原本抽到預兆牌只會解析一次性 `effects` 就結束，牌本身完全不會被追蹤持有。但水晶球/面具這類卡片文字是「當玩家**使用**...時」，代表需要「持有後、之後某回合主動使用」的道具式流程——`resolveCardDraw` 現在對 `deckType==='omen'` 一律 `addItem`，`game:selectAction` 的 `item` 分支查找內容時同時查 `content.cards.items` 跟 `content.cards.omens`。**副作用**：預兆牌現在也會出現在玩家背包清單裡，前端顯示道具清單時要注意這點（M2d 待辦）

**收工關閉自啟動 server 時，不可用全域 `taskkill /IM node.exe`（2026-08-17，操作疏失記錄）**：收工前關閉本次啟動的測試伺服器，直接用 `taskkill /F /IM node.exe` 依「映像檔名稱」全域關閉，結果一次殺掉 4 個 node.exe（預期只有 2 個，backend／frontend 各自的 npm/npx 包裝行程+實際執行行程剛好各佔 2 個，數字對得上但無法回溯證明），如果開發者當下有其他跟本專案無關的 Node 服務在跑，會被一併誤殺且無法察覺。**已改用的正確作法**：先用 `netstat -ano` 找出目標 port（例如 3001/5173）對應的 PID，再用 `tasklist /FI "PID eq <PID>"` 確認該 PID 確實是 `node.exe`，最後用 `taskkill /F /PID <PID>`（可疊加多個 `/PID`）只關閉這些精準鎖定的行程，關閉後再用 `tasklist /FI "IMAGENAME eq node.exe"` 確認沒有其他殘留。**通用教訓：關閉自己啟動的背景服務，一律用「啟動時記下的 port/PID」精準關閉，絕對不要用全域的 image name／process name 條件，即使當下自己覺得系統上應該只有這幾個同名行程在跑**

**環境問題（M2c-4/M2c-5 執行期間發現）——`server/test/socketHandlers.test.js` 執行後 Jest 進程不會自然結束**：用 `-t` 篩選單一測試（例如 `npx jest test/socketHandlers.test.js -t "..."`）時，測試本身 1 秒內就跑完並印出正確結果，但 Jest 之後會卡住印出 `Jest did not exit one second after the test run has completed. ... asynchronous operations that weren't stopped`，導致包住它的 shell 指令永遠不會回傳（背景執行也一樣，指令本身「完成」但底層 node 進程持續存活）。已重複驗證兩次，結果一致，確認是這個測試檔案既有的非同步 handle（很可能是 socket.io client/server 或計時器）未關閉的問題，跟任何一次程式改動無關。**後續在這個檔案（或整個 `server` 測試套件）上跑測試時的因應方式**：加上 `--forceExit` 旗標（例如 `npx jest --forceExit`）即可正常在數秒內返回，已驗證有效（279/279 全數通過）。如果沒加這個旗標又不想背景執行，改用背景執行＋直接讀取輸出檔案內容判斷測試結果，不要等待指令本身回傳完成；如果懷疑跟先前殘留行程搶資源，先用 `Get-CimInstance Win32_Process | Where-Object CommandLine -like '*jest*'` 檢查並清掉舊的 jest 行程鏈。尚未排查 handle 洩漏的實際來源，也還沒決定要不要修（可能是刻意的 fire-and-forget 設計，也可能是遺漏的 teardown），如果要修，屬於架構決策，需要先跟開發者討論方向，不要自行動手

**`subagent-driven-development` 執行「統一考驗彈窗機制」期間的教訓（2026-08-17，供未來同類多任務執行參考）**：
- **便宜模型（haiku）的 implementer 用「整檔重寫」而非「局部編輯」修改既有測試檔案，導致 UTF-8 BOM＋部分繁體中文字串亂碼，測試檔案直接無法解析（`node --check` 報 SyntaxError），但 implementer 自己還是回報「測試全數通過」**——因為它本地執行測試時用的內容跟最終 commit 進去的內容不一致（很可能是編輯工具本身在整檔寫回時的編碼問題）。這是 Task 1 審查抓到的 Critical，修復方式是從乾淨的 base commit 撈回原始內容、只重新套用該任務要改的部分，不要整檔重寫。**通用教訓：任何 implementer（尤其便宜模型）在派工指示裡都要明確要求「用 Edit 工具做局部修改、不要讀整個檔案再整個寫回去」，且改完繁體中文檔案後要用 `node --check` 驗證能解析、肉眼確認中文沒有變亂碼——這次之後在後續 6 個任務的派工指示都加了這個提醒，沒有再復發**
- **手動實機驗證因隨機性（抽卡/開門）沒能覆蓋到某條路徑時，與其無限期重試碰運氣，不如把這個已知驗證缺口明確告訴最終整分支審查，請它特別加強檢查那條路徑**——這次真的靠這個做法在「沒有實機驗證過的 `CheckModal` 擲骰→動畫→結果流程」抓到一個會讓核心「依序彈窗」功能失效的 Critical bug（`CheckModal` 缺少 `key`）。**通用教訓：手動 QA 覆蓋不到的路徑不是不能合併的理由，而是要明確告知審查者「這段沒有實機驗證，請加強檢查」，讓程式碼審查補上手動驗證覆蓋不到的信心**
- **`subagent-driven-development` 技能流程預設「每個任務最多一輪修正、整分支審查的修正只跑一輪」，但如果修正本身的副作用（複審抓到的新問題）直接命中使用者最初的核心需求，controller 應該主動說明利弊並詢問是否要破例多跑一輪，而不是逕自套用「不能有第二輪」的預設規則、也不是逕自決定要跑**——這次的案例：第一輪修正把 `CheckModal` 的 `key` 從無改成 `pendingCheckQueue.length`，複審發現這個 key 選得不夠精準（佇列長度在「附加到隊尾」時也會變，導致隊首元件被誤觸發重新掛載），這正好會讓「依序彈窗」這個核心功能在特定組合情境下失效，屬於值得破例的案例。開發者同意後才多跑一輪，第二輪複審確認乾淨
- **合併後清理已合併的 worktree 資料夾，第三次踩到同一類「權限拒絕刪不掉」的問題**（前兩次分別記錄在上方 M2D1／dice-interjection 段落）：這次根因跟前兩次一樣——`socketHandlers.test.js` 已知會留下未正常結束的非同步 handle（見上方「環境問題」條目），某個任務執行/審查期間跑過的 jest 行程沒有自然結束，殘留著佔用 worktree 資料夾內檔案的控制代碼。修法一樣：`Get-CimInstance Win32_Process -Filter "name='node.exe'"` 查出 `CommandLine` 包含該 worktree 路徑的行程（這次是兩個殘留的 `jest test/socketHandlers.test.js` 行程），用 PID 精準關閉後才能刪除。**這是第三次踩到，考慮到頻率，未來如果又要對 `socketHandlers.test.js` 跑測試，可以考慮在派工指示裡直接要求 implementer/reviewer 加 `--forceExit` 旗標，從源頭減少殘留行程的機會，而不是每次都事後排查**

**`game:selectAction room_action` 的三分支優先序（2026-08-18 道具合成機制＋搜索機制建立，未來新增房間機制要注意）**：`socketHandlers.js` 的 `room_action` 處理現在依序判斷——① `roomDefinition.craftRecipes` 非空 → 合成流程；② `roomDefinition.effects` 裡有任何一個**不是** `onceOnlyPerPlayer:true` 的項目 → 走既有的通用 effects 解析（保險庫的開保險箱等）；③ 都不符合（含只有 `onceOnlyPerPlayer` 效果、或完全沒有 `effects`）→ 預設走搜索。**這代表任何房間如果同時想要「結束回合被動加成」以外的真正 room_action，加的 `effects` 只要有一項不是 `onceOnlyPerPlayer`，就會自動搶走搜索/合成的機會**——新增房間機制時要記得檢查這個優先序會不會誤判。目前 `actions` 欄位本身只是描述性資料，沒有真正的多選單消費端，一個房間目前只能有一種 room_action 結果，這是已知限制（見下方「目前的瓶頸或停頓點」Minor 2）。

**這次道具合成機制／搜索機制的開發流程都跳過了獨立 worktree（開發者主動選擇「直接在 main 上做」）**：跟過去每個里程碑用 `EnterWorktree`／獨立分支的慣例不同，這次 `subagent-driven-development` 的 5 個任務直接在 `main` 上逐一 commit，`SDD` workspace（`.superpowers/sdd/2026-08-18-search-mechanic/`）在整分支審查通過後正常清理刪除。功能範圍夠小、單人開發（不需要跟其他並行分支隔離）時這樣做流程更精簡，之後如果開發者又選擇不開 worktree，比照這次做法即可。

## 目前的瓶頸或停頓點 (Current Blocker/Status)

**「統一考驗彈窗機制」整分支審查記錄的 6 項 Minor 建議，已於 2026-08-18 處理完畢**：項目 1（缺組合測試）、2（`roomId` 欄位路徑不一致）、4（`STAT_LABELS` 四檔重複）、6（樓梯/崩塌房間掉落缺少 `game:roomEntered`）皆已修復；項目 3（死碼防呆）依開發者指示維持現狀；項目 5（`useEffect` 閉包）已確認安全不需要改。過程中意外發現 `data/cards/item-cards.json` 有 9 處缺逗號的語法錯誤（開發者剛編輯完的內容），已修正（純語法，內容未變動）。詳見下方「除錯注意事項」與「關鍵設定」。

**武器/消耗品分類問題（2026-08-18 記錄，已確認併入 M3 戰鬥系統設計，非本階段處理）**：`category` 欄位表達不了的三個問題——複合分類（例如精緻的十字弩 `item_020`、左輪手槍 `item_001` 的 `category:"weapon,consumable"` 是逗號分隔字串不是陣列，引擎的 `consumeItemIfApplied` 判斷式只認單一字串完全等於 `'consumable'`，永遠判定不了）、限定使用次數（左輪手槍隨機 1～6 次用完才消失，目前沒有「剩餘次數」欄位或機制）、屬性門檻（斧頭 `item_011` 需要力量大於六才能用，`game:selectAction` 完全沒有門檻檢查）。**跟開發者確認：這三個問題本質上是武器攻擊系統的一部分，等 M3 設計戰鬥機制（攻擊/傷害分配）時一併處理，不單獨開一場討論。**

**道具合成機制的通用架構已完成上線（2026-08-18 第 2 次工作階段）**：詳見上方「已完成進度」的道具合成機制條目。目前只接了唯一一組真實配方（廚房：泡麵＋礦泉水→烹飪過的食物）；開發者原本提到的「化學實驗室合成藥物」範例，資料庫裡還沒有對應房間跟道具，之後有實際內容時，直接照 `craftRecipes` 的既有格式在該房間加一筆資料即可，不需要改程式碼。

**搜索機制整分支審查記錄的 7 項 Minor 建議——項目 1/3/4/5 已於 2026-08-19 修復，項目 2 列入下方「下一步行動」待辦，項目 6/7 純提醒不需要動程式碼**：
1. ~~`NO_ROOM_ACTION_AVAILABLE` 從 socket 路徑打不到~~ **已修復**：`turnFlow.js` 補上註解說明這是刻意保留的防呆檢查，不是待清除的死碼
2. ~~`rooms.json` 的 `actions` 欄位目前 server／client 都沒有真正的消費端~~ **已於 2026-08-19 完成（房間多重行動機制）**：`actions` 改成結構化清單，`getRoomActions` 已接上真實消費端，前端「行動」按鈕有 2 種以上有效行動時會開選單。詳見上方「已完成進度」
3. ~~`resolveCardDraw` 的 item 牌堆分支沒有 `addItem`~~ **已修復**：`deckType === 'omen' || deckType === 'item'` 都會呼叫 `addItem`，跟 omen 分支同一套邏輯（含「之後如果內容作者又寫了要持有後使用的 item 卡，記得加 `activatedOnUse:true`」的既有慣例，跟 omen 完全一致）。**開發者確認今後不會再有房間設定用到 `drawType:"item"`**，這條路徑目前仍不可達，純粹是修正函式本身的正確性
4. ~~`placedRoom.item` 會被 `game:stateUpdate` 原樣廣播~~ **已修復（開發者明確要求方向：玩家不該預先知道房間原始/剩餘道具數量，花行動力探索、逐步得知房間物品上限是遊戲樂趣的一部分）**：`gameState.js` 新增 `serializeRoom()`，序列化時直接剔除 `item` 欄位，不只是「不洩漏內容」，而是連「這間房還搜不搜得到東西」都不透過狀態廣播讓玩家知道，只能透過實際搜索的結果（`game:cardDrawn`/`game:searchEmpty`）才會知道。伺服器內部（`performSearch` 讀寫 `gameState.board` 的 Map 實體）完全不受影響，只有廣播給客戶端的副本被裁減
5. ~~缺一條跨回合整合測試~~ **已修復**：新增測試驗證「搜索→結束回合（雙人輪流一圈）→下回合原地再搜一次成功，拿到清單裡剩下的另一件道具」
6. ~~手動驗證時要注意：剛開門探索到的新房間，行動力會直接歸零（既有機制，不是新 bug），要先結束回合才能在該房間搜索~~ **規則已於 2026-08-20 第 2 次工作階段變更**：開門現在固定扣 2 點（不歸零），如果角色行動力夠，開門後可以直接在新房間搜索，不再需要先結束回合
7. 搜索分支在 `performSearch`/`addItem` 之前就先呼叫了 `ack(result)`，如果後續拋錯外層 catch 會二次呼叫 ack——`addItem` 只在道具 id 不合法時才會拋錯，發生機率趨近於零，記錄但不需要處理

**房間美術圖 prompt 已補齊剩餘 33 間，但其中 14 間門框配置已經跟 `rooms.json` 對不上，開發者確認生圖時自行手動調整（2026-08-18）**：`rooms.json` 49 筆房間裡，之前只有 16 筆有 `filename`。比對舊文件 [2026-08-13-m2d3-room-art-prompts.md](docs/superpowers/specs/2026-08-13-m2d3-room-art-prompts.md) 後發現：10 間舊 prompt 仍然有效（門型未變，只是還沒生圖）、4 間舊 prompt 已經因為房間資料變動而過時、19 間是後續內容稽核新增、從未寫過 prompt 的全新房間。新文件 [2026-08-18-room-art-prompts-remaining.md](docs/superpowers/specs/2026-08-18-room-art-prompts-remaining.md) 把這 33 間全部合併成一份、依當時 `rooms.json` 的實際資料重新產出。**寫完新文件之後**，開發者又提供一份更精確的房間門數/樓層規格，更新了其中 16 間房間（見下方「房間門數/樓層規格更新」條目），導致新文件裡有 **14 間房間的「本房間配置」（門框位置/門數）跟現在的 `rooms.json` 已經不一致**（僅 2 間手術室/鍋爐室維持原樣，這 2 間開發者當時只是確認現況、沒有要求改動）。**開發者已明確指示這次不修 prompt 文件**，生圖時會自己手動對照新的門數調整——下次工作階段如果要處理房間美術圖，先跟開發者確認這 14 間是否已經手動調整過，不要直接照文件字面內容生圖。這 14 間分別是：餐廳／天花閣樓／五芒星室／墓園／地下墓穴／地底深淵／酒窖／倉庫／髒亂的房間／風琴室／地下湖／包廂房／崩塌的房間（以上 13 間門數或門型都變了）／臥房沒變不算（臥房本身沒被列入這次調整清單，維持文件原樣）。過程中也發現 17 間 `doors:2` 房間完全沒有 `doorPattern` 欄位，依開發者指示統一補上 `"adjacent"`（後續 16 間規格更新時，其中多間又因為門數變成 1/3/4 而移除了這個欄位，見下方條目）。

**房間門數/樓層規格更新（2026-08-18，已完成並合併進 `main`，開發者已確認理由是「要符合生圖後較合理的房間動線」）**：開發者提供一份精確清單，`data/rooms/rooms.json` 依此更新了 16 間房間：餐廳／地下湖／崩塌的房間改為 4 門；墓園／地下墓穴／酒窖／倉庫／髒亂的房間／風琴室改為 3 門；天花閣樓／五芒星室／地底深淵／地窖改為 1 門；包廂房維持 2 門但 `doorPattern` 從 `adjacent` 改成 `opposite`；手術室／鍋爐室確認維持 2 門/`adjacent`，未變動。樓層變動：五芒星室 `basement`→`any`、墓園 `basement`→`ground`、餐廳 `any`→`ground`，其餘樓層不變。改為 1/3/4 門的房間，`doorPattern` 欄位已一併移除（這個欄位只有 `doors:2` 房間才有意義，見上方美術圖 prompt 規則）。後端測試 469/469 全數通過（門數/樓層資料本身沒有任何測試直接依賴具體數值，純內容資料異動未影響任何既有邏輯）。

**2026-08-16 收工時記錄的 4 項待辦，全部已完成**（UI 直式版面／leaveCheck 失敗扣屬性／三樓層架構／座標式房間連接機制），不再是待辦。

以下是既有、已確認無阻塞的完成進度：`summon-control-and-item-drop`、房間獨立小任務、`dice-interjection`（Part A + Part B）、M2D1（大廳流程）、M2D2（角色選擇正式畫面）、**M2D3 後端資料串接計畫**、**大門廳整合＋遊戲開始畫面**、**M2D3 一般房間地圖骨架**、**`lobby:players` 競態條件修復**、**房間／角色資料稽核與內容補完**、**手機直式版面改版**、**三樓層＋座標式房間連接機制＋leaveCheck 扣屬性**、**角色圖示串接與版面調整**、**統一考驗彈窗機制＋訊息欄可讀化**、**統一考驗彈窗機制 6 項 Minor 修正**皆已完成並合併進 `main`，目前工作目錄就是 `main`，worktree 全部清理乾淨。

**`data/rooms/rooms.json` 的語法錯誤與內容缺口已於 2026-08-19（第 2 次工作階段）修復完畢**：開發者要求檢查「美術圖能否顯示」「每間房都有物品欄位」「描述都已填」「修正發現的語法錯誤」四項。修復內容：檔案結尾的空白佔位房間（`"doors": ,` 缺值，導致整檔無法解析）已刪除；崩塌房間的 id **依開發者選擇改回 `room_collapsed_room`**（開發者這次編輯把它改成 `room_collapsed`，會讓 `turnFlow.js`/`socketHandlers.js`/`mapUtils.js` 三處寫死比對這個 id 的程式碼失效，已跟開發者確認改回舊 id，不改程式碼——開發者其餘的房間改名如 `room_game_room`→`room_gamble_room` 等，皆未被任何程式碼寫死引用，不受影響）；`room_guest_1.item`／`room_EtiquetteSavior.drawType` 的字串 `"null"` 改回真正的 JSON `null`；5 間房間（傭人房/兇殺現場/野獸房/餐廳/手術室）已生圖但 `filename` 仍是 `null`，已補上；紙牌室 `filename` 打字錯誤（`room_gameble.webp`→`room_gamble.webp`）已修正；52 間房間全部補上 `item` 欄位（原本只有 13 間有，缺的補 `item: null` 供開發者填寫）。**額外發現並修復**：`img/rooms/`（開發者暫存資料夾）裡有 38 張圖從未被複製進 `client/public/images/rooms/`（遊戲實際讀取的資料夾），就算 `filename` 寫對也顯示不出來，已全部同步過去並用瀏覽器實測兩張確認 200 OK。後端測試 493/493 全綠，已 commit＋推送。
**手機透過家用路由器連線失敗的根因已排查並修復（2026-08-19 第 2 次工作階段）**：症狀是手機瀏覽器輸入暱稱後無反應。排查過程依序排除：Windows 防火牆（有涵蓋 Public 網路類別的 Node.js 允許規則、無封鎖規則、「封鎖所有連入」總開關關閉、無虛擬網卡干擾，自我模擬連線成功）；路由器 AP 隔離（開發者無路由器帳密，此路暫時無法查證，但已由後續發現排除嫌疑）；`5173`（Vite 開發伺服器）只監聽 IPv6 萬用位址 `::`、未監聽 IPv4 `0.0.0.0`（已修，見下方關鍵設定）；iOS Safari「防止跨網站跟蹤」（開發者實測關閉後問題依舊，排除）。**真正根因**：`client/.env.development`（7/31 專案初期就存在、從未被之前的手機連線修正處理過的殘留檔案）寫死 `VITE_SERVER_URL=http://localhost:3001`，這個值透過 `socket.js` 的 `import.meta.env.VITE_SERVER_URL || window.location.hostname 動態組出的網址` 判斷式，完全蓋掉了上次（2026-08-18/19 第 1 次工作階段）修好的動態網址 fallback，導致不管手機打開哪個網址，Socket.IO 實際連線目標永遠是手機自己的 `localhost:3001`。排查方式：在 `LobbyScreen.jsx` 加一段暫時的連線狀態診斷橫幅（監聽 `connect`/`connect_error`，畫面直接顯示錯誤細節與 socket 實際嘗試連線的 `url`，讓沒有開發者工具的手機也能截圖回報），從 `url: http://localhost:3001` 這個線索反查到問題檔案。已刪除該檔案，診斷橫幅已移除，`vite.config.js` 也一併改用明確的 `0.0.0.0` 綁定。此後手機測試建議優先用 `5173`（開發熱重載）；後端 `3001` 本身透過 `express.static` 也會服務 `client/dist`（靜態建置版本，含 Socket.IO 同源），可作為備援測試路徑，但改完前端程式碼要記得重新 `npm run build` 才會更新。
**道具「給予」選項已完成（2026-08-19）**：`DebugGameScreen.jsx` 新增 `roommates`（同房間、排除自己的玩家清單）並傳進 `CharacterPanel`；道具選單點開後，除了既有「使用」／「遺留」，同房有其他玩家時會多一顆「給予」，點下去列出同房玩家名單給你選，選完呼叫既有的 `giveItemAction`（`mode:'give'` + `targetPlayerId`，伺服器端邏輯本來就有，這次補的是前端 UI）。雙人瀏覽器實測（Bob 把治療藥膏給 Alice）：給予方道具清單變空、行動力正確扣 1，接收方道具清單正確多一項，console 無錯誤。

**暫時除錯框線已移除（2026-08-19 第 3 次工作階段）**：視野正方形（綠）／狀態面板（藍）兩區域的虛線框已從 `DebugGameScreen.jsx` 移除，版面確認過關。

**角色美術圖更新（2026-08-19 第 3 次工作階段，已完成）**：`char_005`（石川大輝）改用新圖（`male_athlrte`→`male_Parkour`，職業「運動員」→「跑酷玩家」，開發者自己改的 `characters.json`）；`female_Nurse`/`female_painter`/`female_police` 三張圖片內容更新（檔名不變，實際圖檔換新版）。皆已從 `img/characters/` 同步進 `client/public/images/`，並用 md5 checksum 驗證內容一致（避免 `cp` 只更新 mtime 沒真的換內容的疑慮）。

**遊戲畫面狀態面板（CharacterPanel）全面改版（2026-08-19 第 3 次工作階段，已完成並合併進 `main`）**：這階段是開發者拿著手機截圖持續來回調整版面，過程中發現前幾輪的桌機（1280px）視覺驗證跟手機實際窄螢幕（390px）落差很大，之後全程改用 Browser pane 的手機寬度視窗（390×844）測試，重大教訓見下方關鍵設定。最終版面（`client/src/gameplay/CharacterPanel.jsx`）：
  - **狀態區（在上）**：左側固定 28% 寬的角色形象框（`characterContent` 新增串接進 `CharacterPanel`，`findCharacterPortrait` 查 `character.filename` 顯示玩家所選角色的立繪，暱稱文字放圖片下方當說明），高度自動撐開跟右側等高；右側「行動」（原「行動力」，改用固定 8 格顯示，見下）與四項能力皆靠右對齊，5 行間距統一
  - **行動力顯示改用固定 8 格**（`ActionPointBoxes`）：不再顯示數字，代表遊戲全域行動力上限；前 `actionPoints` 格深綠框淺綠底（目前擁有）、`actionPoints`~`maxActionPoints` 格深綠框白底（角色上限內但目前已用掉）、超過角色自己 `maxActionPoints` 的格虛線框白底（角色永遠碰不到，原本設計是黑底，開發者反映跟角色形象框的除錯黑框搞混，已改虛線白底）
  - **道具區（在下）取代原本的訊息欄位置**（訊息欄暫時不顯示，`messages` prop 已從 `CharacterPanel` 移除）：固定 3 排 4 格（第一/二排道具、第三排預兆粉紅底），依 `cardContent.omens` 判斷道具實際類型分流，格數不足用虛線佔位格補滿維持格線，點擊道具跳出使用/遺留/給予選單不變
  - **px 全面改成百分比（RWD）**：所有寬度/間距類數值改用 `%`（例如角色形象框 `flex:'0 0 28%'`、道具格 `21.875%`→後來微調為 `22.5%`）；正方形格子（能力級距條、行動力格）改用 `flex:1` 平分容器寬度＋`aspectRatio:'1'` 維持正方形，不再寫死 24×24px；字體大小/邊框寬度/裝飾性圓角等維持 px（原因見下方關鍵設定）
  - **四角按鈕文字更新**：「行動」→「房間行動」、「襲擊」→「襲擊目標」（`DebugGameScreen.jsx`，皆透過既有 `wrapLabel` 拆成兩行）；四角按鈕新增明確邊框（深灰 `#555`、底色淺灰 `#f0f0f0`，原本是瀏覽器預設樣式）；南北向「移動」維持一行兩字，東西向「開門」改兩行各一字（`FocusedRoomView.jsx`）；四角按鈕與方向按鈕字體最終定案 20px（歷經 24px 中間值）
  - **開門按鈕文字/樣式**：`kind:'open_door'` 從「開門」改「解鎖」，紅色框線＋淺紅底（原本灰色虛線框），`kind:'move'` 維持綠框綠底不變

**視野區域房間格局部縮放功能（2026-08-19 第 3 次工作階段，已完成並合併進 `main`）**：走 `brainstorming` 流程確認設計（[設計文件](docs/superpowers/specs/2026-08-19-room-tile-zoom-design.md)），開發者當場核准後直接指示實作，沒有另外走 `writing-plans`（範圍單一、開發者已透過互動問答把每個細節都定案，判斷不需要）。技術細節見下方關鍵設定專屬條目。

**本次階段（2026-08-17）瀏覽器面板的 `screenshot` 動作全程逾時失效**，無法用視覺截圖驗證任何一輪版面改動，全程改用 JS 結構化檢查（`getBoundingClientRect`/`getComputedStyle`）驗證數值與結構正確性，但沒辦法確認「視覺上好不好看」——下次工作階段開始時，建議先確認這個工具是否恢復正常，如果還是不行，版面類的改動要更依賴開發者手機肉眼確認，不能只靠 agent 自己的結構化檢查就宣稱做完。

**其餘一般房間的美術圖，持續由開發者生成中**：一般房間的地圖顯示元件（`FocusedRoomView`/`RoomTile`）已經完成，沒有美術圖的房間會自動顯示色塊＋名稱佔位，不會壞掉。目前 `rooms.json` 31 筆裡有 16 筆已補上 `filename`（對照 `img/rooms/` 資料夾），15 筆仍是 `filename:null`。31 間房間的美術圖 prompt 全數已產出（[room-art-prompts.md](docs/superpowers/specs/2026-08-13-m2d3-room-art-prompts.md) 30 張＋設計文件裡的禮拜堂 1 張），開發者陸續拿去生圖引擎生成中，之後只需要重跑一次補 `filename` 的腳本（或手動補）即可接上，不需要再改程式碼。

**角色圖片去背問題已解決（2026-08-17）**：舊版角色圖去背瑕疵（腳下陰影殘影、浮水印未去除）的問題已經不存在——開發者自行處理完成，2026-08-17 直接提供了一整批全新命名的角色圖檔（`male_priest`/`female_Nurse`/`male_worker`/`female_police`/`male_athlrte`/`female_painter`，含 `_icon` 版本），舊檔名（`HighScholl.png`/`Lumberjack.png`/`girl.png`/`nurse.png`/`oldman.png`/`police.png`）已被取代，見上方「已完成進度」的角色圖示串接條目。

**舊的 worktree 清理問題已解決**：`.claude/worktrees/m2c4-m2c5-action-and-haunt`、`.claude/worktrees/m2d2-character-select` 皆已不存在（`git worktree list`/`git branch -a` 皆確認），不需要再處理。

**commit `52cdd1d` 的歷史備註（僅供之後對照 commit 歷史時參考，非待辦）**：該筆提交因操作疏失把「`modifiers.js` 的 `removeWhen` 改可選」這個程式碼修正跟「M2c-3 第一批內容草稿」合併成同一筆——功能跟測試都正確，只是 commit 訊息只提到程式碼修正沒提到內容。

**`data/cards/item-cards.json` 尾端有 2 筆空白佔位資料**（`id`/`name`/`text` 都是空字串）——本次全分支審查合併時發現，已確認這是**分支開始前就存在**的既有資料（不是這次改動造成的），維持原樣沒有動它，僅記錄供之後留意。

## 下一步行動 (Next Steps)

**2026-08-21 開發者重新整理過的待辦清單（取代舊清單；「物品攜帶數量上限」已完成移出，「地下平台美術圖」已完成移出，舊的「執行順序待確認」meta 備註已隨此次重整移除）：**

1. 讀取本 Handover；worklog 讀最近一次工作階段範圍即可
2. **（2026-08-22 已完成，item/event 部分）道具卡/事件卡 text 補完＋回饋文字抽取**：item（48 張）／event（35 張）的 `name`/`description`/`text` 已全數補齊，且已完成「」回饋文字抽取（詳見上方最新更新摘要）。**預兆卡（omen）開發者說還要再調整，這次特意不動，不算完成**——下次開工先確認 omen 目前進度再決定要不要延用同一套抽取規則
2a. **weapon 卡的 `attackStat`/`attackDice` 實際數值尚未填（2026-08-22 記錄）**：欄位骨架已建立（9 張武器卡，見上方），值還是 `null`。開發者會自己填無效果型（`item_041`）；有額外機制的武器（`item_001`／`item_012`／`item_020`／`item_035`）之後要跟開發者討論怎麼把 `text` 描述的機制轉寫成 `effects`，這是判斷力較高的工作，不要自己猜著填
~~2b/2c. 道具手持/配戴機制＋道具選單動態化~~ **已於 2026-08-23 第 2 次工作階段完成並合併，不再是待辦**，詳見上方最新更新摘要。
2d. **scratchpad 備份的額度中斷風險（2026-08-22 記錄）**：scratchpad 是 session-specific 暫存資料夾，不受 git 管理，額度中斷或開新對話階段可能導致舊備份找不到。已改成「改完一批、測試沒問題就直接 commit 進 git」取代單靠 scratchpad 備份，commit 前務必先給開發者確認過（開發者要求自己看過才 commit，不是 agent 自主決定時機）
3. **角色 icon 六格定位機制（2026-08-19 第 3 次工作階段開發者提出，尚未實作，需要先 brainstorm）**：目前玩家圖標定位是依進入方向靠邊＋同側多人自動錯開（見下方 `player.enteredFromSide` 條目）。開發者提出改成固定六個佔位點（考量最大 6 人遊戲，同房兩位角色由同一門進入時），從左上到右下依序、依角色 ID 數字排列。這是全新的定位邏輯，跟現有的 `enteredFromSide`/錯開機制不同，開發者已同意需要另外設計討論
4. ~~遊戲訊息彈窗流程與機制~~ **已於 2026-08-25 第 2 次工作階段完成並合併進 `main`，不再是待辦**，詳見上方最新更新摘要。訊息欄（`messages` 陣列）本身維持現狀不動，這次是新增獨立的彈窗顯示層
4a. **`eventIntro`/`eventNoCheck` 廣播範圍收斂（2026-08-25 開發者確認，列為下階段待辦，尚未實作）**：目前這兩種彈窗廣播給全房間玩家，不只抽卡的人自己看得到（`game:cardDrawn` 本來就是 `io.to(roomCode)` 廣播，`onCardDrawn` 沒有比照 `roomIntro`/`itemUseResolved` 做 `data.playerId === playerId` 過濾）。開發者已確認要**改成只給抽卡的人自己看**——下次開工可以直接照 `client/src/DebugGameScreen.jsx` 的 `onRoomEntered`/`onItemUseResolved` 既有過濾寫法，在 `onCardDrawn` 的 `eventIntro`/`eventNoCheck` 兩個 push 上加同樣的 `data.playerId === playerId` 判斷（`itemDrawNoCheck` 維持不過濾，這次沒有要動它，開發者只提到 event/omen 這兩種）
5. **M2c-3 其餘卡片機制，等待 M3 的列為一項（延後不是本階段待辦，記錄供之後參考）**：仍卡在幾個機制缺口（見上方除錯注意事項），依開發者已確認的方向處理——傷害系統（`damage` 效果類型＋連續提示鏈＋即時數值顯示）與武器攻擊類卡片（含 `category` 分類/限定次數/屬性門檻三個問題，見上方）留給 M3；通靈板整張延後到 M3；`item_008`（中世紀鎧甲，減傷＋防偷竊）也綁在 M3 傷害/偷竊機制上
6. **M2D3 細節①：公開資訊（目前預兆數）**：M2D3 骨架完成之後的細節項目，尚未實作
7. **M2D3 細節②：私人資訊區塊的預留版位（陣營/勝利條件）**：M3 後才有實際內容，這次先預留版位即可
8. **M2D3 細節③：操控實體切換的完整 UI**：犬靈是第一個真實案例，目前只有除錯頁面的最小按鈕，之後 M3 叛徒切換多隻怪物沿用同一套
9. **M2D3 細節④：完整的「擲骰道具介入」選擇畫面**：`dice_check`/`leaveCheck` 兩條路徑現在都完成了，目前除錯頁面都只有最小可用版本——**統一考驗彈窗機制做完後，這裡會不會一併整合進去，值得跟開發者確認**
10. **擲骰機制複查，需要開發者提供例子（2026-08-19 第 3 次工作階段開發者手動測試時發現，尚未修復）**：開發者反映高數值角色多次擲出不合理的低點數。Agent 初步查證：`effectResolver.js` 的 `handleDiceCheck`（`baseCount = getStatValue(player, effect.stat)`）跟 `turnFlow.js` 的 `leaveCheck`/崩塌房間檢定（`getStatValue(player, leaveCheck.stat)`），骰子數量計算本身看起來**已經是用屬性當前實際數值**（`playerEntity.js` 的 `getStatValue` 回傳 `track[currentIndex] + overflow`，不是格子索引/級距），表面上跟開發者期望方向一致，還沒找到實際落差在哪。骰子是自訂骰面 `0/0/1/1/2/2`（見下方關鍵設定），單顆期望值僅 1，不確定是機率本身符合設計但玩起來主觀感覺不合理、還是真的有計算錯誤，**需要開發者提供具體案例（角色/屬性/數值/實際擲出結果）才能繼續往下查**

**以下是仍然有效、未被這次重整取代的既有待辦**：
- **全部完成後，開發者要手動從頭跑一次完整流程**：建房→加入→鎖門（目前是選角開始時隱含鎖門，不是獨立按鈕，已跟開發者確認這個理解一致）→選角（現在是正式畫面，非隨機/佔位）→開始遊戲→（迴圈）選擇行動/開門/移動/觸發房間效果/觸發卡片效果/改變狀態/**手動呼叫結束回合換人（注意不是行動力歸零自動換人）**，直到邪祟考驗觸發邪祟為止。邪祟觸發後的戰鬥內容是 M3，這次測試不涵蓋
## 關鍵設定 (Key Context & Rules)
- **技術棧**：Node.js + Express + Socket.IO（伺服器持有權威遊戲狀態）＋ React (Vite) 前端；純 JavaScript，不使用 TypeScript；單一程式碼庫同時支援區網與雲端部署
- **開發者背景**：新手，主要靠 Claude Code 協作開發；**除錯時遇到非顯而易見的錯誤必須停下列出可能原因與開發者討論，不可自行試錯修改後重跑**
- **輸入驗證慣例（M2a 確立，沿用至今）**：所有函式對不合法輸入一律拋出自訂 `Error`，訊息用 UPPER_SNAKE_CASE 字串，不可靜默失敗
- **角色屬性是刻度制，不是連續整數**：`track`/`currentIndex`/`baseIndex`/`skullIndex`
- **回合機制關鍵慣例**：樓梯移動免費（2026-08-17 起改走 `room_action`＋`move_to_room` 效果＋`freeAction` 旗標實作，見下方專屬條目，底層 `useStairs`/`canUseStairs` 仍保留但前端已不呼叫）、`advanceTurn` 自動重設行動力、所有回合內動作都要驗證 turn ownership、效果選擇未解決前不可推進回合（見上方「除錯注意事項」）
- **回合結束機制（`summon-control-and-item-drop` Task 5，已合併進 `main`）**：行動力歸零**不再**自動結束回合，全體玩家都要手動呼叫新的 `game:endTurn` 才會換人（即使行動力還沒用完也可以提前結束）。`advanceTurnIfOver` 已整個移除；`turnFlow.js` 新增 `endTurn(gameState, playerId)`（`NOT_YOUR_TURN`/`SUMMON_ACTIVE` 兩種拒絕情境，`SUMMON_ACTIVE`＝操控召喚物期間必須先消散才能結束回合）。`useStairs` 也一併補上 `SUMMON_ACTIVE` 檢查（全分支審查發現的漏網之魚，見上方除錯注意事項）。**除錯頁面 `DebugGameScreen.jsx` 已補上最小的「結束回合」按鈕**（否則除錯頁面連過第二回合都做不到），完整的行動選單 UI 仍是 M2d 的範圍
- **「架構性缺口」vs「防呆修正」的處理原則**：只是輸入驗證/防呆類（有明確既有慣例可套用）可以直接修正不用問；牽涉新的遊戲規則/架構設計決策，必須先跟開發者確認方向
- **「操作」跟「道具」的定義（8/1 `turn-flow-and-action-points.md` 已核准，容易混淆，注意）**：「道具」＝使用手上持有卡片的主動能力（面具戴脫、魔術方塊考驗等）；「操作」＝**房間本身**觸發的機制（例如保險庫知識考驗開鎖），不含卡片能力。兩者的 `effects` schema 相同，都可以直接用 `effectResolver.resolveEffects`
- **20 秒兩層計時提問 UI**：8/1 已核准的設計，M2b-2、M2c-4 都明確決定先不做，改用直接事件模式——**這是暫時簡化，不是取消，M2 完整測試跑完後要記得補回去**
- **邪祟觸發規則（M2c-5，已實作於 `resolveCardDraw`）**：每抽一張預兆卡，`omenCount` 遞增，骰 `omenCount` 顆骰子（0/0/1/1/2/2 面），總和 >5 觸發 `hauntStarted=true`——不是實體遊戲的「同名預兆抽第二張」規則
- **物品 `category` 三分類規則（M2c-4，已實作）**：`category` 為 `武器`/`消耗品`/`一般`（欄位值為英文 `weapon`/`consumable`/`general`）。消耗品是否移除看效果是否「生效」（`appliedCount > 0`），不是看玩家是否嘗試使用——考驗類道具（如魔術方塊）未通過視為未生效，不移除。預兆卡**沒有**消耗品分類（必須留在場上供 `omenCount` 計數），這是硬性不變量
- **「操作」（room_action）的行動力時機（M2c-4 Task 5 執行期間確認）**：開新房間會讓行動力歸零、回合立刻結束，同一回合不可能再觸發操作，必須等玩家下一次輪到行動才能對已開的房間操作／考驗；但進房間自動觸發的卡片效果（抽卡）不受此限，仍要解析完才能結束回合——兩者是不同機制
- **`draw_card` 效果類型（M2c-3，已實作）**：`{type:"draw_card", deck:"item"|"event"|"omen", count:N}`，從指定牌庫隨機抽 N 張加入背包，牌庫抽空時中途停止；抽到的卡透過 `game:cardsDrawn` 私人事件只通知抽卡玩家本人，不廣播給其他人（僅限同步流程，見上方除錯注意事項的已知限制）
- **傷害系統設計（已跟開發者定案，確認放在 M3 實作，見上方除錯注意事項）**：多點傷害＝連續跳出 N 次單點選擇視窗，每次顯示候選屬性當下級別+數值，逾時後剩餘點數改用「儘量平均分配」批次處理
- **`persistent_modifier` 的 `removeWhen`（M2c-3 已修正為可選，且已接上真實呼叫點）**：省略＝永久不解除；可以填單一物件或陣列（陣列＝符合任一個條件就解除）；`checkRemoveConditions` 現在會在每次 `game:move` 完成後（檢查同房間所有玩家的 `meetsAnotherPlayer`）跟每次效果解析完成後（檢查解析玩家目前背包裡每一件道具的 `holdsItem`）真的執行，不再是純資料
- **`preview_and_choose`/`take_previewed_card` 效果類型（M2c-3，水晶球用）**：展示牌庫最前面 N 張（牌庫本身建立時就洗過牌，等同隨機展示），動態產生選項讓玩家選一張直接拿走，其餘完全不動、不重排。完全沿用既有 `choice`/`pendingChoice` 機制，只是選項是即時算出來的
- **`toggle_active` 效果類型（M2c-3，面具用，可泛用）**：背包道具項目多一個 `active` 布林欄位，`{itemId, activeEffects, inactiveEffects}` 依目前狀態套用其中一組並切換狀態，之後其他「有生效狀態」的道具可以直接沿用
- **`persistent_modifier` 的 `blocksOpenDoor` 效果（M2c-3，電池耗盡用）**：`{hookType:"blocksOpenDoor"}`，`turnFlow.js` 的 `getAvailableDirections` 檢查到就不列出開新房間的選項，但已存在的相鄰房間仍可移動
- **犬靈操控切換＋道具給予/遺留/撿取（已全部完成並合併進 `main`）**：玩家物件新增 `summons` 欄位（單一物件不是陣列，`type` 欄位分辨召喚物種類，之後可擴充不限犬靈，`undefined`／`null`＝無召喚物；`{type, floor, x, y, actionPoints, carryingItemId}`）跟 `summonUsedThisTurn` 布林欄位（一回合限一次切換，`advanceTurn` 於離開玩家身上重置，兩者都是 lazy-optional，`createPlayer` 不初始化）；房間動態狀態（`gameState.board` 裡的房間物件，不是靜態 `rooms.json`）新增 `droppedItems` 陣列。新效果類型 `switch_control`（`{summonType, actionPoints}`，建立 `summons`，含 `INVALID_SWITCH_CONTROL_EFFECT`/`SUMMON_ALREADY_ACTIVE`/`SUMMON_ALREADY_USED_THIS_TURN` 三種拒絕情境）。`turnFlow.js` 新增 `moveSummon`/`selectSummonAction`（操控召喚物期間限定 移動/撿取/遺留/消散，消散＝純狀態切換不算結束回合，行動力歸零不影響消散）；`selectAction` 的 `item` 動作新增 `mode: 'give'|'leave'|'pickup'`（預設/`'use'`＝原本邏輯，`leave`/`pickup` 都會保留道具物件本身的狀態，不會重建成只剩 `{id}`）。`socketHandlers.js` 的 `game:move`/`game:selectAction`/`game:useStairs` 依 `player.summons` 是否存在分流／擋下。**新增 `activatedOnUse` 卡片旗標**（`resolveCardDraw` 看到就跳過抽卡當下的效果解析，只在玩家之後主動用 `game:selectAction actionType:'item'` 使用時才解析）——這是全分支審查抓到的通用機制，任何「持有後才主動使用」的卡片都要記得標，不是犬靈專屬（詳見上方除錯注意事項的 Critical 案例）。詳見 [設計文件](docs/superpowers/specs/2026-08-09-summon-control-and-item-drop-design.md)、[實作計畫](docs/superpowers/plans/2026-08-10-summon-control-and-item-drop.md)
- **房間「結束回合被動加成」（`onceOnlyPerPlayer`，已合併進 `main`）**：房間 `effects` 裡的 `stat_change` 效果可以帶 `onceOnlyPerPlayer:true`，`game:endTurn` 結束回合時會檢查玩家所在房間，套用尚未領過的加成，記錄進玩家新欄位 `player.roomBonusesReceived`（房間 id 陣列，lazy-optional）——每人每間房限一次，不限這次結束回合是不是第一次進這間房
- **房間「離開前考驗」（`leaveCheck`，已合併進 `main`）**：房間內容新增 `leaveCheck: {stat, min}` 欄位（塔橋/雜亂的房間/藤蔓糾纏的溫室），`moveToRoom`（`turnFlow.js`）新增參數，離開房間前（不管是移動到已知房間還是開新門）先用該屬性當下數值擲骰考驗，通過才正常移動；沒通過扣 1 點行動力（跟正常移動花費一樣，不會多扣），原地不動，行動力夠可以當回合重試；開新門失敗**不會**抽卡也不會歸零行動力，只有真的開門成功才會
- **可被道具介入的擲骰（`dice-interjection`，Part A + Part B 皆已合併進 `main`，功能至此完整涵蓋 `dice_check` 與 `leaveCheck` 兩條路徑）**：卡片新增頂層欄位 `diceInterjection`（`{scope:"any"|"eventTriggered", bonusDice?, override?, cost?, consumesItem}`，跟 `effects` 平行，不是塞進 `effects` 陣列），宣告這張道具能介入「別人正在進行的擲骰」。**Part A（`dice_check`）**：擲骰前（`effectResolver.js` 的 `handleDiceCheck`）會掃描玩家背包，透過 `context.itemCatalog`（呼叫端從 `content.cards.items` 準備好往下傳，`effectResolver.js` 本身不直接讀內容目錄）找符合條件的道具，有的話**不擲骰**，改跳出新的獨立暫停狀態 `pendingRollChoice`（跟既有 `pendingChoice` 平行、各自獨立的逾時計時器 map 與 socket 回應事件 `game:diceChoiceRespond`，兩者絕對不能共用同一個欄位），玩家選好（或逾時預設不使用）後接續原本的擲骰／`tiers` 評估邏輯。純邏輯部分（`findInterjectionOptions`/`resolveFinalRoll`）獨立成 `server/src/game/diceInterjection.js`，刻意不依賴 `gameState`/`content`。套用道具代價/消耗追蹤/覆蓋值/加骰/`onBeforeRoll`/`onAfterRoll` modifier 的邏輯集中在 `effectResolver.js` 的 `computeInterjectedRoll`（已 export，供 Part B 直接呼叫，不重寫）。**Part B（`leaveCheck`）**：`moveToRoom`（`turnFlow.js`）擴充成兩階段——沒有 `resolvedRoll` 時先檢查介入選項，有的話回傳 `{kind:'leaveCheckPending',...}` 不擲骰不扣行動力；`socketHandlers.js` 的 `game:move` 收到這個結果就開啟 `pendingRollChoice`（`resumeKind:'leaveCheck'`，沿用同一組 `pendingRollChoice`/逾時/`game:diceChoiceRespond` 機制，只是新增一個分支，不是另建一套），`ack` 回傳 `{kind:'leaveCheckPending'}`（跟其他動作的 ack 語意不同——因為移動本身還沒有結果，這是跟開發者明確確認過的設計）。玩家回應後，`resumeRollChoice` 新分支呼叫 `computeInterjectedRoll` 算出最終骰值，帶著 `resolvedRoll` 再呼叫一次 `moveToRoom` 完成移動判定；「同房間玩家 modifier 檢查＋抽卡收尾」抽成共用的 `finishMoveResult`（`socket` 可為 `null`，逾時路徑不私訊 `game:cardsDrawn` 但其餘照常執行），直接移動路徑跟介入恢復路徑都呼叫它。`player.diceInterjectionUsedThisTurn`（道具 id 陣列，lazy-optional）追蹤「這回合已經用過」的非消耗品道具，`advanceTurn` 對離開玩家重置。**重要教訓（詳見上方除錯注意事項）**：任何會消耗玩家資源（扣道具、扣行動力）的動作，驗證輸入永遠要排在消耗之前；任何新增的暫停狀態都要主動清空可能共存的其他暫停狀態；**新路徑重用既有邏輯的一部分時，要主動比對這段邏輯在其他既有路徑上是否也該套用，不能只顧新路徑本身正確**（Part B 的 modifier 不一致案例）。詳見 [Part A 設計文件](docs/superpowers/specs/2026-08-10-dice-interjection-design.md)/[計畫](docs/superpowers/plans/2026-08-10-dice-interjection-part-a.md)、[Part B 設計文件](docs/superpowers/specs/2026-08-11-dice-interjection-part-b-design.md)/[計畫](docs/superpowers/plans/2026-08-11-dice-interjection-part-b.md)
- **M2D1 大廳流程（已合併進 `main`）**：`client/src/LobbyScreen.jsx` 是畫面狀態機協調層（`'start'|'nickname'|'lobbyList'|'waitingRoom'|'placeholder'`），5 個子畫面元件都在 `client/src/lobby/`（`StartScreen`/`NicknameModal`/`LobbyListScreen`/`WaitingRoomScreen`/`CharacterSelectPlaceholder`）＋共用 `lobby.css`／`errorMessages.js`。後端新增 `lobby:list`（可加入房間查詢，`maxPlayers` 一律等於 `content.characters.length`，「進行中」判斷跟既有 `lobby:join` 的 `ROOM_IN_PROGRESS` 共用同一條件）、`lobby:leave`（主動離開，一般玩家只移除自己，房主觸發整房解散）、`lobby:closed` 廣播、`LobbyManager` 的 `isHost`（`getPlayers` 回傳形狀的破壞性變更）／`getHostName`／`closeRoom`／`listJoinableRooms`。**房主離開（`lobby:leave` 或 `disconnect`）一律解散整個大廳**，共用 `closeLobbyRoom` 函式，`fetchSockets()` 取得房間目前成員快照後，**必須先廣播 `lobby:closed` 再讓大家離開房間**（廣播目標是即時房間成員，先離開房間會讓廣播送不到任何人，這是 Task 3 審查抓到的真實 bug，見上方除錯注意事項）。**房主自己也會收到 `lobby:closed`**（廣播當下房主還在房間裡），前端刻意讓這個廣播（不是 `lobby:leave` 的 ack）驅動房主自己的畫面轉場，避免兩個轉場來源互相搶跑。詳見 [設計文件](docs/superpowers/specs/2026-08-11-m2d1-lobby-flow-design.md)、[實作計畫](docs/superpowers/plans/2026-08-11-m2d1-lobby-flow.md)
- **M2D2 角色選擇正式畫面（已合併進 `main`）**：`LobbyScreen.jsx` 收斂成唯一的畫面協調層（畫面狀態機 `'start'|'nickname'|'lobbyList'|'waitingRoom'|'characterSelect'|'playing'`），統一監聽 `lobby:closed`/`game:prompt`/`game:promptResolved`/`game:characterSelectUpdate`/`game:started` 五個事件，`game:characterSelectUpdate` 用 functional updater 只在第一次觸發時切畫面（避免後續廣播重複導航）。`client/src/lobby/CharacterSelectScreen.jsx`：角色圖片高度 `(character.tall / maxTall) * 66.67vh`（最高者 2/3 螢幕高度，其餘按比例縮小），橫向可捲動排列（`.cs-row`），允許橫向溢出優先於塞滿單一螢幕；點擊圖片觸發跟 hover 相同的浮出+加亮效果，並開啟屬性資料卡 modal（`STAT_LABELS` 對照 `might`/`speed`/`knowledge`/`sanity` 四項能力）；確定選擇 emit `game:promptRespond`，只有輪到的玩家「確定選擇」按鈕才亮起，未輪到者可以自由瀏覽/開卡但按鈕禁用；已鎖定角色疊加 `.cs-locked-badge` 徽章圖（`selected.png`）。禁用文字採 `isMyTurn`/`isLocked`/`isMine` 三態判斷（`isMine`＝自己已確認的角色，顯示非錯誤樣式的等待訊息；`isLocked`＝別人選走，錯誤樣式；否則＝還沒輪到自己，錯誤樣式）——這是全分支審查才補齊的第 3 態，見上方除錯注意事項。`DebugGameScreen.jsx` 新增 `initialGameState` prop，由協調層捕捉 `game:started` payload 後往下餵（該事件是一次性、且協調層需要它才能決定要不要渲染 `DebugGameScreen`，所以協調層必須搶在元件掛載前先收下，元件內部原本的監聽器因此變成永久 dead code，保留不刪）。**角色圖片目前仍是實體攝影棚淺灰背板去背前的原圖**（開發者已決定自行處理去背，見上方「目前的瓶頸或停頓點」）。詳見 [設計文件](docs/superpowers/specs/2026-08-12-m2d2-character-select-design.md)、[實作計畫](docs/superpowers/plans/2026-08-12-m2d2-character-select.md)
- **M2D3 遊戲進行畫面（設計文件＋後端資料串接已完成並合併；大門廳整合＋起始畫面已完成並合併；其餘 27 間一般房間的地圖顯示尚未開始，見「目前的瓶頸或停頓點」）**：畫面骨架＝左 2/3 地圖區（永遠聚焦目前房間＋鄰居一角，總覽地圖用按鈕切換、是玩家個人探索紀錄不是全體共用）、右 1/3 人物面板（僅顯示自己）。**房間美術圖風格定案為完全平面俯視圖**（無透視傾斜、無方向性光線），這是三輪實際生圖比較後的結論（娃娃屋等角透視氛圍最好但近側牆不存在無法放門框；地牢圖磚等角透視仍有透視傾斜無法靠旋轉對應任意門位置）——選這個風格是因為它能靠**旋轉單張圖**對應這局遊戲隨機出來的真實門位置。**房間專屬「排除牆」**（例如禮拜堂祭壇牆永遠不放門）**不需要新增後端資料**——已推導確認：只要排除牆在原圖裡本來就是牆的身分，不管真實門位置怎麼組合，渲染時永遠能挑到一個旋轉角度讓排除牆落在真實的牆位置上（旋轉整張圖一起轉，門/牆相對關係不會被打散），唯一限制是只有 `doors≤3` 的房間能設排除牆。**後端資料串接（已完成並合併）**：`game:started` 廣播新增 `roomContent:{rooms,startingRooms}`（比照角色選擇畫面已在用的模式）；`serializeGameState` 的 `roomDeck` 新增 `hasRoomForGround`/`hasRoomForUpper`，前端拿這個欄位＋已收到的 `board.ground/upper` 房間列表自己推算「目前房間四個方向的門狀態」（邏輯對應 `turnFlow.js` 的 `getAvailableDirections`，刻意不新增逐玩家推播機制，避免大幅更動十幾個 `game:stateUpdate` 廣播點）；`player.visitedRooms`（`{floor,x,y}` 陣列）在 `playerEntity.js` 的 `movePlayerTo` 單一匯聚點追蹤，`createPlayer` 建立時以起始位置初始化，涵蓋所有 3 個真實移動路徑（移動至鄰室／開新門／樓梯）。全分支審查抓到 1 個 Important（三項新資料組合後沒有整合測試同時驗證，各任務審查者各自看不到這個死角）已補上，418/418 測試全綠。詳見 [設計文件](docs/superpowers/specs/2026-08-13-m2d3-gameplay-screen-design.md)、[後端計畫](docs/superpowers/plans/2026-08-13-m2d3-backend-data-wiring.md)
- **`doorPattern` 欄位（已實作並合併，`rooms.json` 已由開發者填完全部 17 間 `doors:2` 房間）**：`doorLayout.js` 的 `computeDoorLayout` 新增第 4 個參數 `doorPattern`（`"opposite"|"adjacent"|null`），只在 `doorCount===2` 時生效，把原本每次隨機決定的「相鄰／相對」門型固定成該房間宣告的值（省略則維持原本隨機行為）；`boardGenerator.js` 的 `placeNewRoom` 把 `roomDefinition.doorPattern` 傳遞下去。動機：2 門房間原本只出 1 張圖靠旋轉涵蓋任意門位置組合是不可能的（相鄰／相對是兩種無法互相旋轉得到的形狀），開發者決定改為每間房固定其中一種形狀、對應美術圖只需一張。**`room-art-prompts.md` 已同步修剪**：16 間 2 門房間各自只保留跟 `doorPattern` 相符的那組 prompt（原本每間 2 張變體圖，共 46 張→現在 30 張，全套 31 間房間共 31 張）。
- **大門廳整合＋遊戲開始畫面（已完成並合併進 `main`，已在瀏覽器手動驗證整個流程）**：開發者原本構想單一 3x1 大房間當起始大門廳，跟現有引擎「每間房固定佔 1 格」的座標模型不相容，改為**三個 1x1 房間縱向堆疊**（`room_lobby_a`/`room_lobby_b`/`room_lobby_c`）＋既有的二樓平台，**完全取代**原本從未真正設計過的大門廳/廊廳/梯廳（`placeFixedRoom` 原本把所有固定房間的 `doorSides` 寫死成 `ALL_SIDES`）。座標：`room_lobby_b`＝(0,0)（原大門廳位置，四面皆門）、`room_lobby_a`＝(0,1)（南／下方，東西北三門，南側室外正門僅裝飾不算 doorSide）、`room_lobby_c`＝(0,-1)（北／上方，西南兩門，東側鎖死地下室裝飾不算 doorSide，B1 尚未開發）、`room_upper_landing`＝二樓 (0,0)（北東西三門，南側走既有 `stairsLink`/`useStairs` 機制不是 doorSide）。`starting-rooms.json` 新增 `filename` 欄位（`LobbyA/B/C.webp`、`2Fladder.webp`）。**前端**：新元件 `client/src/gameplay/EntranceHallView.jsx`，玩家位於大門廳三格任一格時，固定順序（C 上／B 中／A 下）完整堆疊顯示三張圖（圖片本身已設計成無縫拼接，不做鄰居裁切/淡化），依當前房間真實 `doorSides` 動態產生方向按鈕，`room_lobby_c` 額外顯示「上二樓」按鈕。`DebugGameScreen.jsx` 新增**獨立的 `roomContent` state**（從 `initialGameState.roomContent` 一次性初始化，**不可**放進 `gameState` 一起被 `game:stateUpdate` 覆蓋掉，因為那個廣播的 payload 不含 `roomContent`——這是實作時特別注意、也在程式碼留了註解的陷阱），依玩家目前房間 ID 條件渲染 `EntranceHallView` 或退回原本的通用 N/E/S/W 按鈕。這次改動雖然觸及 8 個檔案（含 6 個既有測試檔的座標修正，多個測試把「原點南北方向必定是空格」當隱含假設在用，跟新大門廳佔用的座標衝突，逐檔排查後改用遠離大門廳格子的座標），但機制單一、設計已完全確認，判斷不需要走完整 subagent-driven-development，改為 agent 自己在獨立 worktree 直接施作＋TDD，426/426 測試全綠。詳見 [設計文件](docs/superpowers/specs/2026-08-14-entrance-hall-and-start-screen-design.md)、[實作計畫](docs/superpowers/plans/2026-08-14-entrance-hall-and-start-screen.md)
- **M2D3 一般房間地圖骨架＋手動測試修正（已完成並合併進 `main`）**：`client/src/gameplay/mapUtils.js` 的 `getAvailableDirections(player, currentRoom, boardRooms)` 鏡射後端 `turnFlow.js` 的同名邏輯（含 `blocksOpenDoor` 修正效果檢查），純函式不依賴 React；`findRoomInfo(roomId, roomContent)` 同時查 `roomContent.rooms` 跟 `roomContent.startingRooms`（一般房間的鄰居可能是大門廳）。`FocusedRoomView` 只負責視覺（房間圖＋鄰居淡化預覽＋玩家徽章），移動/開門按鈕已搬到 `DebugGameScreen.jsx` 的左欄獨立渲染（見下方版面說明），兩邊各自呼叫一次 `getAvailableDirections`（同一個純函式，接受重複呼叫換取元件職責單純）。**大廳單人可進角色選擇**：`server/src/socketHandlers.js` 的 `game:startCharacterSelect`、`server/src/game/characterSelection.js` 的 `createCharacterSelectionState`、`client/src/lobby/WaitingRoomScreen.jsx` 的按鈕 `disabled` 條件，三處各自獨立的「至少 2 人」判斷全部改成「至少 1 人」（`characterSelection.js` 仍保留「至少 0 人」的 `TOO_FEW_PLAYERS` 防呆）。**玩家圖標依進入方向定位**：`player.enteredFromSide` 欄位（`playerEntity.js`），出生/走樓梯時為 `null`（圖標置中），`turnFlow.js` 的 `moveToRoom` 在兩個 `movePlayerTo` 呼叫點都帶入 `OPPOSITE_SIDE[direction]`（`doorLayout.js` 既有的方向對照表）——移動方向的**反方向**才是玩家實際走進新房間的那面牆。前端 `FocusedRoomView` 的 `badgeStyle` 用百分比＋`transform` 定位（不依賴房間圖片素實際像素尺寸），支援同房間多位玩家依各自 `enteredFromSide` 分別定位、同側多人自動 stagger 錯開。**道具真實名稱**：`game:started` 廣播新增 `cardContent:{items,events,omens}`（比照 `roomContent` 模式，一次性靜態資料，前端獨立 state 儲存），`CharacterPanel` 的 `findCardName(id, cardContent)` 依序查三個牌庫（背包道具可能來自任一牌庫，不只 `items`，見上方 M2c-3 除錯注意事項的「預兆牌也會進背包」）。
- **版面最大化置中的 CSS 響應式作法（M2D3，已完成並合併，見上方「目前的瓶頸或停頓點」等待手機實測）**：`DebugGameScreen.jsx` 的 `phase==='playing'` 版面是左中右三欄 flex row：左欄（移動/開門按鈕＋總覽切換按鈕＋標題＋錯誤訊息，`phase==='character_select'` 時標題維持原本置頂不動）／中欄（固定寬度＝`var(--total-square)`，內部用 `justifyContent:'center'` 置中渲染 `FocusedRoomView`/`OverviewMap`）／右欄（`CharacterPanel`），左右兩欄都是 `flex:1, minWidth:0`（平分中欄以外的剩餘空間）。三個 CSS 自訂屬性定義在最外層 flex row 上：`--total-square: calc(100vh - 16px)`（視野正方形邊長，純高度驅動，16px 只是最小緩衝，標題已搬進左欄不佔用這個預算）、`--tile-size: calc(var(--total-square) * 0.7)`（房間圖邊長）、`--peek-size: calc(var(--total-square) * 0.15)`（四邊鄰居預覽寬度，`FocusedRoomView` 的 `peekStyle` 直接讀這個變數）。整套響應式全靠 CSS `calc()`／`var()`，沒有用 JS `window.innerHeight`／resize listener，視窗縮放會自動即時反應。**已知未驗證**：行動裝置瀏覽器的 `100vh` 常有位址列動態顯示/隱藏造成的高度跳動問題，桌面瀏覽器沒有這個現象，尚待開發者手機實測才會知道要不要處理。
- **卡牌/房間內容分工**：agent 依 `card-mechanics-reference.md` 草擬 `effects` JSON，開發者審核修正（跟房間 `doors`/角色數值「開發者自己手填」不同，因為這是聲明式 schema 轉換）
- **MVP 兩個劇本**：劇本1〈神鬼痴漢 The Mummy Walks〉、劇本10〈闔家團圓 Family Gathering〉
- **未來階段**：Phase 2 為 AI 玩家（呼叫 Claude API 決策，選角色順序排真人之後、數量不可超過真人數量）；Phase 3+ 為原創劇本
- **PDF 內容抽取**：用 `pymupdf`（`import fitz`），不要用 `pypdf`。抽取結果不進版控
- **版權**：規則書/卡牌內容屬 Hasbro/Avalon Hill 版權，僅供私人非商業用途
- **語言偏好**：與開發者對話一律使用繁體中文
- **Worktree 慣例**：每個里程碑開獨立 worktree/分支，完成後合併回 `main`；`main` 分支保持乾淨可執行
- **大型計畫拆分**：單一里程碑若預估任務數明顯超過前一個里程碑，要主動跟開發者確認是否拆成多份計畫
- **測試競態的處理先例**：房間廣播事件（`game:prompt`/`game:promptResolved` 等），測試裡等待「下一次廣播」時若只用單一 client 的 `.once`，可能誤收到還在飛行中的前一次廣播——已確立的修法是 `Promise.all` 等所有相關 client 的 `.once` 都先收到才繼續；同類問題以後不用再問，直接套用
- **獨立審查慣例**：inline execution（非 subagent-driven-development）完成的里程碑，合併前應該補一次獨立整分支審查（`requesting-code-review` 技能），不能只靠自己寫的 TDD 測試就假設沒問題——M2c-2 就是靠這次審查才抓到 C1 這個 Critical bug
- **收工流程**：每階段收工前需生成/更新 worklog、chatlog、Handover，並推送至 GitHub repo；需確認本次 session 自行啟動的本機伺服器已關閉
- **手機橫直式並存版面（2026-08-17，已完成並合併進 `main`）**：`client/src/gameplay/playingLayout.css`（新檔案，`DebugGameScreen.jsx` 引入）用 CSS Grid 定義 `.playing-layout` 容器，預設（橫式）`grid-template-columns: var(--total-square) 1fr`／`grid-template-areas: 'viewport panel'`；`@media (orientation: portrait)` 覆寫成 `grid-template-rows: var(--total-square) 1fr`／`grid-template-areas: 'viewport' 'panel'`（單欄堆疊）。`--total-square` 橫式 `calc(100vh - 16px)`、直式 `calc(100vw - 16px)`，`--tile-size`/`--peek-size` 兩種模式共用同一組 70%/15% 公式定義在同一個規則裡（改 `--total-square` 就會連動）。容器本身 `position:fixed; inset:0; overflow:hidden`（＋`body{margin:0}`）完全鎖死捲動，`.playing-layout__viewport`/`.playing-layout__panel` 都補了 `min-width:0; min-height:0`（CSS Grid 項目預設 `auto`，不補會讓 `overflow:hidden` 失效，見上方除錯注意事項）。判斷用哪個版面完全交給瀏覽器 `orientation` media query 自動切換，沒有任何 JS 判斷邏輯。
- **遊戲進行畫面精簡為兩區域（2026-08-17）**：`DebugGameScreen.jsx` 的 `phase==='playing'` 只剩 `.playing-layout__viewport`（房間視野）跟 `.playing-layout__panel`（狀態面板）兩個 `<div>`。原本的除錯用標題/房號/錯誤訊息文字段落、底部 `<pre>{JSON.stringify(gameState)}</pre>` 全量傾印都已移除；錯誤改用 `console.error('[事件名稱]', err)`（8 個動作 handler 都已補上前綴 tag，方便在瀏覽器主控台篩選）。`pendingEffectChoice`/`pendingRollChoice` 這兩種需要玩家選擇的提問，改成蓋在整個畫面上的 `position:fixed` 置中彈窗（半透明黑底），不佔用面板的固定版位，跟後續新增的道具使用彈窗共用同一種「`position:fixed;inset:0` 半透明黑底＋置中白色卡片」樣式模式。
- **視野正方形內的浮動按鈕（2026-08-17）**：兩類按鈕都用 `position:absolute` 疊在房間圖片上（皆有 `z-index:10` 確保不被蓋住）：
  - **方向按鈕**（`FocusedRoomView.jsx` 的 `directionButtonStyle`）：定位在對應方向 15% 鄰房預覽帶（`--peek-size`）的正中央，文字依 `d.kind` 顯示「移動」（已知房間）或「開門」（未知房間），不再顯示東西南北文字（方位由按鈕位置本身表達）。`FocusedRoomView` 現在直接吃 `directions`（`DebugGameScreen.jsx` 算好的完整清單，含 `move`/`open_door` 兩種 kind）跟 `onMove` 兩個 props，不再自己重算一次。
  - **四角動作按鈕**（`DebugGameScreen.jsx` 的 `cornerButtonStyle`）：行動（左上）／總覽地圖切換（右上）／襲擊（左下）／回合結束（右下），定位在視野正方形四個角落「房間圖＋十字形鄰房預覽帶」都覆蓋不到的空白 `--peek-size × --peek-size` 方格正中央。這 4 顆按鈕是 `.playing-layout__viewport` 內、`FocusedRoomView`/`OverviewMap` 的**同層 sibling**（包在一個額外的 `position:relative; width/height:var(--total-square)` wrapper 裡），不是塞進 `FocusedRoomView` 內部——因為總覽地圖模式時 `FocusedRoomView` 根本不會渲染，如果按鈕跟著放在 `FocusedRoomView` 裡面，切到總覽地圖就會連唯一的「切回聚焦畫面」按鈕都不見，永久卡住。**這是通用教訓：任何「不管目前顯示哪個子畫面都要存在」的全域控制項，都要放在子畫面共同的父層，不能塞進某一個子畫面元件內部**
- **樓梯移動改用通用 `move_to_room` 效果類型（2026-08-17，已合併進 `main`）**：新增 `effectResolver.js` 的 `handleMoveToRoom`（`{type:"move_to_room", targetRoomId}`，掃描 `gameState.board` 的每一個樓層 Map，找到 `roomId` 相符的房間就把玩家移過去，樓層無關，`Object.keys(gameState.board)` 天然會跳過 `stairsLink`——之後新增第三樓層完全不用改這個函式）。`data/rooms/starting-rooms.json` 的 `room_lobby_c`/`room_upper_landing` 互相指向對方，各自補上 `"effects":[{"type":"move_to_room","targetRoomId":"..."}]` 跟 `"freeAction":true`。`freeAction` 這個新欄位串連到 `socketHandlers.js`（`selectOptions.freeRoomAction = Boolean(roomDefinition.freeAction)`）跟 `turnFlow.js` 的 `selectAction`（`room_action` 分支看到 `options.freeRoomAction` 就跳過扣行動力）——**這是通用機制，不是樓梯專屬**，之後任何「應該免費的房間操作」都可以直接在該房間資料補 `freeAction:true`。前端「樓梯」按鈕與 `handleUseStairs` 已移除，改成站在 `room_lobby_c`/`room_upper_landing` 時按四角的「行動」按鈕觸發（跟其他 `room_action` 共用同一個按鈕/流程）。**底層 `useStairs`/`canUseStairs`（`turnFlow.js`）／`game:useStairs`（`socketHandlers.js`）三個函式本身沒有刪除**，前端已經不會呼叫，屬於已知的、刻意保留的死碼（有既有測試覆蓋，且開發者只要求前端不要用單獨按鈕，沒有要求砍掉底層機制）。**重要提醒：後端伺服器是純 `node src/index.js` 執行，沒有監看檔案機制，改 `starting-rooms.json`/`effectResolver.js`/`turnFlow.js` 這類伺服器讀取的檔案，一定要重啟伺服器才會生效**（這次因為忘記重啟卡在 `NO_ROOM_ACTION_AVAILABLE` 好一陣子，見上方除錯注意事項）。
- **狀態面板 `CharacterPanel.jsx` 最終版面（2026-08-17，已合併進 `main`，經過 3 輪迭代定案）**：面板整體 `flexDirection:column` 分上下兩段——**上段「中央狀態區」**（`flex:3`，佔面板 75% 高）本身 `display:flex` 分左右：**左 80%**（`flexDirection:column`）由上到下依序是姓名/行動力（`flex:2`，字體 24px 置中，換算佔中央狀態區 2/6＝面板整體 25%）、力量/速度/意志/知識各一排（`STAT_ORDER=['might','speed','sanity','knowledge']`，各 `flex:1`，各佔中央狀態區 1/6，文字格式「當前X：Y」＋24×24px 刻度格）；**右 20%** 是道具區，`player.inventory` 每個道具的名稱本身就是可點擊的 `<button>`（不再有獨立的「道具」動作按鈕），點下去用 `useState` 開啟一個 `position:fixed` 置中彈窗（跟 `pendingEffectChoice` 同一種樣式），顯示「使用」／「遺留」／「取消」三個按鈕（`onSelectAction('item', {itemId, mode:'use'|'leave'})`；**「給予」尚未實作**，需要同房玩家名單，見下方「目前的瓶頸或停頓點」）。**下段「訊息欄」**（`flex:1`，佔面板 25% 高）跟整個面板同寬，不再分左右，虛線框顯示大小，訊息陣列反轉後渲染（`[...messages].reverse()`）讓最新訊息固定顯示在最上方，`overflow:hidden` 裁掉舊訊息而不是捲動（維持整個遊戲畫面完全鎖定捲動的原則）。**這次順便修正一個從很早就存在、直到現在才發現的缺口**：舊版「道具」按鈕的 `handleSelectAction(actionType)` 從來沒有接收/傳遞 `itemId` 參數，代表這顆按鈕從功能上線那天起就不可能真的指定要用哪個道具，一按必定在伺服器端因為 `itemId` 是 `undefined` 而拋 `ITEM_NOT_HELD`——這次順手把 `handleSelectAction` 擴充成 `(actionType, options={})`，`{actionType, ...options}` 一起送給伺服器，才第一次真正把 `itemId`/`mode` 傳過去。
- **地下室新機制設計（2026-08-16 已跟開發者確認，資料已寫入 `rooms.json`/`starting-rooms.json`，程式邏輯全部未實作）**：
  - **崩塌的房間 `room_collapsed_room`**：`floor:"ground"`，速度檢定 5+／骰子物理傷害沿用官方 `_rule.pdf` 原文，掉落位置改用開發者自創的「正下方同座標地下室」規則（不是官方原文的「接到隨機已存在地下室房間」）
  - **舞廳 `room_ballroom` ↔ 包廂房 `room_gallery` 雙向綁定**：生成其中一間時需在對應樓層同座標同步生成另一間；位置衝突則整個抽卡動作重抽；牌堆僅存最後一張的例外改隨機位置生成，不強制同座標。`room_ballroom` 的 `floor` 已從 `"any"` 改為 `"ground"`（綁定邏輯需要明確樓層對應）
  - **2026-08-17 更新：兩者程式邏輯皆已實作完成並合併進 `main`**，`needsCustomLogic:true` 標記與 `text` 欄位維持原樣不動（`needsCustomLogic` 這個欄位在這兩間房的語意仍然成立：即使程式邏輯已經寫好，也不代表房間的「一般結束回合/離開考驗」通用機制適用於它們，維持這個旗標是刻意的，不是遺漏）。技術細節見下方新增的「三樓層＋座標式房間連接機制」條目。
  - 「開鎖道具解開大門廳地下室門」「原創逃生通道 B1/1F 房間」這兩個開發者最初提出的機制構想，目前只在對話中討論過，**尚未落成任何資料或設計文件**，仍待開發者未來提出時再確認細節（目前地下室平台唯一的抵達方式是崩塌房間掉落，見下方新條目）
- **三樓層＋座標式房間連接機制（2026-08-17，已完成並合併進 `main`）**：`boardGenerator.js` 的 `FLOORS` 新增 `'basement'`；`createBoard` 新增 `basement` Map 並固定放置 `room_basement_landing`（北/東/西三門，跟二樓平台同形狀），但**目前沒有任何樓梯/門可以直接走到地下平台**——它單純是「地下室這個 Map 裡已經有一個錨點房間」的角色，實際抵達地下室的唯一途徑是下面的崩塌房間掉落機制。`turnFlow.js` 的 `moveToRoom` 新增：①崩塌的房間（`room_collapsed_room`）速度檢定 5+（`COLLAPSE_CHECK_STAT`/`COLLAPSE_CHECK_MIN` 常數，沿用既有 `pendingRollChoice` 道具介入擲骰模式，`resumeCollapseCheck` 是介入流程回復後的接續函式），失敗時 `applyCollapseCheck` 在正下方**同座標**生成一張新地下室房間卡並把玩家移過去（連結記在 `placedRoom.collapseLink`，供之後查詢「這間崩塌房間通到哪間地下室」用）；②舞廳／包廂房雙向綁定（`placeBallroomGalleryPair`）：生成其中一間時同步在對應樓層同座標生成另一間，座標衝突則整個抽卡動作重抽，牌堆僅存最後一張時改隨機位置生成、不強制同座標。**已知限制（尚未實作、非本次範圍）**：物理傷害（`_rule.pdf` 原文的「受到1顆骰子的物理傷害」）尚未套用，等 M3 傷害分配系統做出來才會補上，見上方除錯注意事項「M2c-3 盤點期間發現的架構缺口」。
- **`leaveCheck` 房間的「失敗扣屬性」引擎缺口（2026-08-17，已完成並合併進 `main`）**：塔橋/雜亂的房間/藤蔓糾纏的溫室/天花閣樓/五芒星室/墓園/髒亂的房間共 7 間房，`moveToRoom` 現在檢定失敗時會真的呼叫 `changeStat(player, leaveCheck.failPenalty.stat, leaveCheck.failPenalty.delta, gameState.hauntStarted)` 扣屬性（`failPenalty` 為 `undefined` 的房間——塔橋/雜亂的房間/藤蔓糾纏的溫室——照舊不扣，這是官方原文本來就沒有這個懲罰，不是缺漏）。
- **玩家物件的 `characterId` 串接＋一次性 `characterContent` 廣播（2026-08-17，已完成並合併進 `main`）**：`playerEntity.js` 的 `createPlayer` 新增 `characterId` 欄位（未提供則為 `null`）；`gameState.js` 的 `addPlayer`、`gameManager.js` 的 `startGame` 依序把角色選擇階段確定的 `character.id` 傳下去。`socketHandlers.js` 的 `game:started` 廣播新增 `characterContent: content.characters`（比照既有 `roomContent`/`cardContent` 的一次性靜態資料模式，**只在這裡送一次，之後的 `game:stateUpdate` 不會再帶，前端要用獨立 `useState` 存，不能塞進會被覆蓋的 `gameState`**——這是既有慣例，程式碼裡有註解警告）。前端 `FocusedRoomView.jsx` 的 `findCharacterIcon(characterId, characterContent)` 查 `character.fileicon` 組出圖片路徑，`PlayerBadge.jsx` 有 `iconSrc` 就顯示圖片，沒有則維持原本顏色圓圈+字母 fallback。
- **統一考驗彈窗機制＋訊息欄可讀化（2026-08-17，已完成並合併進 `main`）**：設計文件 [docs/superpowers/specs/2026-08-17-check-modal-and-readable-messages-design.md](docs/superpowers/specs/2026-08-17-check-modal-and-readable-messages-design.md)、實作計畫 [docs/superpowers/plans/2026-08-17-check-modal-and-readable-messages.md](docs/superpowers/plans/2026-08-17-check-modal-and-readable-messages.md)（7 任務，`subagent-driven-development` 在獨立 worktree 執行，每任務皆通過獨立審查）。
  - **伺服器端**：`turnFlow.js` 的 `moveToRoom` 補上 `leaveCheckResult`（`{stat, roomId, rolled, required, passed}`，只在有 `leaveCheck` 時才用物件展開附加，不會塞 `null`）；`applyCollapseCheck` 補上 `stat`/`required`。`effectResolver.js` 的 `handleDiceCheck` 把 `diceCheckResult`（`{stat, diceCount, rolled, tierEffects}`）附加到回傳值，`resolveEffects` 比照既有 `drawnCards` 的傳遞模式往上傳。`socketHandlers.js` 的 `finishMoveResult` 依 `result.leaveCheckResult`/`result.collapseResult` 廣播新事件 `game:checkResolved`（`{playerId, checkKind:'leaveCheck'|'collapseCheck'|'cardCheck', sourceKind:'room'|'item'|'event'|'omen', sourceId, stat, rolled, threshold, tierEffects, passed}`），同時廣播新事件 `game:roomEntered`（`{playerId, roomId}`，只在 `result.kind` 是 `'move'`/`'open_door'` 時發送，不含樓梯／崩塌掉落）；`handleCollapseCheckRollPending`/`resumeCollapseCheckRollChoice` 的 `resumeContext` 多帶 `leaveCheckResult`，確保「leaveCheck 通過後又遇到需要道具介入的崩塌房間檢定」這種組合情境也能正確廣播兩則。`resolveCardDraw` 的 `game:cardDrawn` 新增 `hasCheck` 欄位（`!card.activatedOnUse && card.effects.some(e=>e.type==='dice_check')`，**必須排除 `activatedOnUse`**，否則水晶球／面具這類「持有後才主動使用」的卡片會誤報有考驗但實際不會跳出任何彈窗——這是整分支審查抓到的真實 bug，已修正）；`handleEffectResolveResult` 依 `effectResult.diceCheckResult` 廣播 `checkKind:'cardCheck'` 的 `game:checkResolved`，新增 `findSourceKind(content, sourceId)` helper 判斷來源是房間還是哪種卡片。卡片考驗的 `passed` 用「命中那層 `tierEffects` 有負面 `stat_change` 就判失敗，否則成功」的啟發式規則（非資料明確定義，之後如不準確需再討論）。
  - **前端**：新元件 `client/src/gameplay/CheckModal.jsx`（兩階段：考驗前「描述＋考驗屬性/門檻摘要框＋擲骰按鈕」→ 按下後 2.5 秒動畫佔位（`setTimeout`，之後開發者會自行換成真正動畫）→ 考驗結果「綠/紅大字成功/失敗＋一句話敘述＋確認鍵」）。`DebugGameScreen.jsx` 新增 `pendingCheckQueue` 狀態（陣列，每筆有 `queueId` 遞增流水號），監聽 `game:checkResolved` 推入佇列；`onCardDrawn` 若 `hasCheck:false` 也推入佇列（`{noCheck:true,...}`，渲染另一個簡化版「描述＋確認」內嵌彈窗，不用 `CheckModal`）；`<CheckModal key={pendingCheckQueue[0].queueId} .../>`（**務必用每筆考驗自己的穩定 id 當 key，不能用 `pendingCheckQueue.length`**——佇列長度在「附加到隊尾」時也會變，會誤觸發隊首元件重新掛載、打斷玩家正在看的動畫/結果，這是整分支審查第二輪才抓到的真實 regression，見下方除錯注意事項）。`mapUtils.js` 新增 `findCardInfo(cardId, cardContent)`，跟既有 `findRoomInfo` 平行。
  - **訊息欄**：`CharacterPanel.jsx` 訊息列字體從 `'0.8em'`/`'0.85em'` 改成數字 `24`（含無訊息佔位文字）。`DebugGameScreen.jsx` 新增 `findPlayerName(playerId, players)` helper；`onRoomEntered` 產生「{玩家名} 進入了『{房間名}』」；`onCardDrawn` 依 `deckType` 產生對應句型（event：「{玩家名}：發生了 {卡名}」／item：「{玩家名} 在房間裡找到了 {卡名}」／omen：「{玩家名}看到了一個怪異的現象（{卡名}）」）；`onCheckResolved` 的考驗結果訊息（例如「{玩家名}：{屬性}考驗{成功/失敗}（擲出 X 點）」）**刻意延後到玩家按下 `CheckModal` 的確認鍵才寫入訊息欄**（`logMessage` 欄位先算好存在佇列項目上，等 `onDone` 才 `setMessages`），避免玩家透過半透明彈窗背景提前看到結果——這是整分支審查抓到、開發者明確決定的行為，不是隨意設計。沒有 `stat` 欄位的考驗（固定骰數，非依屬性），`STAT_LABELS`/`STAT_LABELS_LOCAL` 對照表 fallback 到空字串，不會印出字面上的 `undefined`。
  - **彈窗堆疊層級**：既有的道具介入擲骰選擇彈窗（`pendingEffectChoice`/`pendingRollChoice`，有 20 秒伺服器倒數）z-index 從 50 提高到 **80**，`CheckModal` 維持 70——確保有真實時限的功能性彈窗一定蓋在純表演性質的考驗彈窗之上，這也是整分支審查抓到、開發者明確決定的優先順序。
  - **整分支審查歷程**：完成 7 任務後，controller 自行手動雙人瀏覽器驗證（24px 字體、`game:roomEntered` 跨玩家廣播、無考驗簡化彈窗皆確認正常），但因隨機抽卡/開門運氣不佳，沒能實機觸發真正的擲骰考驗彈窗流程——如實記錄這個驗證缺口，改用「請整分支最終審查特別聚焦這條路徑」的方式補強信心。最終審查（Opus 模型）確實在這條路上抓到本節開頭提到的 Critical（`CheckModal` 缺 key）與 4 個 Important（`activatedOnUse` 卡片不跳彈窗、undefined stat 文字、訊息爆雷時機、彈窗堆疊層級），修復後複審又抓到修法本身的副作用（key 選用佇列長度不夠精準），開發者同意破例多跑一輪修正後才合併。**通用教訓見上方除錯注意事項的專屬條目**。
- **房間多重行動機制（2026-08-19，已完成並合併進 `main`）**：`rooms.json`/`starting-rooms.json` 的 `actions` 欄位從單純字串陣列改成結構化清單 `[{label, kind, ...}]`，`kind` 目前有 4 種——`search`（重用既有 `performSearch`）、`craft`（重用既有 `craftRecipes` 邏輯，讀 `chosenAction.freeAction`）、`effects`（`chosenAction.effects` 走既有 `resolveEffects`，讀 `chosenAction.freeAction`）、`teleport`（新機制，見下）。**房間頂層 `effects` 語意收斂為專屬「結束回合被動加成」**（只有 `applyRoomEndTurnBonus` 讀取，`room_action` 完全不再讀這個欄位），room_action 觸發的效果一律改放進 `actions[].effects`——這是刻意的語意切割，避免重演搜索機制那次「同一欄位身兼兩種完全不同觸發時機」導致分支優先序打架的問題。**`getRoomActions(roomDefinition, placedRoom)`**：server（`socketHandlers.js`）、client（`mapUtils.js`）各自一份邏輯完全相同的版本（**沒有共用檔案，日後改動邏輯要兩邊同步改**），沒有 `actions` 欄位的房間預設回傳 `[{label:'搜索',kind:'search'}]`；`kind:'teleport'` 的項目在 `placedRoom.roomId === 'room_collapsed_room'` 時，只有 `placedRoom.collapseLink` 已設定（代表這間房已經崩塌過一次）才會出現在清單裡。`game:selectAction actionType:'room_action'` 的 `actionIndex` 驗證規則：**payload 明確帶了 `actionIndex` 就一律嚴格驗證是否落在目前有效清單範圍內**（不是只在清單長度>1 時才驗證——這是 Task 2 implementer 發現的計畫本身缺口，已修正，見下方 Task 2 案例），沒帶且清單長度>1 才拋 `INVALID_ACTION_INDEX`。**新的 `kind:"teleport"`**：`turnFlow.js` 拆成 `resolveTeleportDestination`（純函式，只算目的地座標並驗證存在，找不到拋 `NO_TELEPORT_TARGET`，不做任何狀態變更）＋`performTeleport`（呼叫前者，驗證通過才真的 `movePlayerTo`），涵蓋兩種情境：①崩塌房間的「跳下」——讀 `placedRoom.collapseLink`（既有欄位，記錄「這間房是從崩塌檢定失敗生出來的，掉落到哪個地下室座標」），**現在消耗 1 行動力**（跟第一次崩塌檢定失敗的「不可抗拒摔落」是兩回事——摔落本身依然免費、不扣行動力，這裡改的只是「崩塌後玩家自己選擇再跳下去」的情境，兩者要分清楚，不能混為一談）；②包廂房/舞廳新的「跳下」——用既有 `isBallroomOrGallery`/`pairedFloorFor`/`pairedIdFor`（`placeBallroomGalleryPair` 既有的座標配對邏輯）算出對應樓層同座標的房間，消耗 1 行動力，不需考驗（掉落傷害留給 M3），**目的地一定要驗證真的存在且 id 相符才能移動**——因為 `placeBallroomGalleryPair` 本身有兩條例外路徑（座標衝突時改隨機位置放置、牌堆抽空時直接 `return null`）會讓舞廳/包廂房實際上不在同座標，這是整分支審查抓到的真實 bug（詳見下方案例）。`socketHandlers.js` 的 teleport 分支：`resolveTeleportDestination` 必須在 `selectAction`（扣行動力）**之前**呼叫，驗證失敗就不扣行動力；`ack(result)` 必須在 `performTeleport` 真正移動完成**之後**才送，不能提前——這兩點都是整分支審查抓到、原計畫沒注意到的順序問題。**整分支最終審查發現的實際案例（已修復，供未來同類流程參考）**：包廂房/舞廳跳躍原本完全沒驗證目的地是否存在就直接移動過去，可能讓玩家卡在空座標或讓程式拋錯；`ack` 在跳躍真正發生前就送出，失敗時玩家端會收到假成功但行動力已經扣了——這兩個問題的修法（先驗證後扣行動力、成功後才回應）跟 dice-interjection Part A 那次「consume-before-validate」的教訓是同一類，**再次印證「任何會消耗玩家資源的動作，驗證永遠要排在消耗之前」這條通用教訓不會因為換了功能就自動免疫，每次新增涉及資源消耗的動作都要重新檢查一次順序**。詳見 [設計文件](docs/superpowers/specs/2026-08-19-multi-room-action-design.md)、[實作計畫](docs/superpowers/plans/2026-08-19-multi-room-action.md)
- **手機/區網連線設定（2026-08-19 第 2 次工作階段，已修復並合併進 `main`）**：`client/src/socket.js` 的連線網址判斷式是 `import.meta.env.VITE_SERVER_URL || http://${window.location.hostname}:3001`——**任何時候 `VITE_SERVER_URL` 這個環境變數有值，都會完全蓋過動態網址邏輯**，之前踩過一次就是 `client/.env.development`（Vite 開發模式自動載入的環境檔）裡殘留 `VITE_SERVER_URL=http://localhost:3001`，已刪除該檔案；**這條規則要記住：以後不要在 `client/.env*` 系列檔案裡設定 `VITE_SERVER_URL`，除非明確要覆蓋成固定值（例如指向遠端測試伺服器），否則會讓所有非開發機的裝置（手機等）永遠連到自己的 `localhost`**。`vite.config.js` 的 `server.host` 從 `true` 改成明確的字串 `'0.0.0.0'`（`true` 在這台機器上實際監聽的是 IPv6 萬用位址 `::`，不是真正的 IPv4 `0.0.0.0`，雖然同機測試靠雙棧相容能連，但不保證所有情境下都對外可靠，改用明確字串沒有這個疑慮）。後端 `server/src/createServer.js` 本身用 `express.static` 服務 `client/dist`，所以 `http://<IP>:3001` 平時就能當備援測試路徑用（同源、不需要 CORS、不依賴 5173），但只有靜態建置版本，前端程式碼改動後要重新 `npm run build` 才會更新，不會像 `5173` 自動熱重載。**除錯方法論記錄**：手機沒有開發者工具可看 console，這次靠在 `LobbyScreen.jsx` 暫時加一段監聽 `socket.on('connect_error', ...)` 直接把錯誤訊息／`err.description`／socket 實際嘗試連線的 `url` 顯示在畫面上，讓開發者截圖回報，才問到 `.env.development` 這條線索——之後如果再遇到手機連線異常但又拿不到 log，可以比照這個做法（暫時加、問題解決後記得拿掉）。
- **手機版面調整必須用手機實際寬度測試，不能只靠桌機瀏覽器（2026-08-19 第 3 次工作階段，血淚教訓）**：這階段一開始沿用先前的習慣在 1280px 桌機視窗調整 `CharacterPanel` 版面數值（例如「道具欄固定 260px」），開發者手機截圖回報「道具欄被擠出螢幕看不到」才發現桌機 1280px 寬下完全合理的固定 px 數值，換到手機實際 390px 寬螢幕直接爆版溢出。之後全程改用 `resize_window` 把 Browser pane 切到 390×844（iPhone 尺寸）再驗證，才抓到問題根源並重新設計。**通用規則：任何遊戲進行畫面（`playing-layout` 底下）的版面調整，驗證一律要在手機寬度視窗下進行，不能只憑桌機視窗「看起來合理」就判定完成**——這個專案的目標裝置本來就是手機直式，桌機視窗測試在這個畫面下沒有代表性。
- **px 改百分比 RWD 的技術限制（2026-08-19 第 3 次工作階段）**：CSS 百分比 `width`/`gap`/`padding` 只有在容器本身是「明確寬度」（definite width，例如 `flex:1`／`width:'100%'`／有明確 flex-basis）時才會算得準；如果容器本身是「自動撐開寬度」（auto，例如只靠 `justifyContent` 定位、沒有明確寬度的 flex 子項），對它的子元素設 `%` 寬度/間距在 CSS 規範上是未定義/不可靠行為。這次的解法是把每一列（`StatRow`、行動力那一列）都明確設 `width:'100%'`，讓它撐滿右側欄位的完整寬度，子元素的 `%` 才有穩定的計算基準；正方形格子（能力級距條格子、行動力格）改用 `flex:1`（平分容器寬度）＋`aspectRatio:'1'`（高度跟著寬度走維持正方形），不再寫死 `width:24,height:24`。字體大小、邊框寬度、圓角、道具選單彈窗（螢幕置中彈窗，跟容器寬度無關）維持 px，這些是刻意的範圍界定，不是遺漏。
- **視野區域房間格局部縮放功能（2026-08-19 第 3 次工作階段，已完成並合併進 `main`）**：`client/index.html` 的 `<meta name="viewport">` 加上 `maximum-scale=1.0, user-scalable=no`，全域鎖死瀏覽器原生雙指縮放——**這是整個 app 共用的單一 HTML 標籤，會影響大廳頁面，不只遊戲畫面**，如果之後大廳頁面也需要開放縮放，需要用 JS 動態切換 meta 標籤內容，這次沒有做。`FocusedRoomView.jsx` 新增獨立的房間格縮放狀態（`zoomLevel`/`pan`，五段固定倍率 `[100,150,200,250,300]`，跟瀏覽器原生縮放完全脫鉤），只作用在中心房間格本身（`RoomTile` 外層包一個 `overflow:hidden` 的 clip 容器），鄰房預覽帶、四角/方向按鈕都不受影響。**縮放按鈕**：放大鏡＋固定右上角、放大鏡－固定左下角（開發者要求分開放，避免擠在同一個角落），依 `zoomLevel` 顯示/隱藏（100% 只顯示＋、頂格 300% 只顯示－）。**拖動**：改用 Pointer Events（`onPointerDown`/`onPointerMove`/`onPointerUp`，不是 Touch Events）統一處理滑鼠與觸控，方便桌機測試也能模擬手機拖動行為；`e.currentTarget.setPointerCapture(e.pointerId)` 包了 try/catch（可能拋錯，若沒包住會讓後續拖動狀態設定不到，這是測試時期實際抓到的問題，不是憑空防呆）。**拖動邊界限制**：`clampPan(pan, zoomLevel, tileSizePx)` 算出「圖片放大後單邊多出來的可拖動範圍」（`(zoom/100 - 1) * tileSizePx / 2`），拖動與每次縮放切換都會重新夾限，避免拖出房間格看到空白。**重置時機**：`useEffect` 監看 `currentRoom.roomId`/`.x`/`.y`，離開這間房間就自動歸零縮放與拖動位移，避免玩家回到同一間房卻卡在上次的縮放狀態。詳見 [設計文件](docs/superpowers/specs/2026-08-19-room-tile-zoom-design.md)。
